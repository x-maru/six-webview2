# 正規表現

## 正規表現（Sixで使える仕様）
- Six の検索(`/`,`?`)および置換(`:s`)はJavaScriptの正規表現(ECMAScript)に準拠して解釈します。
- 検索では`^`,`$`は常に各行の先頭/末尾にマッチ（内部的に`m`フラグを付与）
- `.`は改行にマッチしません。改行を含めて任意文字にしたい場合は`[\s\S]`や`(?:.|\n)`を使用してください

## フラグ
- `/pat/i`,`?pat?i`で`i`(大文字小文字無視)を指定可能。`m`は常に有効（指定不要）
- `:s`のフラグは `g`(行内で全置換) / `i`(大小無視) / `c`(要確認) / `n`(件数のみ)
- それ以外のフラグ（例: `s`,`u`）は未対応

## 主な構文
- `.`任意の1文字（改行以外） / `[]`文字クラス / `[^...]`否定文字クラス
- `^`行頭 / `$`行末 / `\b`単語境界 / `\B`非単語境界
- `* + ? {m,n}`繰り返し（`*? +? ?? {m,n}?`で最短一致）
- `()`グループ / `(?:...)`非捕捉グループ / `|`選択（OR）
- `(?=...)`先読み / `(?!...)`否定先読み / `(?<=...)`先行後読み / `(?<!...)`否定先行後読み
- 代表的なショートハンド: `\d`(数字) / `\w`(英数_) / `\s`(空白)

## 置換の特殊シーケンス
- `$&`マッチ全体 / `$1..$9`キャプチャグループ1〜9
- '$\`'（マッチより前）、`$'`(マッチより後)、`$+`(最後のキャプチャ)は現状未対応

## 文字クラス
- `[abc]`文字 'a/b/c'のいずれかに一致
- `[^abc]`上記以外（否定）に一致
- `[a-z]`小文字英字の範囲
- `[A-Za-z0-9_]`英数字とアンダースコア（`\w`と同等）
- `[ \t]`スペースまたは TAB（`\s`は空白全般）
- `[\w.-]`英数_ に加えてピリオドとハイフン
- クラス内のリテラル`-`は先頭/末尾に置くか`[\-]`のようにエスケープ
- クラス内の`]`は`\]`でエスケープ
- POSIX 文字クラス`[[:digit:]]`などは未対応（JavaScript 正規表現仕様）

## デリミタとエスケープ
- 検索`/pat/`,`?pat?`の区切り文字や、置換`:s/pat/repl/`の区切り文字`/`をパターン内で使う場合は`\/`のようにエスケープ
- フラグは末尾の`/i`や`?i`のみ解釈。パターン内の`?`は通常どおり量指定子として扱われます

## 例
- `/^\s+$/`空白のみの行
- `/\bfoo\b/`単語 `foo` に一致
- `/(foo|bar)/i`大文字小文字を無視して`foo`または`bar`
- `:s/^/# /`行頭に`# `を付与（現在行）
- `:%s/(^|\n)\s*TODO(?!:)/$1TODO:/g`TODO の後ろにコロンを付ける（既にある行は除外）
- `/\t/`TAB 文字に一致
- `:%s/\t/    /g`すべての TAB を半角スペース4つに展開
- `:%s/^\t+//g`行頭の TAB を削除（インデントの TAB 抜き）
- `:%s/[ \t]+/ /g`連続する空白（スペース/TAB）を単一スペースに圧縮
