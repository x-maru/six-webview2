#399
- `F9`でヘルプダイアログ表示 →OK
- :b popupから切り替えると一瞬「EOFが見える表示範囲」になったあと正しい表示範囲になる。NG
  - :b popupからマウスクリックした際も同様

#400
変化なし。バッファ1が表示されているときにpopupからバッファ1を選択しても一瞬EOF画面に飛んでしまっている。

#401
悪化した。:b popupから選ぶと一瞬だけでなく「EOFが見える表示範囲」になったままになる。
- 「EOFが見える表示範囲」のとき、画面内先頭行前に隙間ができ、そのため画面内全行で「本文・ガター」と「本文背景の行グラデーション」とがズレてしまっている(添付スクショ)。

#402
一瞬だけ「EOFが見える表示範囲」になる症状に戻った。
そのままさらに:b popupで同じバッファを選ぶと#401の添付スクショのように「EOFが見える範囲＆行番号と本文がズレた状態」になってしまう。
F1やF2でダイレクトに切り替えて戻ってくると直っている。

#403
変化なし。
:b popupで例えば`2`が押下されたら「`Esc`, `F2`」と操作すればチラつき無しでバッファ2を表示できるのだから、まったく同じ処理を入れる or 「Esc + F2」の処理に一本化すればよいのでは？

#404
- 「:b → 同一バッファを Enter/クリックで確定しても、EOFフラッシュや半行ズレが残らないこと」→OK
- 「:b → 異なるバッファを Enter/クリックで確定しても、フラッシュが目立たず、表示が安定すること」→OK
- :b → 同一バッファを数字でダイレクト確定 →NG
  - 背景グラデーションと半行ズレた状態でEOF画面のままになる
- :b → 異なるバッファを数字でダイレクト確定 →NG
  - 一瞬EOF画面が見える

#405
変化なし。:b popupからEnter/clickで確定した場合はFnキーダイレクトと同じ挙動で問題なし。
数字ダイレクトの場合はチラつきあり。数字ダイレクトだと:bの「CMD終了時の復元処理」をバイパスしているのでは？

#406
- :b → 異なるバッファを数字でダイレクト確定 →OK
- :b → 同一バッファを数字でダイレクト確定 →NG
  - 背景グラデーションと半行ズレた状態でEOF画面のままになる

#407
:b popupで現バッファを数字ダイレクトで選択した際に不要なスクロールが発生するケースがある。
- 引数無しで起動した空バッファ(EOFは行400)だけのとき：
  - `:376'(表示範囲[352,400])→`:b1`→何も起きない →OK。これが期待動作
    - このときの行400は表示最下行かつ完全に見えている状態
  - `:375`(表示範囲[351,399])→`:b1`→表示範囲[357,400]+余白5行 →caret行375も見えているので「完全に不具合」ではないが不要なスクロール。
  - `:357`(表示範囲[333,381])→`:b1`→表示範囲[357,400]+余白5行 →caret行357も見えているので「完全に不具合」ではないが不要なスクロール。
  - `:356`(表示範囲[332,380])→`:b1`→表示範囲[357,400]+余白5行 →caret行356が画面外になるので完全にNG
    - 行1から行356まで同じ。

#408
何も変わっていない。#407の挙動そのまま。
少なくとも#407の手順内で半行ズレは発生していないが、半行ズレ矯正の処理で不要なスクロールをしているのでは？

#409
何も変わっていない。#407, #408の挙動そのまま。

#410
不要なスクロールは抑止されたようだが、代わりに具合が混入した。
- 表示中のバッファを「:b `数字`」で選択したあと、キーボードフォーカスが失われてキー入力を受け付けなくなる。
  - コマンド入力欄をclickして`Esc`するとフォーカスがtextareaに戻る
  - 数字ダイレクトではなくclickで選択した場合も同じ症状
  - 数字ダイレクト/clickではなくEnterで選択した場合は発生しない(フォーカスは失われない)
- :bで別バッファに移ったあと、直後のキー入力(任意キー)でEOF付近にジャンプする
  - caret位置は変わらないので、hjklなどで動かすと正しい表示位置に戻る
  - :b popupから数字ダイレクトで選択してもEnter/clickで選択しても症状は同じ

#411
- キーボードフォーカスが失われる現象は解消した。OK
- :b popupから「数字ダイレクト/Enter/click」で別バッファに移ったあと、直後のキー入力(任意キー)でEOF付近にジャンプする
  - caret位置は変わらないので、hjklなどで動かすと正しい表示位置に戻る
  - バッファにより、EOF付近にジャンプした際に行番号と本文テキストが下にずれる(表示最上行の上に隙間ができる,背景グラデーションは正常)現象が発生するものとしないものがある
    - 起動時のuntitledバッファではズレは発生しない
    - six.ps1バッファでは発生する

#412
症状変わらず。
- :b popupから「数字ダイレクト/Enter/click」で別バッファに移ったあと、直後のキー入力(任意キー)でEOF付近にジャンプする
  - caret位置は変わらないので、hjklなどで動かすと正しい表示位置に戻る
  - バッファにより、EOF付近にジャンプした際に行番号と本文テキストが下にずれる(表示最上行の上に隙間ができる,背景グラデーションは正常)現象が発生するものとしないものがある
    - 起動時のuntitledバッファではズレは発生しない
    - six.ps1バッファではズレが発生する

#413
症状変わらずだが、:b popup固有の現象ではなかった。Fnキーダイレクトでも、タブclickでも同じだった。
- 別バッファに移ったあと、直後のキー入力(任意キー)でEOF付近にジャンプする
  - caret位置は変わらないので、hjklなどで動かすと正しい表示位置に戻る
  - バッファにより、EOF付近にジャンプした際に行番号と本文テキストが下にずれる(表示最上行の上に隙間ができる,背景グラデーションは正常)現象が発生するものとしないものがある
    - 起動時のuntitledバッファではズレは発生しない
    - six.ps1バッファではズレが発生する

#414
ソースが壊れたようなので#413直後のソースに戻した。
バッファ切替後の最初のキー入力で表示範囲が狂う現象が発生することがあるのを修正しようとしているところ。
- 下記の不具合を修正して。
  - バッファを切り替えた場合(タブclick、F1～F8ダイレクト、:b popup{数字,Enter,click}
    - 切替直後はOK。
    - 切替直後に何かキーを押下すると
      - EOFが見えるような表示範囲になってしまう(NG)
      - caretはそのまま(画面外になることも) (NG)
      - 行番号と本文テキストの最上行の上に余白ができ、下にズレる(背景行グラデーションはズレない)。(NG)

#415
下記の手順で試した。まだNGがある。
- scrolloffはデフォルトのまま3
- 表示行数49
- F2, gg, :20
  - caretは画面中央付近
- F1, F2
  - OK。正常な表示
  - case1) j or k → caret行(行19)が画面内の上から4行目になる →NG。まだscrollは不要なはず。
  - case2) h,l,or other
    - → EOFが見える表示範囲になる(caretは画面外)。→ NG。
    - → 行番号と本文テキストの最上行の上に余白ができ、下にズレる(背景行グラデーションはズレない)。→NG

#416
バッファ切り替え時の不要なスクロールは抑止された。OK。
- six起動時に最後に開いていたバッファ群を再現したい。
  - 未保存のバッファもそのまま再現できるようにする
  - いずれか1つのバッファで何か変更があったとき(＝undoバッファに積まれたとき)に全バッファの情報を記録する
  - caret位置、表示先頭行の情報も再現する
  - バッファの順番も再現する
  - 最後にアクティブだったバッファをアクティブにする
  - 仕様として矛盾や規定不足があれば指摘して。

#417
- ウインドウ右上のクローズボタン→[移動]で終了したあと引数無しで起動した場合
  - 未保存バッファのmodifiedフラグが再現されるが、undoが効かない。
- `Q`を実装しよう。
  - Vimの`Q`とは別物
  - "ウインドウ右上のクローズボタン→[移動]"(確認なし)と同等
- `:q`→"Don't save"や`:qa`→"Discard all"で閉じても次回起動時に復活する
  - localStorageへのセッション保存は「常に実施済み」であり、`:q`時に"Don't save"を選んだり`:q!`だったりした場合は本来の意味どおり"バッファ破棄"であり、破棄した状態をlocalStorageにセッション保存すればいい。
- ウインドウのクローズボタンが押されたときに未保存バッファの存在有無に関わらず終了するようにはできる？([移動][キャンセル]のダイアログも出さない)

#418
- `Q`で閉じてから再度sixを起動した際にセッションが復活している。→意図通り。
  - `Q`で閉じる際には何もセッション情報を保存しないが、復元に必要な情報は常に保存済の状態だという認識。わざわざ復元情報を破棄する機能ではない。
    - その仕様であればウインドウのクローズボタンも同等にできる？
- `Q`は廃止して`F10`にしよう。`Q`を引き継ぐ。INSERT/VISUALでも受け付ける。
- INSERTモード中に他バッファに移って戻ってくるとキー入力を受け付けなくなっている。コマンド入力欄をclickしてEscで抜ければ復帰する。
- セッションに保存するUndoのバッファレベルを_six.customizeで定義できるようにしよう。"UNDO_STEPS_IN_SESSION"ではどうか。今は1のようだが、5にしよう。

#419
- INSERTモード中に他バッファに移って戻ってくるとキー入力を受け付けない現象は改善されていない。
  - 他バッファに移る代わりに`F10`で閉じて復帰した場合も同じ現象になる
- セッションに保存されるUndo段数は「SIX_OPTIONS.UNDO_STEPS_IN_SESSIONが定義されていなければ1」のつもりだった。
  - 加えて、SIX_OPTIONS.UNDO_STEPS_IN_SESSION = 5と入れといて、というつもりだった
- ウインドウ右上クローズもF10と完全に同じにしたい。
  - 現状でもそうなっているように見えるが、何か違いはある？
- オーバーレイパレットにボタン"即時終了^F10" (^は改行の意)を追加して。ヘルプボタンの上(上段右端)。
- 編集中などに`F10`で抜けても再現されない情報はないように思えるが、ひとつ前の仕様「空セッション保存」は何を意図してた？
- ヘルプの「全体」タブに`F10`の記載を。

#420
 即時終了ボタンの挙動はOK。レイアウトがNG
  - オーバーレイパレット内のボタンは最終的に横3x縦2段または横4x縦2段を考えている。
  - 即時終了ボタンはパレット内の上段の右側。現段階では2x2で左上を空欄にする
  - 即時終了ボタンのラベル後半は"^F10"ではなく"F10"
- オーバーレイパレット内のボタン上にマウスカーソルが来たときだけそのボタンのBGを赤っぽい色にしたい。
- 再現条件がわからないが、INSERTモードでcaretのひとつ上の行に挿入される現象が起きた。桁はあっているしcaretも追従している。
  - このとき画面内最上行の本文・行番号の上に余白が発生し、背景行グラデーションとのズレが起きている(添付スクショ)。
- INSERT中に即時終了したバッファで次回起動時、最初のキー入力がひとつ消費されて無視される。

#421
- オーバーレイパレット内のレイアウトはいい感じだが、「即時終了^F10」の古いボタンが残って3x2になってしまっている。NG
  - マウスオーバーの赤表示 →OK
- `F10`/クローズボタンの挙動 →OK。INSERT中の即時終了でも最初のキー入力が無視される現象は解消した。
- INSERT中のバッファ切替などでcaret行とズレて挿入されてしまう現象は今のところ発生していない。
- ヘルプダイアログで`F9`が効かなくなっている
- ヘルプダイアログ[全体]タブで、「即時終了」を「ウインドウ」等と同じセクションに分けてサブセクションで「ウインドウのクローズボタン」と「F10」「ボタンクリック」を設けて。
  - 未保存の編集内容、NORMAL/INSERT/VISUALの編集モードやUndoバッファも保存・再現される旨を記載。

#422
- ヘルプダイアログの内容・動作 →OK
- オーバーレイパレットの背景部分の透明度を上げてみて。
- 質問：
  - INSERTモード以外のとき強制的にIME offにすることは可能？
  - IMEで全角モードになってるときだけcaret色を変えることは可能？

#423
- また「先頭余白・背景行とズレ・caret行と挿入位置がズレる」現象が再発した。
  - 他バッファに移って戻ったりNORMALに戻して即時終了したりしても直らない。
  - `gg`で先頭に飛ぶと解消するが、`G`でEOF行に飛ぶと再発する。EOF行が見えない範囲の行に飛んでから`j`連打でEOFまで進んだ場合は大丈夫
- INSERTモードでIME onにしたままNORMALに移行してもIME onのまま変わらない。
― IMEで全角モードにしてもcaretの色は変わらない。

#424
- 「先頭余白・背景行とズレ・caret行と挿入位置がズレる」現象は改善していない。
  - 全87行程度の_six.htmlで再現する。`G`で末尾に飛ぶだけで発生。
    - `gg`も含め、`:70`まではズレないしズレがあっても修正される。`:71`から`G`はだめ。
    - `:70`のあと`j`連打でEOFまでcaretを動かしても大丈夫だがその状態から`G`を押すとズレが発生する。
- オーバーレイパレットの背景部分の透明度をさらに上げてみて。
- INSERTモードでIME onにしたままNORMALに移行してもIME onのまま変わらない。
― IMEで全角モードにしてもcaretの色は変わらない。

#425
- NORMALモードで`:`とタイプするとコマンド入力欄に":"が表示されるが、フォーカスが当たっていないので続けて入力できない。NG
  - コマンド入力欄をclickしてやれば正常に続けられる
- 「_six.html で :70 → G 直後」→NG。「先頭余白 + 行背景ズレ + caret挿入オフセット」が発生する
  - _six.htmlのEOFは行89、表示範囲は「余白0.5行ほど＋[46,89]＋余白4.5行ほど」
- INSERTモードでIME onにしたままNORMALに移行してもIME onのまま変わらない。
  - OSはともかく特定のIMEに限定したくない。
  - 代わりに、INSERTモードから他モードに移るときにOSに向けて(?)`英数`キー(VK_OEM_ATTN)が押されたようにイベントを発生させることはできない？
    - `英数`キーであればトグルではなく必ずIME-OFFになるだろう、という前提。
― IMEで全角モードにしてもcaretの色は変わらない。
  - 入力前に色を変えたい意図なので、入力し始めてから色が変わってもあまり嬉しくない。⇒全角モードでのcaret色分けは諦める。

#426
- `:`押下後に続けて入力できるか？ →OK。
- `/`,`?`押下後に続けて入力できるか？ →NG。
- 「_six.html で :70 → G 直後」→NG。「先頭余白 + 行背景ズレ + caret挿入オフセット」が発生する
  - _six.htmlのEOFは行89、表示範囲は「余白0.5行ほど＋[46,89]＋余白4.5行ほど」
- IMEの制御は効いていない。諦めるので、IME制御のために入れたコードは削除しておいてほしい。
- IMEで全角モードかどうかを判定しようとして入れたコードは削除しておいてほしい。
- 「G 直後のあなたのコンソールの debug（先に埋め込んだ metrics）がまだ表示される場合」←何これ？見たことないんだけどどうやったら見れるの？

#427
- `/`,`?`に続けて入力 →OK。
  - `/`,`?`で検索語を確定した後の`n`,`N`が反応しない。NG。
- 「_six.html で :70 → G 直後」→NG。「先頭余白 + 行背景ズレ + caret挿入オフセット」が発生する
  - _six.htmlのEOFは行89、表示範囲は「余白0.5行ほど＋[46,89]＋余白4.5行ほど」

#428
- `/`,`?`で検索語を確定した後の`n`,`N`が反応しない。NG。
- 「_six.html で :70 → G 直後」→NG。「先頭余白 + 行背景ズレ + caret挿入オフセット」が発生する
  - _six.htmlのEOFは行89、表示範囲は「余白0.5行ほど＋[46,89]＋余白4.5行ほど」→添付スクショ

#429
- `/`,`?`で検索語を確定した後の`n`,`N`が反応しない。NG。
```
_six.js:5876  Uncaught ReferenceError: _lastSearch is not defined
    at HTMLTextAreaElement.<anonymous> (_six.js:5876:9)
(匿名) @ _six.js:5876
```
`n`または`N`押下時に下記エラーが出る。`/`のあとでも`?`のあとでも同じエラー。
- 「_six.html で :70 → G 直後」→NG。「先頭余白 + 行背景ズレ + caret挿入オフセット」が発生する
  - _six.htmlのEOFは行89、表示範囲は「余白0.5行ほど＋[46,89]＋余白4.5行ほど」→添付スクショ

#430
- `n`,`N`が反応しなくなっていた件は修正されたようだ。OK。
  - `/`のあとでも`?`のあとでも`N`が2つのマッチ箇所にしかジャンプしない(5ヶ所以上マッチしているのに)。NG。
    - `n`は正常に全マッチ箇所に順次飛んでいく。OK。
- Gで飛んだときに先頭余白が発生して行がズレる件は発生しなくなった。しばらく観察だが一旦OK。
- オーバーレイパレットの背景の透明度をさらに上げて。

#431
- `N`の挙動は修正された。OK。
- `/`または`?`を押下した途端に「:70→G」と同じ現象が起きた。「先頭余白＋行背景ズレ」。
  - 一度検索語を確定すると発生しなくなる。
- オーバーレイパレットの背景の透明度を0.06にしてみて。

#432
- `G`直後に`/`と入力すると`gg`の挙動になる。ただしcaretはEOF行に残っている。NG
  - 直後だけであり、コマンド入力欄を`Esc`で抜けたあと再度`/`と入力しても`gg`の挙動は発生しない。
- `gg`直後に`/`と入力すると`G`の挙動になる。ただしcaretは行1に残っている。NG
  - このとき「先頭余白＋行背景ズレ」の現象が起きる。NG
  - 直後だけであり、コマンド入力欄を`Esc`で抜けたあと再度`/`と入力しても`G`の挙動は発生しない。
- `n`,`N`の挙動 →OK
- パレット背景色 →OK。THEME化は不要。

#433
- `gg`,`G`直後の`/`,`?`の挙動→OK。
- `n`,`N`の挙動→OK。
- 横スクロールで行頭が見えていない状態で`/`と入力すると行頭から表示されるよう横スクロールが発生した上でコマンド入力に移行する →NG
  - `$`や`n`によるcaretジャンプなど、横スクロール発生時の操作には依存しないっぽい(検証不十分だが)
  - `Esc`で`/`をキャンセルしたあと再度`/`を入力しても横スクロールは発生しない。
  - 再度`n`による横スクロールを発生させても再現しなかったりする。初回だけなのか、条件はよくわからない。
  - 即時終了して起動し直すと、再び1回だけ再現するようになる。

#434
- `/`,`?`での挙動 →OK。
- `:`では同様に横スクロール直後の強制スクロールが発生しているので、`/`,`?`と同様の対処を入れてほしい。

#435
- `:`での横スクロール抑止 →OK
- オーバーレイパレットと重なりがちなので、EOF行下の余白行を5行から10行に増やしたい。
- 仕様変更だが、即時終了時もセッション情報を保存するようにしたい。
  - 着目点としては「INSERTでの編集を終えて`Esc`,`F10`としても次回INSERTから再現される」のを改善したい。

#436
- 余白10行 →OK
- 即時終了でのセッション保存、ヘルプ文言 →OK
- `G`直後にEOF行を削除すると「先頭余白＋行背景ズレ」が発生する
  - NORMALモードで`dd`でも発生するしINSERTモードで`Backspace`等でEOF行を全文字削除した瞬間にも発生する
  - 「先頭余白」は余白ではないかも。単に直上の行が中途半端に見えているだけかもしれない。

#437
OK。次はUIの仕様変更。
- INSERTモード時だけアクティブ行グラデーションとcaretグラデーションの色を変更したい
  - window.THEMEに下記で定義
```
  activeEditLineGradStart: "white",
  activeEditLineGradEnd: "#f4f4f4",
  editCaretGradStart: "#ffe000",
  editCaretGradMid: "rgba(255,239,0,0.5)",
```
- 今回の追加分に限らず、window.THEME対応しているのにwindow.THEMEに記述がないものは一律"yellow"で描画するようにしたい。

#438
- さっきの値を_six.customizeのwindow.THEMEに追加して。
- 「未定義なら通常設定」「未定義ならベース値」→代替は一切せず、未定義なら即"yellow"で。
  - window.THEME対応しているものはすべて漏れなくwindow.THEMEに記述する想定で運用したい。なので「記述がない＝事故」扱いなので、記述漏れをいち早く見つける意味でyellowとしている。

#439
- activeEditLineGradStartは反映されているが、activeEditLineGradEndが反映されていない。→NG
- editCaretGradStart/Midがyellowで表示されている →NG

#440
- THEMEが適用されたが、_six.customizeの値を変更して保存し`F10`→起動としても変更が反映されない。
  - 適用するタイミングの変更で起動時に反映できるならしてほしい。
  - 難しいなら`Ctrl+F5`を消費せず素通しするようにしたい。(`F5`は消費)

#441
- セッション再現時も`Ctrl+F5`でも即刻THEMEが反映された →OK
- `F5`が無視されてタブ5に移れない。
- INSERTモードでのactiveガターの背景色にwindow.THEME.activeEditGutterGradStart/Endを使って。

#442
- `F5` →OK
- INSERTモードでのactiveガターの背景色にwindow.THEME.activeEditGutterGradStart/Endが使われていない。NG
  - "#f4f4f4"なんて_six.customize内に見当たらないけど、どこから来た値？
- INSERTモード時のactive行背景のグラデーションがガター部分まで侵食している。NG
- prompt.mdを保存したつもりが失敗していた。下記のエラーがコンソールに出るが、予防的対処は可能？
```
_six.js:7554   POST http://127.0.0.1:59891/write?fs=%5C%5Cwsl.localhost%5CUbuntu%5Chome%5Cymaru%5Cwork%5CWebView2%5Csix%5Cprompt.md net::ERR_CONNECTION_REFUSED
_saveToURL @ _six.js:7554
(匿名) @ _six.js:4026
runCommand @ _six.js:4036
(匿名) @ _six.js:6548
```
  - 即時終了して起動し直したら`:w`で保存できた。

#443
OK。次はWindowsクリップボードに対応する機能を追加したい。
- NORMALモードの`Y`：
  - `y`と同じバリエーションに対応し、yankバッファではなくWindowsクリップボードに格納する
    - `YY`と`Yy`はどちらも`yy`のクリップボード版とする。`yY`はない。
    - 数字前置詞も対応する。`3Yw`など。
    - 格納直後にトーストを出す。"Copied to Windows clipboard."
- VISUALモードの`Y`：
  - 選択中に`Y`でyankバッファではなくWindowsクリップボードに格納する
    - 格納直後にトーストを出す。"Copied to Windows clipboard."
- 仕様として足りない点や不整合があれば指摘して。

#444
OK。問題なさそうなのでヘルプダイアログに記載してくれる？

#445
ヘルプダイアログは時々更新を加えつつ完成させていくが一旦はOK。
次は細かい挙動の修正を。
- caretを動かすキーを押し続けて高速オートリピートが効いているとき、caretが動いている最中でも点滅が続いているように見える。動いている間は点灯したままにできないか？

#446
OK。十分。
- sixで`Esc`に意味のあるあらゆる場面で`Ctrl+[`も`Esc`と同等に扱ってほしい。
  - キーバインドの表記などでも完全に`Esc`と同一扱いでよい。つまり`Ctrl+[`に言及しなくてよい。

#447
`Ctrl+[`対応 →OK。
- NORMALで`V`を押したときcaretが行頭や行末に移動するのは不要だと思うので移動しないようにして。
- `Ctrl+U`がEdgeに渡るとソースウインドウが開くようなので、sixで消費してEdgeに渡らないようにして。

#448
- `Ctrl+U`の消費 →OK。
- `V`でcaretが先頭 or 末尾に飛ぶ →NG。改善されていない。
  - 軽く観察した限りでは、行末までの中央より行頭寄りにcaretがあるときは行末に飛び、行末寄りにあるときは行頭に飛んでいるようだ

#449
OKだが、VISUALで選択中のcaret移動時も#445で対応した「高速オートリピート中は点滅せず点灯のままにする」を適用してほしい。

#450
OK。次は仕様変更。
- 「アクティブタブのみフルパスで表示する」という仕様を廃止する。アクティブでも他タブと同じ表示に。
- アプリのタイトルバーの、現在"six-webview2"と表示されているところに代わりにアクティブタブのフルパスを表示する
- タイトルバーのフルパスには`F1`などの前置は付けない。

#451
OK。続いてタブ周りを。
- タブバーにHTML標準の横スクロールバーが発生することがないようにする。
  - タブが表示しきれない場合は自前の仕組みでスクロールする。
  - タブ群の左右にスクロールボタンを配置する。'◀'、'▶' ※横向き三角でUnicode的にこれより標準のものがあればそちらで。
    - はみ出ていないときでも常に表示する(非活性)。
    - とりあえずキーバインドは無し。click操作のみ。
    - スクロールボタンの色はwindow.THEMEで指定する。
```
  tabScrollBtnEnableBG: "white", // タブスクロールボタン(活性)
  tabScrollBtnEnableText: "orange", // タブスクロールボタン(活性)
  tabScrollBtnDisableBG: "black", // タブスクロールボタン(非活性)
  tabScrollBtnDisableText: "cyan", // タブスクロールボタン(非活性)
```
- はみ出て表示されていないタブに対しても`F1`～`F8`でのダイレクト選択は有効とする
- タブ内の上下marginを0.2remずつ増やして。

#452
- 表示できるかどうかの判定でスクロールボタンの幅の考慮が間違っているように見える。
  - タブが右側にはみ出るかどうかの判定でスクロールボタン幅を考慮していないように見える。(スクロールボタンの下に潜り込んでいる)
  - タブが左側にはみ出るかどうかの判定でスクロールボタン2個分くらい考慮しているように見える。
- タブ高をタブバー高の90%にして下寄せにして。(タブ上下のmargin指定は撤去)
  - タブとスクロールボタンの高さを揃えて。下端も。
- スクロールボタンの幅は1.6remにして。

#453
履歴が消えてしまったが、#345から順に#452まで進んだところ。
スクロールの様子はOK。
- タブ高をタブバー高の90%にして下寄せにして。(タブ上下のmargin指定は撤去)
  - タブとスクロールボタンの高さを揃えて。下端も。
- スクロールボタンの幅は1.6remにして。

#454
タブとスクロールボタンの高さが揃っていないのは添付スクショでも明らか。タブの高さが足りない。スクロールボタンの幅も全角文字より幅が大きいということは2.0remを超えているということでは？

#455
- タブとスクロールボタンの高さ調整 →OK
- タブの色もwindow.THEMEで指定できるように。
```
  tabBg: "#406040",
  tabText: "#e0ffe0",
  activeTabBg: "#709070",
  activeTagText: "white",
```
- スクロールボタンの幅を1.4remにして。

#456
- "activeTagText"→"activeTabText"に修正して。
次はVimの:set listを実装しよう
  - 全バッファ共通、アプリで1つの設定とする。デフォルトでON
  - listcharsは固定。動的に変更不可でいい
  - 「listchars=eol:↲,tab:▸\ ,trail:·」で、eolはLF/CRLF/CRで色を変える。
  - :set list / :set nolist / :set list! / :set list? を実装する
  - 右下のオーバーレイパレットにボタン追加「制御文字表示<br>OFF ON」位置は即時終了ボタンの左側。
  - 文字幅の計算方法などHTA版sixで十分に詰めて完成させたので、できればロジックをそのまま持ってきたい。

#457
- window.THEMEにactiveTabTextが無いときにyellowになっていない。NG
- タブを切り替えた際に制御文字がリドローされずに前タブの制御文字がそのまま残っている
  - `:set list`を実行すれば再描画される
- 編集しても制御文字が追従しない
  - 1文字挿入 or 削除する度に追従すべき (行内)
  - 行単位での挿入 or 削除でも追従すべき (全体、少なくとも表示画面内)
- 制御文字の位置がズレている
  - 空行に対してもオフセットのように右にズレている
    - ちょうどガター幅の分だけ右にズレているように見える
- EOF行の下の余白行にまで改行文字が表示されてしまっている NG

#458
いずれもOK。
- 改行文字以外の制御文字と改行文字unix/dosの色分けをwindow.THEMEの下記で設定したい。
  - macフォーマット(CR)を赤(固定)で描画するのは隠し機能のようなもので、ヘルプなどでもmacフォーマットには一切触れなくていい。
```
  controlCharColor: "#a0ffff",
  controlCharColorLF: "#80ffe0",
  controlCharColorCRLF: "#0020a0",
```

#459
OK。
- INSERTモード中の`Tab`/`Ctrl+I`でTABコードを挿入できるようにする。
  - ヘルプへの記載は不要

#460
- TABコードは挿入されるようになったが、TABコードの後ろに挿入した文字がTAB中央あたりに表示される NG(添付スクショ)
- INSERTモードで`CTRL+H`を`Backspace`として扱ってほしい。

#461
- 全角空白もTABコードのような扱いでcontrolCharColorでIDEOGRAPHIC SPACE(unicode)で描画して。
- TABコード/全角空白は「trailを途切れさせないもの」として扱ってほしい。つまり行末の「	   　  」は丸ごとtrail。

#462
- 全角文字が見えない。IDEOGRAPHIC SPACEはフォントによっては見えないのかな？他によい文字ないかな？EM SPACEとか？
  - あくまでも「描画に使う文字」であって、全角空白は普通に「　」でよい。
- trailの扱いはOK
- TAB幅はwindow.SIX_OPTIONSの'tabstop'に従うようにして。

#463
- 全角文字が見えるようになった。OK
  - 見えるが、幅が細く見える。フォントによると思うが、制御文字表示だけ専用のフォントを使えば改善するかな？何かよいフォントない？
- TAB幅は正しく表示されているが、tabstopの設定がないときのデフォルトは8にして。

#464
- 全角空白および改行文字の描画は．．．
  - 線をもう少し細くしたい
  - 幅をもっと広くしたい(連動して高さも広がる)

#465
- TAB文字以降のcaretや改行文字の位置がズレる。NG
- window.SIX_OPTIONSのtabstopを変更しても反映されない。NG
- .listchar-ideospc { --controlCharWeight: 100;}などとしても変化がないように見える。

#466
- tabstopは反映されるようになった。OK
- TAB文字以降のズレは解消した。OK
- .listchar-ideospc { --controlCharWeight: 100;}などとしても変化がないように見える。

#467
OK、controlCharの調整完了。
- DevToolsで「計算済み」を見てて気づいたが、<textarea id="editor">がwidth:1178px, height:998pxに対してpadding-bottomが260pxもある。
  - 起動時の桁数、行数ってどこかで指定してたっけ？

#468
EOFの余白行が見えない範囲でホイールスクロールしても問題は起きないが、余白行が現れる範囲だと制御文字が1行ズレたり先頭行余白が発生してその分だけ背景とのズレが生じる現象が起きる。
余白10行分をpaddingで確保していることは影響しないのか？heightに吸収するのがシンプルな気がするが。

#469
改行文字が余白10行目まで描画されたり余白まで行番号が付いたりGの飛び先が狂ったりデグレがひどかったのでパッチを破棄した。
ホイールスクロール問題は時間がかかりそうなので後回しにする。
- 次は、右下オーバーレイパレットの下にパレット表示ボタン【🎨<br>^F9】を設ける。
  - '🎨'はモノクロで表示したい。<br>は改行の意。
  - 右下オーバーレイパレットに含まれるものではない。
  - clickまたは`Ctrl+F9`で右下のオーバーレイパレットを全表示・全消去のトグル動作をする。
  - 今オーバーレイパレットを表示中かどうかのフラグは内部だけで持つ。セッション情報には含めず、起動時は必ず表示する。
  - パレット表示ボタンそのものは常に表示する。

#470
`gg`,`G`,`k`とするだけで画面内先頭行前に隙間が空いて文字の挿入位置と表示がズレる。

#471
`gg`,`G`,`k`とするだけで全行が0.5行ほど上にスクロールするが背景グラデーションは追随しないので見た目ズレが生じる。
- ズレた状態で`:`を押すだけでズレが解消するようだ。
- caretと実際の挿入位置がズレる現象は発生しなくなった。
- パレット表示ボタンのclickは機能しているが`Ctrl+F9`では何も起こらない。

#472
`Ctrl+F9`が効かない件は撤回する。
- #468あたりでwindow.SIX_OPTIONSのscrolloffを99999に変更していたが、#471で報告したズレと関連がありそう。scrolloffを3にすると発生しなくなる。
  - scrolloff=99999で運用していきたいので、この設定でもズレは解消したい。

#473
OK, scrolloff=99999でもズレは解消した。
- `:set scrolloff=N`で動的にscrolloffを変更できるようにしたい。
  - 設定した値はセッション情報に含め、再現できるようにする。
    - セッション情報があれば十分なので、window.SIX_OPTIONSのscrolloffは廃止する。
      - セッション情報にscrolloff設定がない場合の初期値は3とする。
- `:set scrolloff?`で現在値を表示したい(トースト)。
- パレット表示ボタンにもホバー時に赤っぽくなる効果を付けたい。

#474
scrolloffまわりの修正 →OK。
次は、外部でファイルが更新(変更)されたのを検知した際の読み込み直し確認を実装したい。
- 起動時やバッファ切替などバッファがアクティブになったときにモーダルダイアログで確認(アクティブバッファのみ確認)
  - バッファがmodifiedではないとき：
    - "このファイルは外部で編集されたようです。読み込みますか？[Y][N]"
  - バッファがmodifiedなとき：
    - "このファイルは外部で編集されたようです。未保存の編集を破棄して読み込みますか？[Y][N]"
  - 一度[N]を選んでいても他バッファに移って戻ってきたときなど確認タイミングが来れば毎回確認する
  - [N]を選んだことは憶えておいて、保存するときにモーダルダイアログを出す。
    - "このファイルは外部で編集されたようです。無視して保存しますか？[Y][N][Cancel]"

#475
- 特定のファイルを最後に保存したのがsixなのに必ず外部変更検出ダイアログが出る
  - ハッシュで比較するのは廃止。ファイルのタイムスタンプ(最終更新日時)とファイルサイズ(バイト数)だけで判断しよう。
  - 最終更新日時とバイト数はmodified=false時点の値をセッション情報として保存しておく
- "[Y][N]"は変更しよう→"[読み込み直す][無視]", "[強制保存][編集内容を破棄][キャンセル]"

#476
外部で編集しても検出ダイアログが出なくなったが、途中だから、なら了解。
- 確認タイミングとして、アプリが別ウインドウに隠れて再び前面に来たときなども追加して仕上げて。

#477
- 外部で編集しても外部編集検出ダイアログが出ない。NG
  - タブ切替タイミング、アプリ前面復帰時、即時終了からの起動時、いずれも同じ
  －`:e!`で読み込み直すと外部での編集内容も反映される。

#478
- 外部編集が一切検出されない。wsl/Cドライブどちらも。six側で初めて開くファイルで何も編集しないままでも。NG
  - 'baseline'とは何を指しているのか？
  - 「mtime/size のどちらか片方しか取得できない場合」→通常は両方取得できるはずだが、そもそも「mtime/sizeの両方に差分がある場合」ではない。「mtime/sizeのいずれかだけでも差分がある場合」で判断しなければ意味がない。

#479
何も挙動が変わっていない。`baseline`って何？文脈によって意味が全然変わる汎用な語を説明なく使うのはやめて。
- 外部編集が一切検出されない。wsl/Cドライブどちらも。six側で初めて開くファイルで何も編集しないままでも。NG
- 「mtime/size のどちらか片方しか取得できない場合」という特殊なケースではない気がする。
  - 取得したmtimeをfnameと一緒にコンソールに出力するようにして。(DevToolsのコンソールで確認できるように)
  - 取得したsizeをfnameと一緒にコンソールに出力するようにして。(DevToolsのコンソールで確認できるように)

#480
- six起動時はカレントバッファではない「前回開いていたバッファ」をカレントにしたがコンソールに何も出ない。(実際には外部で編集している)
  - その状態で`:e!`とすると外部で編集された内容で読み込み直される。
- タブ切替、six起動(セッション再現でタブ11個)、最小化→復帰など、何をしてもコンソールに何も出ない。

#481
- six起動直後のコンソール：
```
_six.js:1589 [switch] begin 10 80x40.txt file:///C:/Users/ymaru/HTA/six/80x40.txt
_six.js:1741 [switch] external-check trigger 80x40.txt
_six.js:1759 [ext-check] invoke 80x40.txt
_six.js:1763 [ext-check] start 80x40.txt
_six.js:1767 [ext-check] miss meta; cache-bust and retry 80x40.txt
```
- 現在バッファにないファイルを読み込んでみてテスト：
  - コンソールクリア
```
_six.js:1759 [ext-check] invoke prompt.md
_six.js:1763 [ext-check] start prompt.md
_six.js:1767 [ext-check] miss meta; cache-bust and retry prompt.md
```
  - `:e /Users/ymaru/HTA/six/110x40.txt`
```
_six.js:1589 [switch] begin 11 110x40.txt file:///C:/Users/ymaru/HTA/six/110x40.txt
_six.js:1741 [switch] external-check trigger 110x40.txt
_six.js:1759 [ext-check] invoke 110x40.txt
_six.js:1763 [ext-check] start 110x40.txt
_six.js:1767 [ext-check] miss meta; cache-bust and retry 110x40.txt
```
  - 別バッファに切替→外部で11x40.txtを編集&保存→sixに戻って110x40.txtのバッファに移動
```
_six.js:1759 [ext-check] invoke prompt.md
_six.js:1763 [ext-check] start prompt.md
_six.js:1767 [ext-check] miss meta; cache-bust and retry prompt.md
```

#482
起動しなくなった。画面最下部に下記メッセージ。
"six: Javascriptの初期化に失敗しました。F12でコンソールを開き、Ctrl+F5で再読込してください。"
- コンソールの内容：
```
_six.js:9625  Uncaught SyntaxError: Unexpected token ')'
```

#483
起動しない。画面最下部に下記メッセージ。
"six: Javascriptの初期化に失敗しました。F12でコンソールを開き、Ctrl+F5で再読込してください。"
- コンソールの内容：
```
_six.js:9636  Uncaught ReferenceError: runCommand is not defined
    at _six.js:9636:5
    at _six.js:9640:3
```

#484
起動しない。画面最下部に下記メッセージ。
"six: Javascriptの初期化に失敗しました。F12でコンソールを開き、Ctrl+F5で再読込してください。"
だがコンソールには何も出ていない。Ctrl+F5で再読込しても変わらず。

#485
正常に起動しない。
画面最下部にメッセージは出なくなりコンソールにも何も出ていないが、起動が完了していない状態(添付スクショ)。
- コマンド入力欄をクリックすれば文字入力できるがEnterでも何も起こらない。
- textaeaはクリックしても文字を入力できない。
- パレット表示ボタンをクリックしたり`Ctrl+F9`を入力しても何も起きない。

#486
変化なし。`Ctrl+F5`でもコンソールに何も出力されない。
- _six.js:l.2138にbreakpointを置いて`Ctrl+F5`
- step実行していくとl.2141("let _apiFailCount = 0;")から次に進めようとしたところでl.9666に飛んだ
- さらにstepを進めると"VM393 _six.html"というソースが開き、進めると_six.html:l.82に飛んだ
  - l.82の"if (!window.six){"ブロックには入らずl.94まで進んだところでstep実行が終了した(イベントループに戻った？)

#487
変化なし。`Ctrl+F5`でもコンソールに何も出力されない。
- _six.js:l.2154にbreakpointを置いて`Ctrl+F5`
- step実行していくとl.2158("let _apiFailCount = 0;")から次に進めようとしたところでl.9678に飛んだ
- さらにstepを進めると"VM79 _six.html"というソースが開き、進めると_six.html:l.82に飛んだ
  - l.82の"if (!window.six){"ブロックには入らずl.94まで進んだところでstep実行が終了した(イベントループに戻った？)
- コンソールには一切出力なし
- パレット表示ボタンをクリックしたらコンソールに表示された(画面最下行にもオーバーレイで表示された)。
```
_six.js:14 [six][diag] early-palette-toggle
```

#488
- 外部変更検出の実装は後回しにする。
- ソースは#473の直後まで戻した。scrolloffまわりの修正が完了したところ。
次は:e popupのデグレに気づいたので修正したい。
- Cドライブ下のパスでpopupから"../"をclickするとwslパスのように扱って一覧が取れなくなる
  - 「C:\Users\ymaru\HTA\six\80x80.txt」がカレントバッファのとき：
    - `:e `でpopupを開き、
    - "../"をclick
    - コマンド入力欄は":e //C:/Users/ymaru/HTA/six"になりpopupは"(loading...)"になっている (NG)

#489
- #488で報告した現象は修正された。OK
- ":e C:/foo/bar"などとなっているときに`Esc`を押すとpopupが閉じるが、続けて`Tab`を押すと"C:/foo/bar/"の一覧がpopupに表示される。
  - コマンド入力欄は":e C:/foo/bar"のままであり、popupは"C:/foo/"の一覧が開いて(マッチすれば)"bar"にカーソルが乗るのが正解と思われる。

#490
変化ないようだ。例1でも例2でも同様。入力欄の末尾に'/'が付いているかのような挙動になっている。(NG)

#491
OK。
次は、NORMALモードおよびVISUALモードでtextareaにフォーカスがあるときは全角文字の挿入は全て無視してほしい。
  - うっかりIMEがonのまま`：`と入力しても`:`として扱う、みたいなフォールバックは一切不要。弾いてくれた方がいい。

#492
変わらない。なお「全角入力」と言っているのは「IMEでの未確定文字列を確定させる」操作のつもり。
- IMEからの確定文字列がNORMALモードでも挿入されるが、undoバッファには入らずmodifiedフラグも立たない。
  - ひとつ前のundoバッファに追加されるようで、undo/redoで直前操作と一緒に消えたり現れたりする。
- IMEの未確定文字列は必ずしもcaret位置に挿入されるわけではなく別の場所になることがある。
  - おそらく「最後にINSERTモードだったときのcaret位置」に挿入される、と推察
  - 確定文字列もそのまま未確定文字列の先頭位置に挿入される
- VISUALモードで未選択のときはNORMALモード時と同等だが、常にcaret位置に挿入されている。
- VISUALモードで選択中にIMEで未確定文字列の入力を始めたときに選択はリセットされる。

#493
いいね！以前どうしても実現できず諦めた「INSERT以外ではIMEを強制offに」の実現に近い。OK。
- six上はEOFの空行が1つ余分に存在する扱いになってしまっている。NG
  - 保存したファイルを他アプリで見ると空行が1つ少ない。
  - おそらく、EOF行で最初から入っている改行文字が「本来は存在しない、便宜上表示だけしている改行」なのではないか？
    - もしそうなら、保存するときは実体化して本物の改行コードとして書き出すべきか？

#494
変化なし。
six.ps1の末尾はsixで見ると行355が改行だけから成る空行で終わるが、編集&保存した上でubuntu上で確認すると、
- wc
- `tail -c 4 six.ps1 | xxd`
```
00000000: 6c20 7d0a                                l }.
```
- `wc --lines six.ps1`
```
354 six.ps1
```

#495
NG。空行を1つ加えて保存するのではなく「末尾に改行のない行」が発生しないようにすればいいのではないか？
  - 同時に「存在しないのに便宜上改行を表示」するのをやめる。これならsixの処理もシンプルでかつ他と整合性が取れるのでは？

#496
改行の扱いはOK。
関連して、EOF付近での`o`,`O`の挙動が期待動作ではなくなった。例えば末尾の行100(改行文字1つだけから成る)で`o`でも`O`でも改行がひとつ挿入されるのが期待動作(`o`なら行101にcaretが移動し、`O`なら行100にcaretが移動する)。

#497
- 空行でない末尾行からの`o`,`O`はOK
- 改行1つだけから成るEOF行で`o`,`O`を入力しても改行文字が挿入されない NG
  - 見た目に改行が増えないのにmodified扱いにはなる
  - LFでもCRLFでも同じ。
