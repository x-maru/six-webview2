# NORMAL

## 移動(モーション)
- `h`,`←`左へ1文字
- `j`,`↓`下へ1行　※markdownモードでは`gj`の動作
- `k`,`↑`上へ1行　※markdownモードでは`gk`の動作
- `gj`表示折り返し有効時: 見た目の1行下へ（visual line）
- `gk`表示折り返し有効時: 見た目の1行上へ（visual line）
- `g↓`(`gj`),`g↑`(`gk`) (カウント対応。例:`3gj`)
- `Alt+j`,`Alt+k`スムーズスクロール（上/下）
- `l`,`→`右へ1文字
- `gg`先頭へ
- `G`末尾へ
- `0`行頭へ
- `Home`行頭へ（`0` と同等）
- `^`空白文字に続く行頭へ
- `$`行末へ
- `End`行末へ（`$` と同等）
- `w`,`b`単語の先頭へ進む/戻る
- `W`,`B`WORD（空白区切りの大きな語）単位で進む/戻る
- `Nw`,`Nb`,`NW`,`NB`：N回分まとめて移動（例: `3w`）
- `{`段落/空行区切りの前へ
- `}`段落/空行区切りの次へ

## オペレータ
- `x`caret直下の1文字削除（yank バッファは更新しない。2以上の前置数字時のみ更新）
- `dd`行削除
- `d モーション`削除（範囲はモーションによる）
- `Nd モーション`カウント付き（例: `2dw`）
- `yy`行ヤンク(行コピー)（空ならyank バッファを更新しない）
- `y モーション`ヤンク(コピー)（範囲はモーションによる）
- `Y`Windowsクリップボードへコピー（y のモーション/カウントと同等、unnamed レジスタは変えない。空の場合はWindowsクリップボードを更新しない。例: `YY`,`3Yw`）
- `p`caret行の下に行ペースト
- `P`caret行の上に行ペースト
- `s`1文字変更（`cl`と同等。前置カウントで複数文字）。1文字のみではundoバッファに格納しない。改行も1文字として扱う
- `cl`1文字変更（`s`と同等。前置カウントで複数文字）。1文字のみではundoバッファに格納しない。改行も1文字として扱う
- `>>`インデントを `shiftwidth` 分増やす（現在行から）。前置カウント `N` で `N` 行を対象（例: `3>>`）
- `<<`インデントを `shiftwidth` 分減らす（現在行から）。前置カウント `N` で `N` 行を対象
- `TAB` `>>` と同等（前置カウント対応）
- `Shift+TAB` `<<` と同等（前置カウント対応）
- ※ 空行では無視される。行頭の連続 `TAB` は保持し、その直後に空白を挿入/削除します

## 検索
- `/`EOF方向にインクリメンタル検索（確定で最後の検索状態を更新）
- `?`ファイル先頭方向にインクリメンタル検索（確定で最後の検索状態を更新）
- `n`最後の検索語を検索方向に沿って検索（`/`による検索ならEOF方向、`?`による検索ならファイル先頭方向）
- `N`最後の検索語を検索方向の逆方向に検索（`/`による検索ならファイル先頭方向、`?`による検索ならEOF方向）
- 正規表現 `^`,`$` は各行の先頭/末尾にマッチ（内部的にmultiline）。`/i`で大文字小文字を無視
- 例）TABを検索するには`/\t`(`/`でも`?`でも可)。行頭の連続TABは`/^\t+`など

## モード切替
- `v`VISUAL モードへ（文字単位選択）
- `V`VISUAL モードへ（行単位選択）
- `i`INSERT モードへ（caret位置そのまま）
- `I`行頭へ移動 & INSERT モードへ
- `a`INSERT モードへ（caretを右に1文字移動してから）
- `A`行末へ移動 & INSERT モードへ
- `o`caret行の下に空行作成 & INSERTモード
- `O`caret行の上に空行作成 & INSERTモード
- `cc`行削除 & INSERTモード
- `c モーション`削除 & INSERTモード（削除範囲はモーションによる）
- `Nc モーション`カウント付き（例:`2cw`）
