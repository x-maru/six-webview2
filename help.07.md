# Markdown
GFM(GitHub Flavored Markdown)にほぼ準拠（あと未実装で実装予定なのはテーブル、Task List）
ただし基本的にHTMLではなく行単位で管理するテキストエディタなので、markdown記法ではない生のテキスト部分に関しては改行や空白文字が1つにまとめられたりすることはない。

## 見出し
ATXとsetextの2種類ある

- ATX
  - `# `, `## `, `### `, `#### `, `##### `, `###### `のいずれかで始まる1行。順に見出し1～6で、Sixでは見出し3が本文文字サイズ。
- setext
  - `:set setext`とした場合に有効になる記法。デフォルトはoff
  - `==`または`--`の直前の段落(複数行)を見出しとして下線付きで大きく表示する。ATX見出し1、見出し2の下線付きバージョン。
    (ただしATXではひとつで複数行に適用することはできない)
  - `=`または`-`を2文字以上何文字でも続けてもよい。
  - `==`,`--`の前は3文字以下の空白文字のみ挿入可(無視する)。※4つになるとコードブロックになる

---
## 水平線
HTMLで言うところの`<hr>`
- `-`または`*`または`_`を0個以上の空白文字(半角空白,TAB)を挟んで3つ以上並べる。
- 行頭には4個未満の半角空白が入ってもいい(無視する)。
- setextが有効な場合、水平線の前に改行を入れないと優先的にsetextと判定されてしまうので注意。

---
## list
階層構造をインデントで表現する列挙型
- 階層1
  - 階層2
    - こういう列挙型(unordered)と、
1. 階層1
   1. 階層2
      1. 階層3の1
      1. こういう数字付き列挙型(ordered)の2種類ある。

- unordered list (列挙)
  - `- `(第1階層)の左の0個～4個未満の空白は無視する
  - 第2階層以降は「親階層のインデント」+「半角2文字分」
  - `-`(マーカー)は`+`でも`*`でもよい。違いはない。
- ordered list (数字付き列挙)
  - `1. `のように数字の後に半角ピリオドと半角空白を続ける。
  - six拡張として、同階層で続く項は先頭項からインクリメントしてレンダリングする。
    - なので`3. `, `2. `, `1. `と並んでいても`3. `, `4. `, `5. `となる。
    - 自動インクリメントのルールを適用した場合の最大桁数に合わせ、階層内の数字が右寄せになるように半角空白で埋める。
      - `8. `, `1. `, `1. `なら` 8. `, ` 9. `, `10. `となる。
- loose list
  - orderedでもunorderedでも、同階層内に空行が来て、かつそのlistが終わらないとき、空行のある階層の行はすべて行間隔が広くなる。

---
## インライン装飾
  - Italic　`*text*`, `_text_`
    - *text*, _text_
  - Bold　`**text**`, `__text__`
    - **text**, __text__
  - Bold Italic　`***text***`, `___text___`
    - ***text***, ___text___
  - Strike　`~~text~~`
    - ~~text~~

※`_`,`__`,`___`は単語の一部になる場合は装飾扱いにならないなど細かいルールがあるので`*`を使う方が無難

---
## インラインコード
キーボード入力などを表すことを想定した装飾
- 1個以上の`` ` ``(backquote。backtickとも言う)で挟む。改行を含めることは出来ない。

---
## コードブロック
レンダリング対象から除外する行。マウスをホバーすると`yank` `clip`というボタンが現れ、それぞれブロック内の全文をyankバッファまたはWindowsクリップボードにコピーできる
- fenceコードブロック
  - 独立したfence行で挟まれた部分。fenceは連続する3文字以上のbackquote。
```
こんな感じ。ここは`レンダリング`されない。https://www.google.comなどもクリッカブルにならない。
```
- indentコードブロック
  - fenceなしで、行頭に空白文字が4つあればその行はコードブロックになる
  - 連続するindentコードブロック行は連結してひとつのコードブロックとしてレンダリングされる

---
## インラインリンク
クリッカブルなURL文字列を別文字列で表示できる
    [表示文字列](http://google.com/sample.html "ツールチップ")
- CommonMark/GFMではidに連続しない改行文字を含めてもよいことになっているが、Sixでは改行は含まないこととする
- 通常はブラウザで開くがローカルのファイルパスだった場合はバッファとして開く
- 表示文字列部分にcaretを持って行くと`Ctrl+E`というバッジが表示され、クリックやキー入力でURLバーが開く
	
- 参照リンク
  URL部分(参照定義)を別の場所に記述す形式。参照定義はバッファ内のどこにあってもよい。
  参照定義そのものはインラインではないので1行に独立して書く。
  ※本来は参照定義行は表示しないが、Sixでは生テキストとして表示している(いずれfenceを実装して畳んで隠すようにする)。
  .
  - 参照定義)
    '[id]: http://google.com/sample.html "ツールチップ"`
  - 明示的参照）
    `[表示文字列][id]`
  - 折り畳み参照）
    `[id][]　※idが表示文字列を兼ねる`
  - ショートカット参照）
    `[id]　※idが表示文字列を兼ねる`

---
## インライン画像リンク
画像専用のインラインリンク
    ![alt文字列:表示行高][id](http://google.com/sample.png "ツールチップ")
- 画像リンクは常にブラウザで開かれる
- 「alt文字列」がインラインリンクの「表示文字列」に相当し、`Ctrl+E`バッジでURLバーが開く
  - 画像リンクのURLバーではURL部分だけでなく全体が編集対象になる
- `"ツールチップ"`は省略可能
- `:表示行高`は省略可能。数字が入り、その行高になるように拡大・縮小して表示される(six拡張)

- 参照リンク
  - 参照定義)
    `[id]: http://google.com/sample.png "ツールチップ"`
  - 明示的参照)
    `![alt文字列:表示行高][id]`
  - 折り畳み参照)
    `![id:表示行高][]　※idがalt文字列を兼ねる` 
  - ショートカット参照)
    `![id:表示行高]　※idがalt文字列を兼ねる` 

---
## 引用ブロック
行頭に`>`を付けた行の装飾
- `>`の直後の半角空白1つは引用記法の一部として巻き取る。半角空白0個でもよい。
- 巻き取ったあとの`>`または`> `より右の部分に対して改めてmarkdown解析が走ってレンダリングされる。
  - 引用ブロック内の例外として半角空白3文字でインデントコードブロックになる(通常は4文字)。
    - ただしインデントコードブロック判定は優先度が一番低いので、"   - "はリストになる。"   abc"ならコードブロック。
