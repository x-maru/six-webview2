### #1945
OK。次はGFMの実装に戻って、参照リンクを実装したい。
1. `[表示ラベル][id]`と対になる参照定義`①[id]: URL "ツールチップ"②␊`から成る
  - ①は「0個以上4個未満の'半角空白またはTAB'」
  - ②は「0個以上の半角空白またはTAB」
  - idは参照側・定義側共に「前後の空白文字を削除」「連続する空白文字(半角空白、TAB)を1つの半角空白に置き換える」という処理をしたうえで照合する
  - 参照定義はバッファ内のどこにあってもよい。
    - 毎回全文検索することは避けて高速化したい。内部にテーブルを持つべきか。
      - `]:`が入力されたときにその行へのリンクを内部に持って、内部テーブルを更新する方式がいい？ ベストな実装方法を提案して。
        - 内部テーブルをセッション情報に含めずバッファを開くときに一度全文検索で内部テーブルを生成する方式が現実的？
          - タブ切替は重くしたくない。オンメモリではバッファ毎に維持すべきか。
  - CommonMarkやGFMではid内に「連続しない改行」を含めることを認めているが、sixでは改行文字は除外する。
  - ツールチップに関しては通常のインラインリンクと同じ(省略可能、囲う文字は`''`または`""`または`()`)
  - 参照リンクの参照側が仕様どおりに記述されていても対になる参照定義が見つからない場合は成立しないので生テキスト扱い。
  - 清書でもdraftでも参照定義行はレンダリングしない(行ごと表示しない)。インラインリンクのURL部分と同じ扱いでURLバー内で表示・編集する。
2. `[表示ラベル兼id][]`と対になる参照定義`①[表示ラベル兼id]: URL "ツールチップ"②␊`から成る
  - `[]`は省略可能 (CommonMarkで「ショートカット参照」と呼ばれている形)
  - 他はフルの参照リンクと同じ
- この仕様で矛盾や不足があったら指摘して。

### #1946
- idの大文字小文字はCommonMark/GFMに従って「区別しない」にしよう。
- 同じidが複数定義されていた場合は「最初に見つかったもの」、つまり現状のままでいい。
- 対になる参照が1つもない参照定義行について：
  - 対になる参照があるかどうかに関わらず「参照定義の行を表示しない」という仕様は撤回する。「常に生テキストで描画」を仕様とする。
    markdown実装の次にでも実装しようと思っていた「段落を隠したり見せたりできるfolding機能」を参照定義行にも対応させることで隠す予定。
  - 参照定義の行を直接編集することも可能だが、参照側で`Ctrl+E`からURLバーを表示して編集する仕組みも残す。
    - URLバーでの編集は確定時に参照定義行にも反映したい。

### #1947
OK。次はコードブロック内での挙動修正を。
- コードブロック内ではインラインリンクは生テキストとして表示しリンクは機能しないようになっているが、caretの移動が生テキストのときの動きになっていない。
  - `h`,`l`で(URL ...)の中に入らずスキップしてしまう

### #1948
- OK。次は画像リンクを実装したい。下記の形式がGFMの仕様のはず。
```
![代替文字列](https://yahoo.co.jp/foo/bar/sample.png "ツールチップ")

![代替文字列][id]
[id]: https://yahoo.co.jp/foo/bar/sample.png "ツールチップ"

![代替文字列兼id][]
[代替文字列兼id]: https://yahoo.co.jp/foo/bar/sample.png "ツールチップ"

![代替文字列兼id]
[代替文字列兼id]: https://yahoo.co.jp/foo/bar/sample.png "ツールチップ"
```
- `!`がない場合のインラインリンクと同様にURLバーに対応させる。
- six独自拡張として、ウインドウサイズに収まらない大きさの場合には縦横比を維持したまま収まるサイズに縮小する。
  - 下にスクロールアウトして一部が見えないのは問題ない。表示範囲の先頭行から表示すれば全体が見えるサイズにする。横も横スクロール無しで表示できるようにする。
  - 大きさに関わらず、[代替文字列]を含む行の行高を「画像がぎりぎり収まる高さ」にする。
- six独自拡張として、代替文字列の末尾が':数字'だった場合に、それ以前を代替文字列(alt)として数字部分は「表示行数指定」と解釈する。
  - 画像の縦横比を維持したまま、縦サイズが指定行数分になるよう拡大または縮小して表示する。
    - 「画面内に収まるように拡大縮小」より「表示行数指定」を優先する。画面からはみ出る大きさになっても優先。
    - 行数はあくまでも大きさの指定用で、拡大縮小後の画像がぎりぎり収まる高さを行高にするsix仕様は変わらず。

### #1949
- draftモードでも清書時と同様に画像を表示し、URLバー内に画像リンクのフルテキストを入れて全体を編集できるようにしたい。清書モードでも。
- インラインなので、`abc![...](...)xyz`といった記述でも`abc★xyz`が行内に表示されるようにレンダリングしてほしい(★が画像)。
  - 文字は行番号と同じように行内で縦方向にセンタリングした位置に描画する。
- 「表示行数指定」に従って行高を決定するが、画像の上下それぞれに0.125chの隙間ができるよう少し小さく表示したい。

### #1950
- 「表示行数指定」の行内に画像が収まっていない(添付スクショ)。
- 仕様追加だが、「表示行数指定」がない場合はウインドウ内に収まる大きさに調整したあと上下に0.125chの隙間ができる行高にして縦方向にセンタリングしてほしい。
- バッファからの相対パスでローカルにあるpngを指定しても表示されるが、リンクとしてクリックすると無理やりテキストとしてオープンしてしまう。
  - `!`で始まる画像リンクなのだから、ローカルパスでも常にブラウザでオープンするように出来ないか？
- 画像を1文字として幅を持たせてほしい(改行表示の位置に反映してほしい)。

### #1951
- 試したリンク：`abc![ロゴ:3](256x256.png 'ちっぷ')xyz`
  - 画像の最終的な占有幅(caretが通過する距離)が表示幅よりずっと小さい。
    - レンダリングとしてはちゃんと"abc★xyz"と並んで表示されるが、★(画像)の文字幅が2chほどしかなく"xyz"のcaret位置も★の中になってしまっている
  - 相対パスの画像がブラウザで開かれずsixで無理やり開いてしまう。NG
- 画像が「上下に0.125chずつ隙間が出来るように」縮小されていない。NG

### #1952
- 試したリンク：`abc![ロゴ:3](256x256.png 'ちっぷ')xyz`
  - 画像の最終的な占有幅(caretが通過する距離)は正しくなった。OK
  - 相対パスの画像がブラウザで開かれずsixで無理やり開いてしまう。NG
    - `Ctrl+E`が出ている状態での`Enter`の場合。クリックは無反応。
- 画像が「上下に0.125chずつ隙間が出来るように」縮小されていない。NG
- listcharsの表示位置が誤り。
  - 画像幅を考慮できていない
  - 行高に関わらず上寄せで描画されてしまっている

### #1953
- 試したリンク：`abc![ロゴ:3](256x256.png 'ちっぷ')xyz`
  - 相対・絶対パスのローカル画像が`Enter`でもクリックでもブラウザで開くようになった。OK
  - listcharsの描画はX位置もYセンタリングもOK
- 画像が「上下に0.125chずつ隙間が出来るように」縮小されていない。NG
  - ":3"の場合、画像の高さを(3-0.125x2)=2.75chに拡大または縮小して表示してという指示だが、高さ3.0chになっているようだ。
- URLバーを開いたときは必ずバー内caretが行末になっているが、行頭にある方がいい。インラインリンクのときも。

### #1954
- URLバーを開いたときは必ずバー内caretが行頭に現れるようになった。OK
- 画像の右側の文字や改行あたりをクリックしても反応しない。caret位置の指定になるのが期待動作。
- 画像の上下マージンは付与されていない(行高ちょうどの高さで縦に隙間がない)。
  - 添付スクショは104行目が「画像リンクの後ろに文字無し」→3行分の高さに見える、105行目が「画像リンクの後ろに文字あり」→2行分の高さに見える。何故？
  - 104行目`![やふー:2](https://s.yimg.jp/c/logo/f/2.0/transit_r_34_2x.png)`、105行目`![やふー:2](https://s.yimg.jp/c/logo/f/2.0/transit_r_34_2x.png)x`

### #1955
- 画像リンクより下の行でクリック位置より数行下の行にcaretが飛ぶ。画像リンクの数に比例して離れていく。NG
  - なので画像の右側クリックが機能しているかどうかよくわからない。
- 画像リンクの後ろに文字があってもなくても表示行数指定が効くようになったが、行高いっぱいに画像が表示されている。元画像からは縮小されているが行内の上下にマージンがない。NG

### #1956
- 画面内の表示範囲のどの行をクリックしても一切caretが飛ばなくなった。NG
- 画像の上下マージン0.125chはOK。

### #1957
- 画像リンク周辺のクリック問題は解消した。OK
- 画像リンクの後ろに文字がある場合とない場合とで画像の横方向の大きさ(縮尺)が変化するのは何故？(添付スクショ)

### #1958
- 後続文字の有無で画像の縮尺が変化しないようになった。OK
- 画像リンクを含むバッファで`G`とするとcaretが画面外になるケースがある。
  - 画像リンクの数に比例してEOF下の余白が減っていく。スクロールバーも早く下端に到達してしまう。

### #1959
- EOF下の余白が減る現象は解消した。OK
- `![やふー:2](https://s.yimg.jp/c/logo/f/2.0/transit_r_34_2x.png)x␊`という行では`0Vl`で画像がハイライトされるが、`![やふー:2](https://s.yimg.jp/c/logo/f/2.0/transit_r_34_2x.png)␊`では`0Vl`で画像ハイライトが見えない。同じようにハイライトしてほしい。

### #1960
- 画像リンクのv選択ハイライトが期待動作になった。OK
- 画像の参照リンクで表示行数指定(`:2`など)の部分を除いてマッチングしてほしい。
  - 例えば`[abc]: URL`という参照定義があれば`![abc:1]`でも`![abc:10]`でもマッチング成立するように。

### #1961
- 画像の参照リンクはOK
- 次はヘルプダイアログ内のmarkdownの修正を。
  - (un)ordered listの階層が正しく描画されていない(すべて第1階層になっている)。
  - 水平線(`---`など)が描画されていない
  - 2文字以上のbackquoteで挟んだインラインコードが正しく描画されていない(`` a ``など)

### #1962
- OK。次はmarkdownのインラインの装飾系を実装したい。
  - Italic
    - *text*, _text_
  - Bold
    - **text**, __text__
  - Bold Italic
    - ***text***, ___text___
  - Strike
    - ~~text~~

### #1963
- Boldのweightをもうちょっと強くして。
- ヘルプ内だけでなく本文側にも実装を。

### #1964
- 「__foo__ みたいなケースの境界ルール（underscore の誤爆/非誤爆）をもう少し厳密にしたい」←どういうケース？
  - 'foo_word_bar'はItalicにしない、'foo__word__bar'もBoldにしない、だっけ。それは実装しよう。他にある？

### #1965
'_'のレアケースは出くわしたときに改めて考えよう。一旦OK。
次はmarkdownの「引用」を実装しよう。
- 行頭に0個～3個の空白に続いて'>'が来たら引用行。(行頭の空白は無視)
- '>'の後ろの空白は任意
- 引用行の次の行は'>'がなくても空行を挟んでいなければ引用の継続扱い(段落が継続)
- 引用内ではインラインの装飾やfenceコードブロックも有効
- Sixの編集機能として、INSERTモードで引用行で`Enter`を入力すると行頭に前行と同レベルの'> >'などを挿入したい。

### #1966
- Sixはエディタなのでmarkdownのレンダリングでも改行を含む空白文字を1つの空白に纏めたりしない仕様なので、引用の仕様とも少し相性が悪かったので、仕様を変更する。
  - 引用行に続く'>'が行頭にない行を継続扱いするのを廃止
  - INSERTモードで引用行での`Enter`の自動'>'挿入の機能を廃止
  - 新たに、引用行の中で「改行を含む文字列」をペーストしたときは引用行にする
    - ペースト文字列中の'␊'をすべて'␊>'に置換する
- 引用内でインラインコード、コードブロックは効いているがItalic/Bold/Bold Italic/Strikeが効いていない

### #1967
- 引用内のItalic, Bold, Bold Italic, Strikeが正常にレンダリングされるようになった。OK
- 「改行を含む文字列」をINSERTモードで引用行にペーストしたときは期待動作だがNORMALモードで`p`でペーストしたときは'>'が自動挿入されない。NG
- 引用とは関係なさそうだが、draftモード時だけItalic/Bold/Bold Italic/Strike部分のcaret移動がおかしい(清書時の文字列長に沿った動きになっている)
- 引用の縦棒を1chの幅で描画して。線幅は今のままでいい。横方向にセンタリングした位置で。
  - 「'>'の後ろの空白は任意」としていたが、0または1個に改める。'>'の後ろの空白1つは巻き取る。つまり'>'も'> 'も同じものとしてレンダリングする。
    - '>'直後の空白1つを巻き取った残りに対して再度Markdown構文解析を適用する。ただし例外的に引用行の中ではインデントコードブロックは空白3つで成立する(CommonMark仕様)。

### #1968
- 「改行を含む文字列」をNORMALモードで`p`/`P`でペーストしたとき'>'が自動挿入されるようになった。OK
- draftモード時のItalic/Bold/Bold Italic/Strike部分のcaret移動が正しい動きになった。OK
- 「引用の縦棒が1chの幅を持つ」というのは清書時に半角1文字分の文字幅を確保するという意味だった。現状はテキストが縦棒と重なって描画されている。
- CommonMarkの仕様ではインデントコードブロックはパース優先順位がリスト項目より下なので、"> "の後ろが"   - "だった場合はunordered listになるのが正しい。(今はインデントコードブロックになってしまっている)
- 縦棒の線色はwindow.EX1_THEME → window.THEMEの順でinlineCodeBGを使うようにして。

### #1969
- 引用の縦棒が1ch幅を持つようになったが、引用内のコードブロック左のマージンに反映されていない。NG
- 引用内の"   - "がインデントコードブロックになることはなくなった。OK
- 引用内の(un)ordered listが清書モードでもレンダリングされていない。NG
- 縦棒の線色はOK

### #1970
- 引用内の(un)ordered listが清書モードでレンダリングされるようになった。OK
- 清書モードでコードブロック左のマージンが引用の幅1chを反映している。OK
  - draftモードは反映しなくても間違いではないが、清書と同じように左マージンに反映してほしい。
  - 引用内のコードブロックで左マージンが引用の幅1chを反映しているときlistcharsがズレてしまう(反映していない)。
- (un)ordered listが左マージンで引用を反映していない。NG
- 引用がネスト(">>"など)していても縦棒1本しか描画されない。NG
  - 1レベルごとに1ch幅で縦棒を。
- 「引用内コードブロックでも > 自体をcleanで隠す（通常行と同じ挙動）」←必須

### #1971
- (un)ordered listが左マージンで引用を反映していない。NG
- draftでもコードブロックの左マージンに引用を反映するとした仕様は撤回する。戻してくれる？
- 引用内コードブロック内のlistcharsのズレは解消した。OK
- 引用のネストの描画は期待動作になった。OK
- 清書モードでは引用内コードブロックで'>'が描画されなくなった。OK

### #1972
- (un)ordered listが左マージンで引用を反映するようになった。OK
- コードブロックの左マージンで引用文を含めるのは清書モードだけになった。OK
- 次はレンダリングの仕様変更だが、(un)ordered listの階層1の左に1ch分のマージンを設けたい。引用の内外に関わらず。
  - 1ch分のマージンもインデントに含めれば、階層2以降のインデントは今までと同じルール(親階層のインデント+マーカー分+ 1ch(空白))になるはず

### #1973
- 階層1の1chマージン追加はいい感じだが、階層2以降の解釈が異なっていたようだ。
  - 「親階層のインデント+マーカー分+1ch空白」がインデントになるというルールはそのままだが、「親階層のインデントを1ch分増やした」という解釈だった。なので階層2以降も結果的にインデントが1ch増えてほしい。
- IMEで未確定文字列が発生している間、階層1の左に追加した1ch分のマージンが消えてしまう。
  - IME入力を確定またはIME OFFにするとマージンが復活する。

### #1974
- IME入力を確定またはIME OFFにするとマージンが復活する件、unordered listでは解消したが引用ブロック内のordered listではまだ発生している。清書でもdraftでも同じ。
`>1. abc␊`

### #1975
- #1974は確認ミスだったかもしれない。ordered listでもunordered listでも引用ブロック内の「折り返しが発生していない短い行」では(他行で)IME未確定文字列が発生すると行頭の1chマージンが消える。両listとも折り返しが発生すると現象が起きなくなる。
- なお「清書でもdraftでも同じ」と書いたが、draftモードでcaretが乗っている行内でIME未確定文字列が発生してもその行では現象は起きず、caretのない他行で発生する。

### #1976
- 引用内のlist冒頭のマージンが他行のIME未確定文字列によって消える問題は解消した。OK
- インデントコードブロックに続く空行がすべてインデントコードブロックの継続行のようになってしまう。NG
  - 添付スクショで"abc"の行頭には空白が4つある。そのあと空行が6行続いてその次は別のfenceコードブロックになっている。

### #1977
OK。次は不具合修正を。
- `` `1.` ``は正しくレンダリングされるが` 1. `だと数字の前の半角空白が詰められる。清書モードだと右側の行内でcaret位置より前に挿入されたりする。

### #1978
CommonMarkの「コードスパン(インラインコード)の内部の両端の半角空白は1つずつ削る。ただし内部が空白2つないし1つだった場合は1つだけ削る」というルールに厳密に従いたい。
- 今は` abc `だと削られるが`` abc ``だと削られない。NG

### #1979
markdownのインラインコードのレンダリングを修正中。
- ` code `は正しく削られるが`` code ``だと削られない。('␠code␠'みたいな感じ)
  - ``` code ```だと逆に空白が増える('␠␠code␠␠'みたいな感じ)ので、fence内部の両端の空白を削る処理の不具合ではなく複数文字から成るfenceが空白になってしまっているのではないか。
  - この「複数文字から成るfence」以降その行内でcaretの描画位置と実際に挿入される位置がズレる。

### #1980
- インラインコード以降のcaretの描画位置がズレる問題は解消した。OK
- fenceがbackquote1つのときは「インラインコード内側の両端の半角空白を除去する」というCommonMarkのルールに従っているが、backquote2つ以上のfenceでは除去されていないように見える。
  - 除去した上でbackquote分の隙間が出来ているのかもしれない

### #1981
- caretの位置も正しいしこれでよいが、backquoteの数に依らずインラインコードは常に「塗り潰し部分を左右0.5ch分広げる」「前後とのマージンを0.5ch加える」としたい。これでbackquote1つで挟んでいるときはdraftと清書とで同じ幅になるはず。
- 清書モードでインラインコードの前後から`w`,`b`で移動するとき、通過するのに2度入力する必要がある
  - おそらく生テキストのbackquote(群)を1wordとして移動しているのではないか。清書時は見た目どおりに移動したい。
