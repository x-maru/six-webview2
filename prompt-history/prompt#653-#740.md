#653
- `s`の挙動・ヘルプ内容はOK
- `s`と`cl`とで相違がないようにしたいので、`cl`でも同様に改行文字も削除対象に。

#654
- NG)`cw`,`CW`の挙動を変更しろなどとは言っていない。word単位に改行も含めるのは望んでいないので元に戻して。
- NG)`cl`の挙動を`s`に合わせろと言っているが、変更されていない。`cl`で改行も削除対象とみなすように。

#655
OK.
- 「ヘルプに cl の改行仕様追記が必要なら」→お願い。

#656
OK。次は不具合対応を。
- `y`/`yy`でコピーしたものを任意の場所で`p`/`P`するとEOFの改行が␤に置き換わる。NG

#657
NG。変化なし。例えば行10にpasteしても最終行100の行末␊が␤に置き換わってしまっている。
  - いま気づいたが、最終行が␊だけから成る行だった場合には変化しない。

#658
OK。
少し戻って`s`の挙動で、仕様変更を入れたい。
- "[行頭]a␤"の'a'にcaretがあるときの`s'や`cl`で、NORMALモードの仕様「␤だけになったら削除して前行にcaretを移動」をスキップしてINSERTモードに移行したい

#659
NG。
- 「直後の改行を同時に削除し、」←不要では？
- 「s: caret が行頭・行長1・後続行あり の場合」←「後続行あり」とは？不要な条件な気がするが？

#660
NG。"[行頭]a␊"のときにaを`s`や`cl`したときは期待動作だが"[行頭]a␤"のときは前行に移動してしまう。
- 追加仕様で、EOF直前の␊は`s`や`cl`による削除対象から除外してほしい。削除せずその場でINSERTに移行してほしい。
  - "abc␊␊[EOF]"の'c'にcaretがあるときに`3s`や`3cl`とした場合に、2つ目の␊は削除せずINSERTへ移行する

#661
NG。
「s/cl は「改行も1文字として扱う」が、EOF 直前の改行は削除対象から除外」←この「改行」に␤も含めれば満たせるかな

#662
NG。
EOF直前の改行をどうするか以外に、NORMALモードでの「`x`などで削除した結果␤だけになったら␤を消去して前行にcaretを移動する」というルールが効いているように見える。
  - `x`などの挙動としてこのルールは継続でよいが、`s`、`cl`だけ例外的にルール適用を除外したいという話か。

#663
NG。何も変わっていない。EOF直前が␊の場合は期待動作だがEOF直前が␤の場合がNG

#664
NG。"xyz␊a␤"でaの位置からNORMALで`s`/`cl`としたところでxの位置にcaretが移動し、␤が消える。期待動作は"xyz␊␤"になって␤の位置(行頭)にcaretが飛ぶこと。
添付スクショで、
- 期待動作：before.png→expected.png
- NG動作：before.png→result.png

#665
NG。変わらず。

#666
NG。変わらず。実現したいことは「␤を残す」というよりは「caret行を維持する」が主眼。
"xyz␊a␤"→NORMALでaの位置で`s`→"xyz␊␤"→NORMALのルールに従って␤削除&caret移動→"xyz␊"という動きに見える。
この「NORMALのルールに従って」の部分を、`s`,`cl`のときは特例でスキップしたいということ。
"xyz␊"になってもINSERTモードなので`↓`で次行に移動すれば望む形にはなるが、その手間を省きたい。

#667
NG。変わらず。
直後のバッファ実体（末尾が \n or \u2424 のいずれか）→見た目で␊でも␤でもない。#664に添付したbefore.png(行55)からresult.pngに遷移すると行番号でも55は消失している。
- 本件はとりあえず「既知の不具合」として憶えておいて、修正は後回しにする。
次は機能追加を。
- NORMALモードの`>>`と`<<`。shiftwidth分だけインデント増減。
  - `:set shiftwidth=N`, `:set shiftwidth?`を実装する
  - shiftwidthはバッファ毎に持つ。初期値は4で、セッション情報に含める。
  - `2>>`で2行まとめてインデント処理。
- VISUALモード選択中の操作に`>`と`<`を追加
  - shiftwidth分のインデント操作。
  - '2>`で2倍量のインデント操作。
  - インデント処理まわりでVim/標準viと相違があれば教えてほしい。

#668
- NG)NORMALで前置カウントが効いていない
- NG)NORMALの`>>`,`<<`のあとのundoが変。関係ない行にまで影響している(undoし過ぎている？)。
- 空行に対しては効かないようにしたい。NORMALでの前置カウントの対象にはする(効かないが)。
- 行頭から1つ以上の連続するTAB文字は無視して、それ以降の行末までに対してインデントするようにしたい。
  - TAB文字の後ろに空白を挿入する。
- ヘルプに記載してくれる？

#669
- NG)NORMALで前置カウントが効いていない
- 他のインデント関係はOKのようだ。
- `>`モーションは実装不要。`>>`と`<<`だけあればいい。

#670
- NG)NORMALで前置カウントが効いていない
  - `>`はShift+`.`だが、キーレイアウトに依存するのは良くない
  - `<`はShift+`,`だが、キーレイアウトに依存するのは良くない
  - `2>>`は`1>>`と同じ挙動になっているが`>2>`は何も起きない

#671
- NG)NORMALで前置カウントが効いていない
- `2>>`は`1>>`と同じ挙動になっているが`>2>`は何も起きない

#672
- NG)NORMALで前置カウントが効いていない
- `2>>`は`1>>`と同じ挙動になっているが`>2>`は何も起きない
- 打鍵速度は測定できないが、これ以上早くタイプするのは不可能な速度でも変化なし。速度の問題ではない。
  - `2`↓ `2`↑ `Shift`↓ `.`↓ `.`↑ `,`↓ `,`↑ `Shift`↑

#673
- NG)NORMALで前置カウントが効いていない
- 押下/離脱順 (#672は書き間違えた)
  - `2`↓  `2`↑  `Shift`↓  `.`↓  `.`↑  `.`↓  `.`↑  `Shift`↑
- 打鍵速度的には`2dd`などと変わらないが`2dd`で失敗したことはない。
- 修正コードを通っていないとかではないのか？ break pointを置く行数を指定してくれれば実際のフローを確認してみるが？

#674
```
2>>: digit (precount): 2 → pending > (precount): 2 → commit >>: 2
>2>: pending > (precount): 0 → digit (pending >): 2 -> 2 → commit >>: 2
10<</<10<も同様にcommit <<: 10になること
```
いずれもOK。実際に前置カウントが正しく効いているようだ。
問題ないと思うので、トースト/Consoleのデバッグプリントを外してくれる？

#675
タブが切り替わったりアプリウインドウがアクティブになる度にConsoleに出ている"[ext-check]"や"[stat]"は外部更新検出の実装時に入れたものだと思うが、安定して動作しているようだし他に目的が無ければ出力を止めてほしい。

#676
"[switch]"も役目が終わったようなので、削除を。

#677
OK。次は機能追加を。
- `y`系コマンドでyankバッファに格納する際に対象文字列を1.0秒間だけcyanで強調表示するようにして。
- "[baseline] after normal-save ..."ってConsoleに出るのはもう用済み？

#678
- 動作OK。
- `Y`系も同じようにフラッシュして。
- 色はwindow.THEMEを参照して。'y'/'Y'での区別は不要。未定義時はyellow
```
  yankFlashColor: "orange",
```
- フラッシュ時間を1.0秒→0.8秒にしてみて。(調整が済んだらユーザカスタマイズは不要)
- "[baseline after normal-save ..."は削除して。(まだ出ているので)

#679
yankフラッシュはこれでOK。
次はshiftwidth関連の機能追加を。
- オーバーレイパレットにボタンを追加する
  - "インデント幅␊2 4 8"
  - パレット内の左下、検索ハイライトボタンの左
  - shiftwidthの値に一致していれば2,4,8のいずれかをハイライト表示
  - クリックする毎に「2→4→8→2」とローテーションする(shiftwidthに代入する)
  - shiftwidthが2,4,8以外の値だったときにクリックすると2にする

#680
- 制御文字表示ボタンの幅を広げるのではなく、パレット左上は空欄にする。
  - 制御文字表示ボタンと検索ハイライトボタンの幅を揃える
- インデント幅ボタンの2と4、4と8の間隔を狭めてボタン幅をもう少し小さくしたい
- 他ボタンも含め、ピル？(OFF, ON, 2, 4, 8)のラウンドをもう少しだけ90°に近づけたい

#681
- ピル角丸の見た目が変わっていないように見える。もう少し角ばらせて。
- オーバーレイパレット内のレイアウト：
```
□■■
■■■
```
  - '□'は空欄を表す。左下がインデント幅ボタン。空欄の右が制御文字表示ボタン。

#682
ボタンレイアウトは意図通り。
- ピルのラウンド指定の数値は変更前と変更後でそれぞれいくつ？間くらいがよさそう。
- オーバーレイパレットの透明度を0にしよう。パレットを無くすわけではない。

#683
OK、いい感じ。次はVISUALモードの機能追加を。
- VISUALモードで選択中の操作に機能追加
  - `/`:選択中の文字列を入力欄の初期値にする形でNORMALモードの'/'に移行する
  - `?`:選択中の文字列を入力欄の初期値にする形でNORMALモードの'?'に移行する

#684
- NG)選択中に`/`や`?`(`Shift`+`/`)を入力しても無視される。
- ヘルプのVISUALタブの操作セクションに記述を。
- req)範囲中の改行やTABコードは"\n"や"\t"に置換してほしい。

#685
- NG)選択中に`/`や`?`(`Shift`+`/`)を入力しても無視される。

#686
- OK)'/','?'が入力できるようになった
- "/選択中"などはヒットするが"/)選択中"はヒットしない。正規表現と解釈されるためか？
  - CMD欄でそのまま`Enter`すれば元の選択範囲がヒットするように、元のVISUAL選択文字列を正規表現化してCMD欄に入れることは可能？CRLFなども含めて。
- 仕様追加)`n`,`N`でヒットした語に対してyankフラッシュと同じ効果を付けたい。
  - window.THEME.yankFlashColorを使うところも含めて流用でいい。フラッシュ時間も流用。

#687
- req)trimしないで。空白はそのまま。連続しててもそのまま。
- CMD欄に入力されてそのまま`Enter`したあと`n`,`N`を押しても他にマッチする箇所がなければno matchになる。
  - `n`,`N`がcaret位置から検索を始めていない？ (既にヒットしているが、caret移動量が0でもno match扱いにはしないでほしい)

#688
- 検証不十分だがtrim除去たぶんOK。次に進めて。
- NG)CMD欄でそのまま`Enter`で"no match"になると検索語の更新が実行されずに以前の検索語がそのまま残っている。
  - #687の対応で合わせて修正されればよいが。

#689
ヒット数0の検索語を格納するのは本意ではないのでパッチはキャンセルした。
- 「n/Nは現在位置から検索します。唯一のヒットしかない場合でも、wrap検索で同じ位置のマッチを返すため「no match」にはなりません（ゼロ移動でもヒット扱い）」←これが仕様通りに動作していないようだ。
  - 例えばこのprompt.md内の'[行頭]#XXX'はユニークになっているが、"␊#689"の'#'の位置で`/^#689`としてもヒットしない。前行などに移ってから`/^#689`ならヒットする。NG。

#690
- OK)`/^#689`はヒットするようになった
- NG)"#689"行の先頭にcaretがある状態で`/#689`がヒットしない。(`/#`の時点で即、次候補に移ってしまう)
  - こちらを修正すれば`/^xxx`の特例扱いは不要なのでは？

#691
NG。変わっていない。1文字進めた位置からインクリメンタルサーチが始まるような挙動になっている。

#692
- OK)`/`,`?`での検索開始位置は正しくなった
- NG)`/abc\n`など正規表現"\n"で␊にマッチしない。

#693
NG)"AB␊CD␊"という2行に対して、`/AB\nCD`はヒットするがVISUALモードの`V`で2行を選択して`/`と入力したときはCMD欄には"/AB\nCD"と表示されるのに`Enter`でno matchになる。

#694
OK。
- req)`n`,`N`でファイル先頭方向にそれ以上マッチしない場合に"no match"は出さずにEOFまでループして検索が続行されるが、EOF方向への検索ではそれ以上マッチしない場合に"no match"とトーストが出てループはしていない。EOF方向へも"no match"なしでループするようにしてほしい。
  - "no match"はファイル全体の中で1つもマッチしない場合のみになる
- NG)インクリメンタルサーチ中のハイライトが\nに対応しておらず、その先まで追従できていない

#695
- 「n/N: 現在位置含めて双方向ラップ」→OK
- req)VISUAL行選択からの`/`や`?`では、caret位置からではなくVISUAL選択範囲の先頭から検索してほしい。
  - もしくはEOF方向でヒットしなかった場合にラップしてほしい(通常の`/`,`?`との共通化など実装がシンプルになる方で)
- 「n/N ラップ仕様とマルチライン検索プレビュー対応、\n/\t アンエスケープ仕様」→ここまでの詳細はヘルプには記載不要。

#696
OK。行選択だけでなく文字選択時もこの仕様で良い。次は機能追加を。
- 検索時に大文字小文字を区別するかどうかのデフォルトのフラグを実装したい。
  - `:set ignorecase`, `:set noignorecase`, `:set ignorecase?`, `:set ignorecase!`
  - `:set smartcase`, `:set nosmartcase`, `:set smartcase?`, `:set smartcase!`
  - `:s`で`i`,`I`オプションを付けなかった場合にも有効。付けた場合はオプション優先。
  - バッファ毎にフラグを持ち、セッション情報に含める
  - セッション情報になかったときや新規バッファでのデフォルトはnoignorecase/nosmartcase

#697
- NG)既に検索語が設定されているとき、ignorecase/smartcaseの設定を変更しても効かない(新たに検索語を設定しないと効かない)
- NG)ファイル先頭方向への`n`,`N`で次候補に飛ばない。EOF方向へは期待動作になっている
- ignorecase/nosmartcase) OK (/Abc/が本文の"abc"にマッチする, /abc/が本文の"Abc"にマッチする)
- ignorecase/smartcase) OK (/Abc/が本文の"abc"にマッチしない, /abc/が本文の"Abc"にマッチする)
- noignorecase/smartcase) OK (/Abc/が本文の"abc"にマッチしない, /abc/が本文の"Abc"にマッチしない)
- noignorecase/nosmartcase) OK (noignorecase/smartcaseと同じ) ※noignorecaseならsmartcase無視

#698
- OK)既に検索語が設定されているとき、ignorecase/smartcaseの設定を変更すれば即、切り替わる
- NG)ファイル先頭方向への`n`,`N`で次候補に飛ばない。EOF方向へは期待動作になっている

#699
- NG)EOF方向への`n`,`N`で次候補に飛ばない。ファイル先頭方向へは期待動作になっている
  - #697,#698の報告が間違い(逆)だったかもしれない．．． いずれにしろ「両方向OK」ではない。

#700
OK。次は別件の不具合修正を。
- wordが改行文字まで含むようになってしまっている
  - `yw`, `vb`など。"A␊␊B"で'A'にcaretがあるときに`yw`で'B'まで含まれてしまう。

#701
- 要求の仕方が悪かった。`yw`,`vb`が「改行を含めないように」ではなく、1度のword移動で通常文字と一緒に改行まで超えてしまわないのが期待動作。
  - "A␊␊B"で'A'からの`yw`では"A"がyankされるし'A'からの`2yw`なら"A␊"がyankされ、`3yw`なら"A␊␊"がyankされるのが期待動作。
- NORMALでの移動`w`なども同じ。"A␊␊B"の'A'から`w`なら1つ目の␊、`2w`なら2つ目の␊に飛んでほしい。

#702
OK。だけどやっぱり仕様変更で、連続する改行は空白のように「1ランまとめて」の扱いにしよう。

#703
OK。次はUI変更を。
― 「右下オーバーレイパレット」と同等の「右上オーバーレイパレット」を新設したい
  - 方針は「バッファ毎に持つ設定値は右上パレット、全バッファ共通のものは右下パレット」
  - タブバー下端および縦スクロールバーと透明パレットが接する位置に描画する。
  - `Ctrl+F9`のパレット表示ボタンは右上と右下の両パレットを同時に扱う
  - 「インデント幅」ボタンを右上オーバーレイパレットに移動する

#704
- OK。次は右上オーバーレイパレット内のインデント幅ボタンの下に「検索時 大/小」ボタンを追加したい。
  - ignorecaseとsmartcaseの状態をまとめて表示・変更する。バッファ単位。
  - ボタン装飾は他のボタンと同じだが、タブバーの「文字コード/改行」のようにクリックでpopupを開く。
  - popup内の選択肢は"常に区別", "混在時区別", "同一視"
    - "常に区別"はnoignorecase、"混在時区別"はignorecase/smartcase、"同一視"はignorecase/nosmartcase
  - ボタンラベル"検索時 大/小"の下に選択された内容を表示
    - 2段目のBGを影付きの浮き上がった紺色にする

#705
- ポップアップはタブバーの「文字コード/改行」のような、その場に表示するpopupがいい。"utf8 unix bom"とか一覧を表示してるやつ。
- ボタン幅を「インデント幅」ボタンに揃えて。

#706
- ignorecaseのpopup幅はボタン幅を超えて良い。選択肢の文字サイズはそのままでpopup幅を広げて。
  - 右端は縦スクロールバーに重ならないようにしたいので左に広げる方向で。
  - 「文字コード/改行」のpopupと同じようにhoverのカーソルを表示するようにして。

#707
- ホバーの色も「文字コード/改行」のpopupの色を採用して。
- popupを開いたままpopup以外をクリックしたらpopupをキャンセルするようにして。

#708
- OK。次は「文字コード/改行」のボタンを右上オーバーレイパレット配下に移動する
  - インデント幅ボタンの上に配置する(元々のボタンを下にずらす)
  - ボタン装飾や挙動はpopupも含めケース設定ボタンに準ずる
  - ボタンラベル(1段目)を"encode"にする
  - popupの選択肢内の"unix"を"LF"に、"dos"を"CRLF"に置き替える
  - "bomb"も改行せずに選択肢の右にappendする

#709
- 表記変更
- "bomb"→"B"
- "utf-8"→"UTF-8"
- "shift_jis→"SJIS"
- NG)popupから選んだあとcaretを動かすなどするまで反映されない(見た目でわかるのは改行文字だけだが)
- 元々の、タブバー内のボタンは削除する

#710
OK。次は仕様変更を。
- `Ctrl+ホイール`による表示倍率をタブ(バッファ)毎に持つようにしたい。
  - set変数による指定などはなし。
  - バッファ毎のセッション情報として記録・再現
  - 新規バッファの表示倍率の初期値は100%。
  - 倍率刻み(選択肢)は従来通り。

#711
NG)各バッファごとの表示行範囲やcaret位置が狂うようになった。#710前には発生していなかった。

#712
NG。変わらない。
F4のタブだけ倍率250%にして他のタブは100%という状態で、一度F4タブに移って戻ってくるとF4タブのcaret位置になってしまうようだ。
(F4タブは2行しかなくcaretは行2にあるが、他タブに移ってもcaretが行2になってしまう)

#713
NG。
- 250%タブは関係なかったかも。100%のタブ同士で行き来するだけでも直前のタブのcaret行数を引きずってきてしまう。
- `G`で最下行に飛んでから即時終了して改めて起動すると常にcaretが先頭行になっている。
- 色々おかしいので、ソースを#711の前に戻した。改めて慎重に実装し直したい。
- `Ctrl+ホイール`による表示倍率をタブ(バッファ)毎に持つようにしたい。
  - set変数による指定などはなし。
  - バッファ毎のセッション情報として記録・再現
  - 新規バッファの表示倍率の初期値は100%。
  - 倍率刻み(選択肢)は従来通り。

#714
- タブを切り替えても前のタブのcaret/表示行範囲に引きずられなくなった。OK
- ひとつのタブ(F1)だけ250%にしてもタブ切替で問題は起きてなさそうだったが、100%のタブ(F2)をカレントにして即時終了して再度起動すると画面が乱れる(添付スクショ)。
  - 行間等は100%時のままフォントが250%になっているように見える。
  - caret行や表示行範囲は正しいと思われ、一度他タブに移って戻ってくると正常な100%表示になっている。

#715
- 概ねOKだが。
- F1(250%, untitled, caret行2), F2(100%), F3(100%), F4(300%)で、
  - F2,F3,F4がカレントの状態で即時終了、再起動 →F1のcaretが行1になる (F2,F3,F4は正常)
  - F1がカレントの状態で即時終了、再起動 →F1の行2以降が表示されない (他タブに行ってF1に戻ると正常。ただしcaretは行1) (F2,F3,F4は正常)

#716
変わらない。F1で終わると再開時1行目しか表示されない、どのタブで終わってもF1のcaretが2→1に変わる

#717
NG。どうもF1、というか一番左にあるタブ限定の症状っぽい。
- F1(250%, untitled)を:`q!`で破棄して、`:e nonexist`で新規バッファを作った(F4)
  - F4を250%に、カレントの状態で即時終了、再起動 →F2,F2,F4は問題なし。F1(元F2,100%,caret行365)だけcaret行1になっていた。
  - F1(元F2,100%,caret行365)をカレントにして即時終了、再起動 →2行目以降が表示されない。他タブへ行って戻ってくると正常

#718
OK。F1のレジューム時の問題は解決した。
次はZoom HUDを常時表示にしよう。
- タブバーのencBtnの残骸を完全に取り除き、代わりにZoom HUDを常時表示する。
  - 右端を右上オーバーレイパレット内のボタンの右端に揃える。

#719
NG。posInfoが消えた。Zoom HUDの下に隠れてる？
- モードインジケータ([NORMAL])、posInfo(行Y,列X/W)、Zoom HUDの順で並べて表示するのが期待値。
- NG)タブを切り替えてもZoom HUD内の倍率が連動して切り替わっていない。
- Zoom HUDのBGを透明にして。境界線は残して。＋－、リセットのBGはそのままで。

#720
OK。
- 本文textareaの外側でCtrl+ホイールの操作をした場合も本文textareaに対するものとしてsix自前で処理したい
  - 現在はEdge側でアプリウインドウ全体の拡大縮小として扱われている
  - Edgeが出すZoom HUDを抑止したい

#721
OK。Ctrl+ピンチ（トラックパッドのズームジェスチャ）も同様に抑止しといて。

#722
OK。
- six.ps1のショートカットを作成するinstall.cmdを書いて。
  - パスはC:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe -ExecutionPolicy Bypass -File six.ps1
  - (powershell.exeのパスは可能なら動的に取得したい)
  - _six.htmlなど構成ファイルはすべて同一ディレクトリに展開されているものとする

#723
互換を取るための作業は不要。今後は.wv2-profileでよい。
- .edge-profileを削除して、新たにinstall.cmdで作成したショートカットから起動してみた
  - 起動が遅くなった (他に起動しているsixはいないはず)
  - `:e popup`で"(loading...)"のまま何も候補が表示されなくなった。
    - `:e /wsl`,`Tab`とすると`:e /wsl.localhost`と補完されるが'/Tab`とすると`:e /wsl.localhost/wsl.localhost`となる。

#724
悪化した。
初回に開く空バッファのタブ(未編集)のみで即時終了した状態で2回目以降の起動で0分50秒もかかった。非実用的。
今気づいたが、.wv2-profileが生成されていない。OLD.edge-profileとリネームしたものとは別に.edge-profileが生成されていた
- .edge-profileを削除して試そうかと思ったが、別のプログラムが開いていると言われて削除できない。#723の時点でWindowsを再起動してからリネームしたが、もっと簡単に出来ないか？
- `:e`の挙動も何も変わっていない。loading...もwsl.localhost/wsl.localhostも。

#725
- OK、起動に時間がかかり過ぎる現象は発生しなくなった。
- `:e popup`でファイル一覧を取得できるようになった。
  - 一点、:e popupを実装してた頃の現象だけど"//wsl.localhost"から`/`で一覧を取得したときに"Ｕｂｕｎｔｕ"のように全角？で表示される現象が再発した。

#726
- NG、全角のまま。`Tab`でコマンド入力欄に取り込むと"Ubuntu"になるし見た目だけだが、直したい。

#727
- NG、全角のまま。少なくともセッション情報からのレジューム起動を実装する前までは頻繁にwsl側のファイルを開いてたので、それ以降でのデグレだと思うが．．．

#728
- NG。全角のまま。なお`Tab`では半角で"Ubuntu"と入るが`Enter`やクリックでは全角のまま入る。
- 「bufpopup .name に unicode-bidi: plaintext; を追加してレンダラ依存差異を減らします。」→リスクがないならやって。

#729
- NG。
- prompt#568直前のcommitでは全角にならず期待動作だった。prompt#577までのどこかで動作が変わったようだ。
- 添付のdiff.txtは両commit間の差分である。確認してみて。

#730
- NG。現象変わらず。
- 添付のスクショをマウスで選択して貼り付けてみたが、張り付けると半角に見える．．．
```
:e //wsl.localhost/Ubuntu/
```

#731
NG。変化なし。

#732
NG。選択した内容は#730に書いたものと同じ。
```
:e //wsl.localhost/Ubuntu/
```
「必要ならコードポイント可視化（デバッグツールチップ）を組み込み、原因文字を特定します。実装しましょうか？」←お願いする。

#733
「U+0055 ...」だった。
U U+0055
? U+0000 CTRL
b U+0062
? U+0000 CTRL
u U+0075
? U+0000 CTRL
n U+006E
? U+0000 CTRL
t U+0074
? U+0000 CTRL
u U+0075
? U+0000 CTRL
/ U+002F

#734
OK。"Ubuntu"表記が修正された。
- `Ctrl+Shift+D`のデバッグ機能を削除しといて。
- 起動時に開くバッファがないときに作成される空バッファに挿入される文言("The quick brown fox...")を変更したい。
```
このバッファは実ファイルに紐づいていないダミーバッファです。
`:q`で破棄しても問題ありません。
※sixはバッファ無し状態で動作することはないので、他にバッファ(タブ)が無くなれば終了します。

好きに編集して`:e ファイル名`で保存することも可能です。
```

#735
- ヘルプダイアログのヘッダの文言("Sixヘルプ")を「_six.jsの冒頭で定義した`VERSION_STR`」にしてほしい。

#736
OK。次は細かいUI調整を。
- ヘルプボタンの2段目を"F9;:help"から"F9, :help"に変更
- 右上パレットのボタン「検索時 大/小」を「検索時 A/a」に変更

#737
OK。次。
- パレット表示ボタンの"^F9"をボタン内で縦方向センタリングで。
- Zoom HUDの左端マージン("100%"の左)を0.5remに。右端マージンも。

#738
- Zoom HUDの指示を間違えた。"100%"(id=zoomVal)の左右マージンは0で、その外側(div.box)のpadding左右を0.25remにしてほしい。

#739
OK。次。
- モード表示(modestatus)のフォントサイズを1.2倍にして。
- 右上(右下)パレット内のボタンのpaddingを{4px, 3px}にして。

#740
OK。
質問1：1024x1024.png, 512x512.png, 256x256.pngにアクセスするタイミングは？
質問2：アイコンファイルなどをjarファイルにまとめておいて、動的に参照することは可能？
