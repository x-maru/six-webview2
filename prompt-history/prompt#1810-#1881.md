### #1810
バッファの総行数が画面行数より大きいときに`ggG`としたときの仕様：
- 縦スクロールバーのthumbが下端に来る
- EOF後の余白行が[6行+半端]になる (半端はないケースもある)
markdown offのときは期待動作になっているが、200行を超えるバッファでmarkdown on(常にwrap on)のときに`ggG`でthumbが下端に来ない不具合を修正中。
- 画面表示範囲は正しく「余白6行+半端」になっているがthumbがおかしい。

### #1811
- 『md-rich 専用の「物理bottomへthumbをピン留めする分岐」』は誤ったコードで、削除したつもりが残骸として残っていただけ。完全に消しといて。
  - EOFが行812のとき、`:800`あたりから`j`で下がって行くと行811までは正常(ただしthumbの下が空きすぎている)で行812に到達した途端にthumbが下端に飛んで本文が何も表示されなくなる(画面全体が余白行になっている)。
  - 「EOFに来たらthumbを下端に」が期待動作なのではなく、「EOFに来たときにちょうどthumb下の空きが無くなる」ようにスクロールバーの縮尺を調整しろ、ということ。

### #1812
- prompt812.md(添付,812行)
  - `:800`あたりから`j`で下がって行くと行812で止まる(それ以上下がれない)が、そのときthumbの下に大きな空きがある)。
  - `ggG`でthumbは下端に行くが本文エリアはすべて余白。posInfo表示では行812になっており、`k`を連打すると行781に来たところで急に本文が表示される。
  - scrolloffは10。行783から行806までコードブロックがある影響か？(そのコードブロック内に折り返しはない)

### #1813
- prompt812.md(812行)
  - `ggG`でEOF下の余白が[6行+半端]だけ見える位置に表示範囲が設定された(OK)が、そのときthumbの下に大きな空きがある(NG)。
  - `ggG`から`k`で画面内全行が消えてthumbが下端になる。さらに`k`で`ggG`直後と同じ状態に戻り、以後その繰り返し。
    - その間ずっとposInfoは行812のまま
- なお行数199のheavy_md-199.md(添付)では固定余白が過剰(24行+半端)だがthumbやcaret移動の動きは自然。

### #1814
かなり改善した。
- prompt812.md(812行)
  - `v`/`V`後のハイライト表示が2行下にズレる
  - `ggG`でthumbが最下端になり余白が[5行+半端]になる。期待動作は[6行+半端]
  - EOF行812から`k`で行772に飛ぶ。(表示範囲[733,779]) 何故？
    - 直後に非markdown/wrap onのバッファに移動するとcaret/アクティブ行背景/行番号/posInfo表示がセットで「1行下にズレる」。入力は正しい位置(1行上)になる。markdownを1度onにしてoffに戻すと正常になる。
- heavy_md-199.md(199行)
  - `ggG`でthumbが最下端になり余白が[5行+半端]になる。期待動作は[6行+半端]
  - 他は正常

### #1815
- prompt812.md(812行)
  - `y`/`Y`後のハイライト表示が2行下にズレる
    - 先頭80行にH1/H2や折り返しといった行高の変わる行は1つもない(行81で初めて折り返しが現れる)が、行39で`h`,`l`とタイプするたびに行番号ガターが一瞬上にズレてすぐ戻る
      - 行1～行38では発生しない。表示範囲は[1,48+半端]。
      - scrolloff=10にしてあるのが影響しているのかとも思ったが、scrolloffを5にしても「行38はOK、行39で振動する」は変わらない。
  - `ggG`でthumbが最下端になり余白が[5行+半端]になる。期待動作は[6行+半端]
  - EOF行812から`k`で行772に飛んで一瞬後に行771に勝手にcaretが動く。
    - 「直後に非markdown/wrap onのバッファに移動するとcaret/アクティブ行背景/行番号/posInfo表示がセットで「1行下にズレる」→この現象は収まった。
    - 行772から行771に行ったあと`j`で下に移動すると行番号とcaretの描画が半行上にズレる。
- heavy_md-199.md(199行)
  - `ggG`でthumbが最下端になり余白が[5行+半端]になる。期待動作は[6行+半端]

### #1816
何も変わっていない。
- prompt812.mdで'y'/'Y'時のハイライトのズレは、+2,-1,0,+1,+2行のケースがあるようだ。
  - caret位置より上に存在する折り返しや(un)ordered listの数によるのかも？

### #1817
- prompt812.mdで'y'/'Y'時のハイライトのズレは収まった。OK
- scrolloff=10で、prompt812.mdで`k`によるscrolloffの境界が`j`の境界になってしまっている。
  - 下から10行目を抜けるときにcaret移動ではなくスクロールになってしまう。

### #1818
変化なし。下から10行目の境界を超えられないのではなく、たとえば下から15行目にジャンプしたあとも`k`がスクロールになってしまう。

### #1819
- markdownでのcaret移動/スクロールが正しくscrolloffを反映するようになった。OK
- unordered listで折り返しているのにmarkdown onで折り返し表示にならないケースがある。(おそらく折り返し後の文字数が少ない場合)
  - 添付のL743-wrap.png(markdown off)と同じ個所をmarkdown onにしたのがL743-wrap-md.png
    - どちらも同じファイル(添付prompt812.md)

### #1820
- prompt812.md(812行)の行743:
```
- Toastは不便なので_six.js冒頭のVERSION変数の末尾".s"の部分を書き換えて。zの次はaに戻るでいい。ヘルプダイアログで確認する。
```
- markdown off/wrap on:「で確認する。」が2行目になる。
- markdown on,清書:「する。」だけが折り返し2行目になり、本文は期待通り折り返しの行高で表示されるが、折り返し2行目にまで行番号(744)が振られてしまっている。NG
- markdown on,draft:行高が1行分で行番号も743行のみだが「する。」が折り返しており、次行と重なって描画されてしまっている。

### #1821
- markdown onでの清書とdraftでの折り返しのずれが解消した。OK
- prompt812.md(812行)で、`ggG`のあと`k`押しっ放しで(scrolloff=10に従って)スクロールが発生したところで止めると、行番号とcaretが半行ほど上にズレる。他バッファに移って戻ってくると正常に戻る。

### #1822
`ggG`後にスクロールが発生するまで`k`押しっ放しで止めた際のズレは解消した。(scrolloff=10)
次は関連していそうな不具合修正を。
- prompt812.md(812行)で、`ggG`のあと`k`押しっ放しで(scrolloff=10に従って)スクロールが発生したところで止めたあと、再度`k`と入力すると12行上にcaretが飛ぶ。

### #1823
- caretが12行上に飛ぶ現象はなくなった。OK
- prompt812.md(812行)で、`ggG`のあと`k`押しっ放しで(scrolloff=10に従って)スクロールが発生したところで止めたあと他バッファに移って戻ってくるとposInfoは変わらずcaret等も正しいが表示範囲先頭が行767から行771に変わっており、`h`,`l`など行内に留まるcaret移動をしただけで先頭行が瞬時に767に変わる。NG

### #1824
表示範囲がバッファ移動後に補正される現象は解消した。OK
- markdownバッファでコードブロック内にcaretがある状態でmarkdown off,wrap onのバッファに移ると画面内のすべての改行文字の位置がズレる。
  - 即時終了して起動し直すと正常になる。
  - wrap offでは発生しない。

### #1825
- markdown off,wrap onバッファの改行描画が他バッファに引きずられなくなった。OK
- 次はGFMのインラインリンクを実装したい。
  - [表示テキスト](URL "ツールチップ")
  - ツールチップは"str"でも'str'でも(str)でも可
  - ツールチップはwindow.EX1_THEMEまたはwindow.THEMEのlineGradientStart/editorTextColorで描画する。
  - URL内に')'を含めるときは'\)'とする
  - マウスホバー時はツールチップを別レイアで描画すると同時に(markdown offでも有効な)クリッカブルリンクと同じハイライトを描画する

### #1826
- レンダリングされていない。NG
- markdown offなのにマウスホバーでツールチップが出る。NG
- "windowsEX1_THEME"と書いたのはtypo。theme = 'ex1'のときはwindow.EX1_THEMEを優先的に参照して。
- 書き忘れたがツールチップ指定は省略可能。[表示テキスト](URL)でもいい。

### #1827
```
[表示テキスト](URL "ツールチップ")
```
は、markdown清書では
```
表示テキスト
```
と表示されなければならない。
- markdown draftではcaret行はmarkdown offと同じレンダリングとしてURL部分だけが従来仕様でクリッカブルになる。
  - なのでdraft時はハイライトはURL部分だけ。清書時は表示テキスト部分だけ。
- ツールチップ指定を省略したときにデフォルトのツールチップなど出さない。ツールチップなしにする。

### #1828
- `表示テキスト`に対するホバーがズレたりハイライトがズレたりツールチップがズレたりする。ズレない場合もある。
  - おそらくバッファ内でインラインリンクより先頭よりにあるmarkdown記法の種類や数によって違ってくるのではないか。
  - `表示テキスト`の1行上のあたりにホバーするとハイライト/ツールチップが出るケースがある。
  - ハイライトの表示位置が1.5行ほど上にズレるケースがある。
  - ツールチップが`表示テキスト`の右側(Y座標は揃っている)に出たり、右側で2行ほど下に出たりする。条件不明

### #1829
- インラインリンクの後ろに文字が続くとインラインリンクが認識されなくなる。1行独占の仕様ではないのでNG
- インラインリンクより先頭側にコードブロックがあるとホバー判定とツールチップ表示が上に2行分ほどズレる(ハイライトは正しい位置になっている)。
- インラインリンクの右側が行内すべてリンク扱いになっている。期待動作は`表示テキスト`部分のみがリンク。

### #1830
- インラインリンクより先頭側にコードブロックがあってもホバー判定とツールチップ表示がズレなくなった。OK
- ハイライトが1ch分長すぎる
- `[表示テキスト](URL "tooltip")XYZ`のように後続の文字があるとホバーが有効な領域が横に短くなる(左端は正しい)。
- インラインリンクの後ろがすぐ␊のとき、␊の右側が行内すべてリンク扱いになっている。期待動作は`表示テキスト`部分のみがリンク。

### #1831
- ハイライトが1ch分右にズレている
- `[表示テキスト](URL "tooltip")XYZ`のように後続の文字があるとホバーが有効な範囲が左にズレる
  - 下記の例では"ijkl"がホバー範囲で"xtOPQ"を右に0.1chくらいずらした範囲がハイライトされる。
abcdefghijklmn[text](URL "label")OPQRSTUVWXYZ
- インラインリンクの後ろがすぐ␊のとき、␊の右側が行内すべてリンク扱いになる現象は解消したが、下記の例では"ijklm"がホバー対応かつクリッカブルになっている
abcdefghijklmn[text](URL "label")

### #1832
- ハイライトが0.3ch程度右にズレている
- インラインリンクより左にある文字列の長さに比例してハイライト範囲が右にズレるようだ
  - 下記の例では"extOP"がハイライトされる。
abcdefg[text](URL "label")OPQRSTUVWXYZ
  - 下記の例では"xtOPQ"を右に0.1chくらいずらした範囲がハイライトされる。
abcdefghijklmn[text](URL "label")OPQRSTUVWXYZ

### #1833
- ハイライトのズレが解消し長さも正しくなった。OK
- 清書時でもlist表示の改行文字がdraftの位置になっている。清書/draftを動的に反映させたい。

### #1834
『次に「清書↔draft切替コマンド（md_draftedit）」実行時にも listchars を即リフレッシュするように（全行再描画 or 可視範囲再描画）も追加』←追加して。

### #1835
- listcharsはOK。
- md_draftedit='清書'のとき、NORMALでもINSERTでもインラインリンク行内のcaret移動範囲がdraft時のものになっている。NG

### #1836
- インラインリンクに続く文字列がなく␊で終わっているとき、インライン末尾の␊から`l`で16chほど右の位置に飛ぶ。
- インラインリンクの後ろに文字列が続いている場合、インラインリンクの次の文字の位置から`l`で␊の3chほど右の位置に飛び、そこから12文字分は1文字ずつcaret移動する領域があるようだ。

### #1837
↓列8に移動できない。列7⇔9。ほか列12⇔27。列27～39は繋がっている。
abcdefg[text](URL "label")OPQRSTUVWXYZ
↓列1から`l`で列2に移動できない。列3から`h`で列2へは移動可能。列2から`h`で列1へは不可。`0`なら列1に移動可能。列5が␊だが列6まで移動できてしまう(`$`でも列6になる)。列7以降はない。
[text](URL "label")

### #1838
移動可・不可の桁がズレたくらい。先ほどからずっとmarkdown清書の話。NORMALでもINSERT(`←`,`→`)でも同じ。
↓列9に移動できない。列8⇔10。あとは変わらず列12⇔27。列27～39は繋がっている。
abcdefg[text](URL "label")OPQRSTUVWXYZ
↓列2に移動できない。列1⇔3。列5が␊だが列5⇔列20。20はEOL
[text](URL "label")

### #1839
- 適切な範囲を抜けなくcaretが移動するようになった。OK
- 次はインラインリンクとは関係ないがmarkdown時の挙動修正を。
  - markdown off,wrap onのときは␊だけが次行に折り返すことはなく1行目にぶら下がっているが、markdown onでは␊だけの折り返し行が発生してしまう。発生しないようにしたい。

### #1840
- 下記は今のウインドウ幅でぎりぎり折り返していない(markdown on)。ここで`e`とタイプすると'e␊'まで1行目に表示されるが何もない折り返し2行目が発生する。さらに`f`とタイプすると1行目にappendされるが'␊'は折り返し2行目に表示される。
あいうえおＡＢＣＤＥかきくけこＦＧＨＩＪさしすせそＫＬＭＮＯたちつてとＰＱＲＳＴＵなにぬねのＶＷＸＹＺはひふへほabcd

### #1841
markdown onのときの折り返しで改行だけ折り返してしまうケースを修正しているところ。
- markdown off,wrap onでは␊だけ次行に行くことはなく1行目にぶら下がる。
- 下記は今のウインドウ幅でぎりぎり折り返していない。ここで`e`とタイプすると'e␊'まで1行目に表示されるが何もない折り返し2行目が発生する。さらに`f`とタイプすると1行目にappendされるが'␊'は折り返し2行目に表示される。
あいうえおＡＢＣＤＥかきくけこＦＧＨＩＪさしすせそＫＬＭＮＯたちつてとＰＱＲＳＴＵなにぬねのＶＷＸＹＺはひふへほabcd

### #1842
かなり改善した。
- #1840の下記の例で'…e␊'、'…ef␊'までは良いがさらに`g`をタイプすると'…ef␊g'となってしまう。期待値は「'…ef' 'g␊'」という折り返し2行表示(␊は論理行末だけに表示)
  - ␊が論理行末にならない症状はmarkdown off,wrap onのときも同様
あいうえおＡＢＣＤＥかきくけこＦＧＨＩＪさしすせそＫＬＭＮＯたちつてとＰＱＲＳＴＵなにぬねのＶＷＸＹＺはひふへほabcd

### #1843
変化なし。
- markdown onでは'…ef␊','g'の折り返し2行表示になる(行番号は1つだけ、行高は2行分)。
- markdown offでは'…c ␊','d'になる。(␊の前に'd'を表示できそうな空きがある)
  - 折り返しが発生する文字列長がmarkdown時と異なるのは問題ではない

### #1844
␊の描画位置に変化なし。
- markdown onでは'…ef␊','g'の折り返し2行表示になる(行番号は1つだけ、行高は2行分)。
- markdown offでは'…c ␊','d'になる。(␊の前に'd'を表示できそうな空きがある)
  - 折り返しが発生する文字列長がmarkdown時と異なるのは問題ではない

### #1845
- 折り返した際に␊が正しく論理行末側に付くようになった。OK
- markdown onで`g`をタイプして'…ef' 'g␊'になった直後に`Ctrl+z`でundoすると画面内すべての改行list描画がズレる
  - 行単位でズレているのかもしれない。検証不十分
  - 他バッファに移って戻ってくるだけで改行list描画も正しくなる。
  - `Ctrl+z`の代わりに`Backspace`で削除した場合は改行list描画も正しい。
  - markdown off,wrap onのときは`Ctrl+z`でも正常

### #1846
- markdown onで`g`をタイプして'…ef' 'g␊'になった直後に`Ctrl+z`でundoすると画面内すべての改行list描画がズレる(listcharsがリドローされない)
  - `Ctrl+z`の代わりに`Esc`, `u`とした場合は正しく描画される(NORMALモードでのundo)

### #1847
- markdown,INSERTで折り返しが発生した直後の`Ctrl+z`でlistcharsが正しく描画されるようになった。OK
- 次は、インラインコードを含む行でcaret位置より左にあるインラインコード(`a`など)の数だけcaret描画位置が左に少しずつズレるのを修正したい。インラインコード1つでズレる量は0.5chにも満たないように見える。

### #1848
- ズレは解消した。OK
- 次はインラインリンク(清書)時のマウスホバーのハイライトのズレを修正したい。
  - #1833でズレを解消したが、リンクのある行より上に可変行高(見出し1,2やコードブロック。折り返しは大丈夫のようだ)があるとハイライトが上にズレる。

### #1849
- インラインリンクのハイライトズレは解消した。OK
- コードブロック内はMarkdown記法があってもレンダリングせず清書モードでも生テキストで表示されるが、レンダリングされた場合の位置と長さでホバー・ハイライト・改行表示が対応し、クリックするとリンクが開いてしまう。
  - コードブロック内のインラインリンクは反応しないようにしたい。

### #1850
- コードブロック内のリンクが無効になった。OK
- 改行のlist表示がコードブロック内でも生テキストでのEOL位置ではなくレンダリング時のEOL位置になっているようだ。NG

### #1851
- コードブロック内の改行位置が正しくなった。OK
- コードブロック内で生テキスト表示なのにcaretがレンダリング時のEOL位置までしか移動できない。NG
- 生URL文字列のリンク機能に関してはMarkdown実装とは別物だが、特例でインラインリンクのdraft時には効かないようにしよう。
  - 清書モード時のコードブロック内もdraft扱い。

### #1852
- コードブロック内で生URL文字列のリンクが無効になった。OK
- コードブロックや可変行高を含むバッファでmarkdown offにしても改行の描画位置が更新されず、caretの移動範囲も改行描画位置に従っているようだ。別タブに移って戻ってきても解消しない。即時終了&再起動で正しくなる。
- markdown on時、NORMALモードでcaretを動かす度に(スクロールしていないのに)listcharsが再描画される。不要では？
  - INSERTモードでは再描画されない。(期待動作)

### #1853
- markdown offにしたとき、wrap onだとlistcharsが即座に更新されない。
- markdown offにしたとき、wrap offだと全行でcaret描画位置と改行描画位置が1chか2chほど左にズレる。
- markdown on,NORMALでcaret上下移動(`j`,`k`,`↓`,`↑`)時に画面内の全listcharsが都度再描画される。
  - INSERT時は元々上下移動でも再描画は発生していない。

### #1854
- markdown offにしたとき、wrap onだとlistcharsが即座に更新されない。→変わらず。NG
- markdown offにしたとき、wrap offだと全行でcaret描画位置と改行描画位置が1chか2chほど左にズレる。→解消した。OK
- markdown on,NORMALでcaret上下移動(`j`,`k`,`↓`,`↑`)時に画面内の全listcharsが都度再描画される。→変わらず。NG
  - keydownのまま移動している間は再描画されないが、keydown,keyupを繰り返すとkeydownの度にlistcharsが再描画される。

### #1855
変化なし。
「連打」とは？keydown/keyupを高速に繰り返すと、繰り返している間はlistcharsの描画が消えるので、そういう意味では「再描画はしていない」かもしれないが、keyup後に0.5sほど空けるとそのあとのkeydown時に毎回listchars再描画が走っている。

### #1856
- markdown on,NORMALでcaret上下移動でlistcharsが再描画されなくなった。OK
- markdown offにしたとき、wrap onだとlistcharsが即座に更新されない。→変わらず。NG
- markdown on,NORMAL,draftでcaret行のmarkdownのうちインラインリンクや見出しが清書のままになっている
  - (un)orderded listでは問題ない
  - commit-id 6862e1dでは正常だった。commit-id 5b63faeでは最新同様に清書のまま。

### #1857
OK。次はmarkdown on時のUI変更を。
  - markdown onのときだけ、wrap行の行番号を縦方向にセンタリングしたい。

### #1858
OK。次はヘルプダイアログを改造したい。
- ヘルプダイアログ内のタブの内容を外部ファイルから読み込んでmarkdownと解釈してReadonlyで表示したい。
  - タブ毎に分かれたファイルを"_six.js"と同じ場所から起動時に読み込む
    - ファイル名は"任意名.NN.md"とする。NNは01～99。例）"maintab.01.md"
    - NN順に左からタブに割り当てる
    - ファイル1行目をタブ名にする。行頭に"# "～"###### "の見出し記号があれば記号を取り除いた部分をタブ名にする
  - 基本的に操作体系や表示スタイルは今のままとする。破綻や未検討仕様があれば指摘して。
- NN=01～06のmdファイルを置いて先頭行だけ埋めたので、現在のヘルプダイアログの文面を極力体裁を保ってMarkdownで書き込んでみてくれる？

### #1859
OK。次はunordered listレンダリングの不具合修正を。
- draftでは折り返さない文字数で、清書でも␊まで1行目に描画されているのに空の2行目が発生してしまう。
  - 末尾に例えば'x'1文字を追加しても変化なし('x␊'まで1行目)。もう1文字追加すると折り返す。
  - draftと清書で折り返す/折り返さないの違いがあるのはいいが、見た目1行に収まっているのに2行になるのがNG
- ordered listで発生するかどうかは不明

### #1860
- 折り返し発生境界前後でdraftと清書の長さが揃い、空の2行目が発生することが無くなった。OK
- インラインコードの清書において、draft時よりピクセル幅が大きくなっている。
  - インラインコードの塗り潰しにborderを付けている影響か？左右マージンを減らしてもいいので、draft時と同じピクセル長になるようなレンダリングにしたい。

### #1861
- インラインコードのレンダリング幅がdraft時と一致するようになったが、border colorが四辺で同じになってしまった。
  - up/leftをwindow.EX1_THEME/window.THEMEのinlineCodeBorderUL、bottom/rightをinlineCodeBorderBRにできる？

### #1862
- インラインコードのborder幅を1pxにして。
- インラインコードの塗り潰しの色をwindow.EX1_THEME/window.THEMEのinlineCodeBGにして。
- there='ex1'のときはTHEME参照が指定されている全ての色に対して、window.EX1_THEMEを参照して未定義だった場合はwindow.THEMEを参照し、window.THEMEを参照して未定義だった場合はyellowにして。
- there='basic'のときはTHEME参照が指定されている全ての色に対して、window.THEMEを参照して未定義だった場合はyellowにして。

### #1863
ヘルプダイアログとgrepダイアログの色指定を変更する。
- ヘルプダイアログはwindow.HELP_THEME→window.MODAL_THEMEの順で参照し、見つからなかったらyellow。
- grepダイアログはwindow.MODAL_THEMEのみを参照し、見つからなかったらyellow。
- 文字色(固定) → 'fgColor'
- 'helpModalBg' → 'bgColor'
- 'helpCloseFg','helpCloseBg','helpCloseBorder' → 'simpleBtnFg','simpleBtnBg','btnBorder'
  - grepダイアログの[実行]/[キャンセル]もこれで。
- 'helpTabFg','helpTabBg' → 'tabFg', 'tabBg'
- 'helpTabActiveFg','helpTabActiveBg' → 'activeTabFg','activeTabBg'
- Markdownインラインコード：
  - 'helpKbdFg','helpKbdBg' → 'inlineCodeFg','inlineCodeBg'
  - borderのtop/leftとbottom/rightも新たに指定する → 'inlineCodeTL','inlineCodeBR'

### #1864
grepダイアログの色指定を修正する。
- 入力欄のbackground, border(top/left), border(bottom/right)をそれぞれMODAL_THEMEの下記を参照する：
  - inputBoxBg, inputBoxTL, inputBoxBR
- 「履歴 F4」「F1」「F2」「F3」は文字色/bg/borderともinlineCodeFg/Bg/TL/BRを適用する
- 「このバッファ」「ディレクトリ+ファイル指定」「ファイル指定」「検索時 A/a」はオーバーレイパレットのボタンにスタイルを合わせる。(今あってないのはborder color)
- 「正規表現」「リテラル」を囲う線はMODAL_THEME.groupBorderにする。

### #1865
新機能を実装したい。
- `:colorpicker` を実行するとマウスクリック待ちになり、sixウィンドウ内外を問わずクリックした点の色をWindowsクリップボードにコピーする。
  - 16進3バイト表記、16進4バイト表記、rgba表記、(あれば)一致するHTMLカラー名称を1行ずつ3行ないし4行で出力する
  - 例）ff0000␊ff0000ff␊rgba(255,0,0,1.0)␊red

### #1866
- colorpickerの動作はOK。
  - `:colorpicker!`、`:colorpicker?`って要らなくない？
- 色名変更：
  - HELP_THEME/MODAL_THEMEのbtnFg,btnBg → simpleBtnFg, simpleBtnBg
- markdown onのときNORMALで`V`と入力するだけでスクロールが発生してcaret行が画面最上行になったり画面上端より上にはみ出たりする。
  - 選択を伸ばそうと`k`,`j`と入力すると不規則に10数行スクロールしたり、動きが把握できない。

### #1867
- 「互換のため旧キーも読みます」→やめて。基本的に「万一に備えたフォールバック」系は実装しないで。バグがあっても気付けない。クラッシュする方がずっといい。
- markdownでの`V`でスクロールする現象はどのバッファでも発生するわけではないが、添付のprompt812.mdで発生する。
  - `gg:400`で表示範囲は[377,422],だが、ここで`V`を押しただけで[453,495]になる。posInfoでは行400のままだが行453にcaretが表示される。`v`として選択をキャンセルすると[392,436+半端]になって行400にcaretが表示される。

### #1868
`gg:400`直後は[378,423]で`V`でスクロールは発生しないが、`gg:400`,`kV`だと[430,475] (posInfoは行399なのに行440にcaret表示)になり、`gg:400`,`jV`だと[430,475] (posInfoは行401なのに行442にcaret表示)になる。

### #1869
- markdown onでの`V`での謎スクロール現象はなくなった。OK
  - 別現象だが、markdown off,wrap on,INSERT時にIMEで未確定文字列を1文字入れるだけでcaretとアクティブ行背景が数行下に飛ぶ。ただし未確定文字列の入力は元の位置に挿入され、未確定文字列が無くなると(確定すると)care/アクティブ行背景は元に戻る。
  - EOFまでの行数が少ない行のときは、入力位置とcaretの乖離だけでなく数行上への本文ジャンプも伴う。ただしlistcharsはついてこない。

### #1870
400行程度のバッファでは発生しなくなったが、prompt812.md(行数812)のEOF付近などではcaretの乖離は起きないが、
- [763,810]posInfo行780だったものが未確定文字を入力した途端に行番号だけ[27,74]になり、本文は7行ほど下にジャンプする。(行780だった本文行の行番号が51になる)

### #1871
prompt812.mdのEOF付近でも行番号がおかしくなる現象は起きなくなった(text縮約回避)。OK
同バッファでmarkdown onにしたときの日本語入力は(以前からだが)尋常でなく重い。もう対策尽きたんだっけ？
- 重いというか、先行入力やIMEのポップアップ処理などは普通に出来るが完全にブラインドタッチになってしまって、10数文字入力したものが画面に現れるのが10秒前後遅れる感じ。別プロセスからの返事を待っているような感じ？

### #1872
Markdown onで行数の多いバッファのINSERTモードでIME入力をすると入力したキーが即座に画面に表示されずに数文字分まとめて遅れて表示される症状の対処中。
- 入力した未確定文字列がすぐには表示されないのでブラインドタッチ状態になる。
- 例えば「にほんのなつはとてもあつい」と入力し始めて3,4秒後に「にほんｎ」と表示され、7,8秒くらいしてから「日本の夏はとても暑い」と出る。重いというより何かのラグがある。cs側との通信が滞っているとか？

### #1873
背景が赤くなる・不意にスクロールする・入力していない文字が挿入される、といった症状はなくなったが、数文字単位で10秒前後遅れたあとまとめて表示される症状は改善されていない。HEADと2907386とでコード差分はかなりあるが、挙動の違いがないように見える。

### #1874
- IME ONでもOFFでも10秒前後→5,6秒前後になった気がするが。誤差かもしれない。
- 遅延の挙動はdraftモードでも清書モードと変わらない。
  - XYZ

### #1875
OK。次はmarkdown on時の仕様変更を。
- markdown onのときだけ`Tab`、`Shift+Tab`の挙動を変えたい。
  - ordered listおよびunordered listの行内では`>>`,`<<`の挙動にしたい
    - NORMALだけでなくINSERTモードのときも。(markdown onではTABコードを入力する術が無くなるが、それでよい)
  - ヘルプメッセージにも反映して。

### #1876
『必要なら次、「list行」の判定範囲（cont行やネストしたサブリストをどこまでブロックとして一緒に>>/<<するか）を、実
際の使い方に合わせて微調整できます。』
- ネストしたサブリストも一緒に>>/<<する仕様がいい。
- 「cont行」とは？同階層の兄弟項目の事であれば、一緒に>>/<<しない今の仕様でいい。

### #1877
- ネストしたサブリストのインデントが連動しないようだ。
```
- abc
- def
  - xyz
```
というlistで、def行で`TAB`押下してもxyz行が変化しない。期待動作はxyz行にも'>>'が効いてインデントが増えてほしい。
- INSERT時に`>>`/`<<`の挙動になっていない。(caretが行頭にないとき)
- cont行も一緒にインデント調整したい。
  - 下記の例でdef行でTAB押下したら"cont-line"も揃えてほしい。
    - まだ実装していないが、list内cont行での`Enter`で同階層の兄弟list itemを発生させたいと思っている
```
- parent
  - abc
  - def
  cont-line
  - ghi
```

### #1878
- (un)ordered listではNORMALでも`Tab`が`>>`の動作になった。OK
- NORMALでもINSERTでも、階層1で`Shift+Tab`とすると階層2(以降)のサブリストのインデントが減ってしまう。
  - 階層1が動かないのは期待動作。その場合、階層2(以降)も動かない(=階層構造が崩れない)のが期待動作。
- markdown onのときはNORMALモードでの`>>`,`<<`でも(un)ordered listでは下位の階層も同時にインデントを増減するようにしたい。

### #1879
- 階層1の動きはOK
- '>>','<<'の動きはOK
- ordered list時のインデント量が親階層の「インデント量＋マーカー幅＋ピリオド(or括弧)1ch＋空白1ch」になっていない。NG
  - 「ピリオド(or括弧)1ch」分が抜けていると思われる。※期待動作は単純なインデント増減ではなくlistの階層増減。

### #1880
- OK。インデント0の階層1のordered listでは`TAB`押下で4ch分インデントするようだが、どういう計算式？
- unordered listのcircle記号の中身を透明度0.25で塗り潰すことは可能？(描画負荷を上げてまではやりたくない)
- WSL側ソースのWindows側へのコピーはこちらの責任でルーチンワーク化してるので不要。

### #1881
- 階層1の`>>`時のインデント計算でshiftwidthをフォールバック参照するのはいいが、バッファローカルのshiftwidthを優先して参照してほしい。先の報告では2に設定してあるのに無視されていた。
- 階層1の`<<`では非list行での`<<`と同様にshiftwidth分のインデントを減じ、ぶら下がっている階層2以降も同じだけインデントを減じてほしい。

### #1882


x
- abc
- def
  - xyz
.
- parent
  - abc
  - def
  cont-line
  - ghi
.
  - 右右右上パレット内の並びで「折り返し」「インデント幅」ボタンをこの順で一番下に移し、markdown on時にマスクをかける
:set formatoptions+=or
:set formatlistpat?
  a.
x
1. abc
   1. xyz
      1. 右上パレット内の並びで「折り返し」「インデント幅」ボタンをこの順で一番下に移し、markdown on時にマスクをかける右
- 右右右右上パレット内の並びで「折り返し」「インデント幅」ボタンをこの順で一番下に移し、markdown on時にマスクをかける


:
- OK。次はGFMの実装に戻って、画像リンクを実装したい。
- 下記形式がGFMの仕様のはず。
```
![代替文字列](https://yahoo.co.jp/foo/bar/sample.png "ツールチップ")
```
- six独自拡張として、ウインドウサイズに収まらない大きさの場合には縦横比を維持したまま収まるサイズに縮小する。
  - スクロールアウトして一部しか見えないのは問題ない。表示範囲の先頭行から表示すれば全体が見えるようにする。横も横スクロール無しで表示できるようにする。
- six独自拡張として、代替文字列の末尾が':数字'だった場合に、それ以前を代替文字列(alt)として数字部分は「表示行数指定」と解釈する。
  - 画像の縦横比を維持したまま、縦サイズが指定行数分になるよう拡大または縮小して表示する。
    - ただし
- six独自拡張として、
- 🖼️🗙
