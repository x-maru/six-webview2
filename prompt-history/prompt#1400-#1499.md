#1400
起動時に1回だけDevToolsのConsoleにエラーが出た。
```
_six.js:4 [six] _six.js build#912 loaded ts=1765725519041
127.0.0.1:49349/dir?cwd=file%3A%2F%2Fwsl.localhost%2FUbuntu%2Fhome%2Fymaru%2Fwork%2FWebView2%2Fsix%2FPOS%2FA%2F:1   Failed to load resource: the server responded with a status of 400 (Bad Request)
```
- IMEが重い様子は変わらずだったが、ずっとprompt.mdのバッファ末尾で試していたのだが、中身をprompt-history/prompt#1172-#1399.mdとして保存し、prompt.mdは一度空にして#1400から書き込み始めたところ、IMEの動作が軽快になった。
- 改めてprompt#1172-#1399.mdを開くと、そこではやはりIMEが重い。

#1401
起動時のログが増えた。
```
_six.js:4 [six] _six.js build#912 loaded ts=1765726334581
127.0.0.1:40004/dir?fs=%5C%5Cwsl%24%5CUbuntu%5Chome%5Cymaru%5Cwork%5CWebView2%5Csix%5CPOS%5CA:1   Failed to load resource: the server responded with a status of 400 (Bad Request)
127.0.0.1:40004/dir?fs=%5C%5Cwsl%24%5CUbuntu%5Chome%5Cymaru%5Cwork%5CWebView2%5Csix%5CPOS%5CA:1   Failed to load resource: the server responded with a status of 400 (Bad Request)
127.0.0.1:40004/dir?cwd=file%3A%2F%2Fwsl.localhost%2FUbuntu%2Fhome%2Fymaru%2Fwork%2FWebView2%2Fsix%2FPOS%2FA%2F:1   Failed to load resource: the server responded with a status of 400 (Bad Request)
```
- prompt#1172-#1399.mdでのIME入力はほぼ変わっていないと思う。引き続き重い。

#1402
- 起動時のログは出なくなった。
- prompt#1172-#1399.mdでのIME入力はほぼ変わっていないと思う。引き続き重い。

#1403
- prompt#1172-#1399.mdでのIME入力はほぼ変わっていないと思う。引き続き重い。
- いま気づいたが、未確定文字列を確定してもcaret表示が付いていかない。IME ONの間は確定後の入力があってもcaretは最初の位置のまま。(入力自体は正しい位置に挿入される)
  - IME OFFにしてキー入力があると正しい位置にcaretが飛ぶ。

#1404
変化なし。ネイティブのcaretは一切見えない。

#1405
- ネイティブcaretは見えるが幅1pixel？の黄色い細い線で表示される。
- prompt#1172-#1399.mdでのIME入力は引き続き重い。
- 新規バッファなどでは重くなる現象は起きないが、自前caretが付いていかない現象は全バッファで発生している。

#1406
- 40行程度のバッファ(prompt.md)でNORMALでもINSERTでもcaretを1文字移動させる度に制御文字が再描画されている
  - スクロールは発生していない。
  - EOFは47行とかだが行42あたりより下に`j`でcaretを移動できなくなった。`G`などで飛ぶことは可能。scrolloff=10。余白＋本文で10行か？
  - 先頭から`j`で辿り着けない行にいる間は制御文字の再描画は発生しなくなった。下から5行ほどの領域では1文字caretが動くたびに画面全体の改行文字が再描画されてチラつく。

#1407
- 制御文字のチラつきは発生しなくなった。OK。
- 新規バッファなどで未変換文字列入力中はnative caretが追従し、確定すると自前caretに置き換わるようになった。
- 完全新規バッファで`ggyG？pG`として実験：
  - ？=200)問題なし
  - ？=300)少しIMEが重くなる。10000行超えでこの程度なら許容するかどうかというレベルだが、たかだか1505行では許容したくない。
  - ？=400)さらに重い。実用上NGレベル
  - ？=500)prompt#1172-#1399.mdと同等に重い。NG
  - ？=1000)10秒待っても反応がなく、即時終了して再起動すると"already running"と言われて起動できない。数分待っても起動できない。

#1408
- ？=500,1000を試したが改善されず。
- `G`で末尾に飛んで試しているが、`gg`で先頭に飛んで試すとIME入力も軽快。
- 以前「IME入力中はその行だけ制御文字表示を消す」という仕様にしたが、確定しても描画されなくなった。小さいバッファでも。NG
  - IME offでINSERTモードで文字入力したとき制御文字(改行)表示が更新されなくなった。小さいバッファでも。NG

#1409
- 「G→IME開始→確定後に、該当行のEOL記号が必ず戻るか」→OK
- 「小さいバッファでIME OFF のINSERT入力でもEOLが追従するか」→OK
- INSERTモードで、改行すると全制御文字が消える。EOFでなくても。小さいバッファでも。NG
  - 新たに改行したり改行を消すと全画面で復活する。
- 「ggyG ?pG の ?=500/1000 で「EOFだけ極端に重い」→改善せず

#1410
- INSERTモードで改行すると全制御文字が →消えなくなった。OK。
  - OKだが、改行する度に(スクロールしてないのに)表示範囲内の全行で制御文字がリドローされている。
- 「ggyG ?pG の ?=500/1000 で「EOFだけ極端に重い」→改善せず
- C:\tmpを新規作成し、その中で新規に展開したsix_v0.9.1フォルダ内でinstall.cmdで生成されたショートカットから起動して.wv2-profileを確認：
  - 直後：
    - ファイル数280, フォルダ数129, 20.1MB
  - 3分間放置した後：
    - ファイル数429, フォルダ数235, 29.5MB
  - `:e`,`SPACE`でpopupを開いてすぐ`Esc`で閉じた直後：
    - ファイル数434, フォルダ数236, 31.0MB
  - 10分間放置した後：
    - ファイル数674, フォルダ数360, 43.4MB
  - 放置してるのに増え続けるのは何故？
    - C:\Users配下のフォルダだとさらに10倍くらいまで増えていく。

#1411
.wv2-profileの増大とIMEが重くなる現象に相関はなさそうなので、.wv2-profileまわりの修正は一旦不要。
EOF付近の挙動変更もまだ導入せず他の解決策を探りたい。
- ファイルサイズは関係なさそう。バッファ先頭からIME入力位置までの改行の総数による？
  - 新規でも既存でも、任意のバッファで`ggdG`で空にする → `a␊`と入力して`yy`で行コピー → `1500pG` → IMEが重い。
    - listをfalseにすると若干高速化した気がするがまだまだ重い。
  - 新規でも既存でも、任意のバッファで`ggdG`で空にする → `a`と入力して`yl`で1文字コピー → `99999po` → IME入力は軽快。

#1412
明らかに効果はあったが、もっと軽くしたい。

#1413
かなり改善できた。「挙動変更なし」で試せそうな改善案はまだありそう？

#1414
改善はしたが、まだ重い。1500pGだと「遅い」レベルだが3000pGだと無限ループに嵌ったと判断して強制終了してしまうレベル。
次の改善策も取り入れたい。

#1415
改善したかもしれないが誤差レベル。。次の改善案も試したい。

#1416
体感的には変化なし。
手順全体に平均して遅い、という感じではない。
「かなかんじへんかん」とローマ字変換で入力して最後に変換キー(`SPACE`)を押すと`かなかｎ`で止まったりする。数秒後に「仮名漢字変換」と一瞬で表示される。
`かなかｎ`などで表示が止まっても気にせずたとえば「かんきょうせってい」などと入力を続けると、IMEには随時渡っていて、数秒後に一気に「仮名漢字変換環境設定」に置換されたりする。

#1417
IME入力中の軽量さはOKだし入力中にスクロールしないのも問題ないと思われるが、入力を開始した途端に画面最下行が入力位置になってしまう。
直前の「表示画面内での相対位置」を維持してIME入力に移行することはできないか？

#1418
動作も軽いしcaretの位置も維持されて、いい感じ。
行数が多いと縦スクロールバーの大部分が空白(？)でバーはかなり小さく表示されていて、textareaを縮小しているので当たり前だが急にバーが大きくなるのが目立つ。
目立たなくさせる工夫は何かないかな。textarea縮小中だけバーの色を透明にするとか。(バーが見えなくなる方が違和感が小さい)

#1419
IME入力中だけthumbが透明になった。OK。
次はgrepの実装に戻ろう。
検索実行についてはひと通り動作確認が済んだところだったはず。次は検索結果バッファの修正。
- 「検索時 A/a」欄と値との間のTABが1つ多い
- 「ヒット総数」欄と値との間のTABが1つ多い
- 下記がヒットしないのはなぜ？
  - 準備)`rm -rf POS && mkdir -p POS/A/B/C/D && cd POS && echo a >A/AA.md && echo ab >A/B/BB.md && echo abc >A/B/C/CC.md && echo abcdd >A/B/C/D/DD.md`
  - `:grep -r /a/i -basedir POS *.md`

#1420
- 「外部grepの basedir モードだけ app配置ディレクトリ基点」←そんな仕様はNG。あくまでもカレントバッファが基準。app配置ディレクトリを基点にするのはパス未設定のバッファのときだけ。

#1421
- 結局、なぜヒットしないのかわからない。
- CMD欄でbasedirをフルパスに変換するのはやめて。入力のままでいい。

#1422
OK。ひとつ前のIME入力高速化の際の弊害だと思うが、不具合修正を。
- `o`や`O`でINSERTに移行したとき改行表示が更新されない(行挿入によるズレが反映されない)。
  - 反映されるときもある (EOF付近でスクロールが発生するとき？)

#1423
OK。次もgrepとは関係ないが不具合修正を。
- VISUALの行選択で下から上に選択を伸ばしたときハイライトは上に伸びているのに実際は逆に下方向に縮んでしまうことがある。
  - 条件ははっきりしないが、EOF行の1つ上の行または2つ上の行から選択を開始したときだけっぽい？

#1424
NG。悪化した。「下方向に縮む」は誤報告だった。「ハイライトは1つ上の行に伸びているのにcaretとアクティブ行背景が取り残される」という症状だった。
- 取り残されてももう一度`k`押下でハイライトは伸びず「選択範囲内をcaret移動」して追い付き、その後はcaretで選択範囲を伸ばせていたのだが、それが2行上まで伸びるとcaretが選択範囲の最下行に飛んでしまうようになった。NG

#1425
OK。VISUAL行選択の不具合は解消した。
- IMEが重い件の対策で未確定文字列入力中は自前caretを消してnative caretを表示するようになったが、自前caretに戻してくれる？

#1426
OK。
- 行数の大きいバッファでIME入力時にtextareaを縮める対策を入れたが、その際に自前caretがあった位置とIME入力の位置がずれる不具合が発生して修正したのに再発している。NG

#1427
まだ発生するケースがある。EOF下の余白行が3行以上見えているとき、caretの2行下にIME入力が表示されるっぽい。

#1428
- 余白行が2行見えているときは自前caretのあった行が1行下に下がるようにスクロールされてその位置に未確定文字列が表示される。ただしアクティブ行背景は1行上に置き去りにされている。
- 余白行が3行見えているときは自前caretのあった行が2行下に下がるようにスクロールされてその位置に未確定文字列が表示される。ただしアクティブ行背景は2行上に置き去りにされている。
- 余白行が4行見えているときは自前caretのあった行が3行下に下がるようにスクロールされてその位置に未確定文字列が表示される。ただしアクティブ行背景は3行上に置き去りにされている。
- 余白行が5行見えているときは自前caretのあった行が4行下に下がるようにスクロールされてその位置に未確定文字列が表示される。ただしアクティブ行背景は4行上に置き去りにされている。

#1429
ズレは解消した。OK。
次は:grepに戻って、grepダイアログの拡張を。
- 検索対象欄の値としてボタンを3つ並べる。[このバッファ] [ディレクトリ+ファイル指定] [ファイル指定]
- 検索対象欄の下にパス入力欄を追加する。左端を検索対象欄のボタン群の左端に揃える。
  - 検索対象が[このバッファ]のときは網掛け(disable)
  - 検索対象欄が[ディレクトリ+ファイル指定]のときはパス入力欄は2つに分かれ、それぞれ-basedirとfileglobに対応する
  - 検索対象欄が[ファイル指定]のときはパス入力欄は1つだけ。

#1430
- [ディレクトリ+ファイル指定]のときのパス入力欄2つは左右に並べよう。8:2くらいでいい。
- パス入力欄に「チェックボックス+"サブディレクトリまで検索"」の行を追加。[ディレクトリ+ファイル指定]以外ではdisable
  - その右にmaxdepthの入力欄を。選択肢「1～9」のpopupで。

#1431
- [ディレクトリ+ファイル指定]のときのfileglob入力欄を4remほど広げたい。その分ダイアログを大きくする。
- グレーアウト時の色をもう少し暗いグレー寄りに。
- TABキーでフォーカス移動できるように。
- [このバッファ]を選んだときにパス入力欄はグレーアウトするのみでレイアウト変更はしない。
- [ファイル指定]時の入力欄の右にもmarginを。

#1432
- disableになっている欄はTABフォーカス移動時にスキップするように。
- maxdepthのポップアップが開かなくなった。→ポップアップは廃止して手入力専用に変更しよう。
- 検索対象のボタンの上に`F1`,`F2`,`F3`の表示を乗せて`F1`,`F2`,`F3`でダイレクト選択できるようにする。:e popupのパンくずインタフェースと同じ感じで。
- 「サブディレクトリまで検索」のチェックボックスは廃止して[する][しない]のボタンにする。パレット内ボタンのOFF/ONと同じ色で。
- 検索対象の選択されたボタンの色もパレット内ボタンのOFF/ONと同じ色にする。
- 検索対象欄と「サブディレクトリまで検索」の欄にはフォーカスが当たらないようにする。
- ダイアログを開いた時は検索語入力欄にフォーカスが当たるようにする。
- 質問):grepの/REGEXP/の代わりに定数を指定することは可能だっけ？

#1433
- `F1`,`F2`,`F3`で検索対象が切り替わるが同時に本文側のタブまで切り替わってしまう。NG
- `かな`キーを押下すると本文側で反応してしまう。NG
- 検索対象ボタンの上下マージンをそれぞれ1.5倍にして。
- ダイアログを開いたときに検索語入力欄にフォーカスが当たっていない。NG
- 検索対象の選択に合わせて、必要な入力欄が埋まっていないときは実行ボタンをdisableして`Enter`も効かないようにしたい。
- disable時の色をもっと暗い色に。入力欄内の文字もdisable時は暗くする。
- 質問):grepの/REGEXP/の代わりに定数を指定することは可能？("*?"などをそのまま文字として検索する指定)

#1434
- `かな`キーを押下すると本文側で反応してしまう。NG
- ダイアログを開いたときに検索語入力欄にフォーカスが当たっていない。NG
- 「検索対象ボタンの上下マージンをそれぞれ1.5倍にして」←間違えた。ボタンの外側のmarginは広げなくてよかった。ボタン高を大きくして、paddingを広げたかった(今は`F1`などがボタンの文字に重なっているので。重ならないように)。
- disable時の色をもっと暗い色に。
- 「入力全体をリテラル扱い” を簡単にしたいなら、将来的に -F（fixed string）」←実装したい。まずはCLIで使えるように。

#1435
- `かな`キーを押下すると本文側で反応してしまう。NG
- 検索対象ボタンの上マージンを半分にして。
- disable時の色をもっと暗い色に。
- -Fの検索動作はOK。検索結果バッファのCMD欄の下に「検索語		正規表現」「検索語		リテラル」のどちらかを。
- 検索結果バッファのCMD欄にも-Fを反映させて。
- grepダイアログで検索語欄の右に[正規表現][リテラル]のボタンを2つ並べて(その分検索語欄を短くする)。

#1436
- `かな`キーの制御はOK
- 「検索対象ボタンの上マージンを半分にして。」←また間違えた。上マージンじゃなくて「ボタン内の上paddingを半分に」だった。
- disable時の色をもっと暗い色に。disable時のmaxdepthの入力欄の文字色で統一して。ラベルやボタンにも。
- ダイアログからのリテラル検索や検索結果バッファの表示はOK
- 検索語入力欄にフォーカスがあるときにTABを押下すると[正規表現][リテラル]の2ボタンを囲う領域にフォーカスが移って`Space`や`Enter`でトグルとして切り替わるようにしたい。
  - 2ボタンを囲う領域にフォーカスがあるときだけ領域がわかるように描画して。フォーカスが無いときは線色をdisable色に。

#1437
- 正規表現/リテラルの枠にフォーカスがあるときにTAB押下でパス入力欄にフォーカスが移動するが、disableのときはskipしてその次までフォーカスを移動してほしい。
- 「サブディレクトリまで検索[する][しない]」を領域で囲って`Space`/`Enter`でトグル切り替えできるようにしたい。
- 「maxdepth[  ]」を領域で囲ってフォーカスが当たるようにしたい。
- 「検索時 A/a」ボタンにフォーカスがあるときにボタンの境界色を変えてわかるようにしてほしい。
- 「検索時 A/a」のポップアップを表示しているときに`Esc`を押下するとgrepダイアログが閉じてしまう。ポップアップを閉じるだけの動作にしたい。
- フォーカス移動でキャンセルボタンの次は検索語入力欄に移動してほしい(ループ)。

#1438
TAB移動はいい感じ。
- maxdepthへのフォーカスは領域にではなく領域の中の入力欄にダイレクトにフォーカスが当たるようにしたい。
- 「検索時 A/a」のポップアップ内で`↑`,`↓`でカーソル移動、`Space`,`Enter`で確定にしたい。
- ラベル「検索語(正規表現)」を入力欄の左に配置して。その分、入力欄を短く。
- 検索対象のボタンの高さを1.0rem(上0.5rem下0.5rem)減らして。
- maxdepth領域を左に寄せて「サブディレクトリ検索領域」との間を1remに。

#1439
- 検索対象のボタン内のpaddingを上25%増、下25%減にして。
- maxdepth入力欄にフォーカスが来るたびに文字列を全選択状態にしたい。
- 検索語入力欄内の右端に[履歴]ボタンを置いて、クリックすると上に検索語履歴の一覧を出したい。
  - `↑`,`↓`で選んで`Space`, `Enter`で確定すると入力欄に上書きする。
- 検索語と同じように検索パスも独立して履歴を持つようにする。
  - 最大数はwindow.SIX_OPTIONS.GREP_BASEDIR_IN_SESSIONで、全バッファ共通でセッション情報に含める。
  - 入力欄で履歴を辿れるようにする。`↑`,`↓`で選んで`Space`, `Enter`で確定すると入力欄に上書きする。

#1440
- 検索対象のボタン内のpaddingを上0.25rem増、下0.25rem増にして。
- 検索対象のボタン外の上marginを0.25rem増にして。
- 検索語履歴ポップアップで`Del`でカーソル下の1つを履歴から削除できるようにしたい。
- 検索対象の選択を憶えておき、次回grepダイアログを開くときに再現したい。
  - 全バッファで共通に1つの記憶でよい
  - レジューム情報に含め、次回に引き継ぐ
- パス履歴も入力欄右端に検索語と同様の履歴ボタンを設けてポップアップを出したい。
  - `↑`,`↓`,`Space`, `Enter`, `Del`の操作系も共通に。

#1441
- 一旦Grepから離れるが、数時間放置したあと`F19`,`かな`,`英数`などが効かなくなった。`F10`なども効かないようだ。
  - この状況を検知して再起動を促すダイアログを実装したはずだが、出なかった。
  - 即時終了ボタンをクリックして終了、再起動することで復帰できたので、警告ダイアログが出るかどうかだけの問題と思われる。
- 即時終了前のDevToolsコンソール：
```
_six.js:4860   GET http://127.0.0.1:34499/dir?fs=%5C%5Cwsl%24%5CUbuntu%5Chome%5Cymaru%5Cwork%5CWebView2%5Csix net::ERR_CONNECTION_REFUSED
ac @ _six.js:4860
_fetchJSONWithTimeout @ _six.js:4840
tryFs @ _six.js:16730
_listDirEntries @ _six.js:16744
_listDirEntriesWithQuickRetry @ _six.js:16546
_statFileMeta @ _six.js:3433
(anonymous) @ _six.js:4224
_maybeCheckExternalChangeOnActivate @ _six.js:4280
(anonymous) @ _six.js:10494
_six.js:4860   GET http://127.0.0.1:34499/dir?cwd=file%3A%2F%2Fwsl.localhost%2FUbuntu%2Fhome%2Fymaru%2Fwork%2FWebView2%2Fsix%2F net::ERR_CONNECTION_REFUSED
ac @ _six.js:4860
_fetchJSONWithTimeout @ _six.js:4840
_listDirEntries @ _six.js:16757
await in _listDirEntries
_listDirEntriesWithQuickRetry @ _six.js:16546
_statFileMeta @ _six.js:3433
(anonymous) @ _six.js:4224
_maybeCheckExternalChangeOnActivate @ _six.js:4280
(anonymous) @ _six.js:10494
_six.js:4860   GET http://127.0.0.1:34499/dir?fs=%5C%5Cwsl%24%5CUbuntu%5Chome%5Cymaru%5Cwork%5CWebView2%5Csix net::ERR_CONNECTION_REFUSED
ac @ _six.js:4860
_fetchJSONWithTimeout @ _six.js:4840
_listDirEntries @ _six.js:16778
await in _listDirEntries
_listDirEntriesWithQuickRetry @ _six.js:16546
_statFileMeta @ _six.js:3433
(anonymous) @ _six.js:4224
_maybeCheckExternalChangeOnActivate @ _six.js:4280
(anonymous) @ _six.js:10494
_six.js:4860   GET http://127.0.0.1:34499/dir?fs=%5C%5Cwsl%24%5CUbuntu%5Chome%5Cymaru%5Cwork%5CWebView2%5Csix net::ERR_CONNECTION_REFUSED
ac @ _six.js:4860
_fetchJSONWithTimeout @ _six.js:4840
tryFs @ _six.js:16730
_listDirEntries @ _six.js:16744
_listDirEntriesWithQuickRetry @ _six.js:16546
_statFileMeta @ _six.js:3433
(anonymous) @ _six.js:4235
await in (anonymous)
_maybeCheckExternalChangeOnActivate @ _six.js:4280
(anonymous) @ _six.js:10494
_six.js:4860   GET http://127.0.0.1:34499/dir?cwd=file%3A%2F%2Fwsl.localhost%2FUbuntu%2Fhome%2Fymaru%2Fwork%2FWebView2%2Fsix%2F net::ERR_CONNECTION_REFUSED
ac @ _six.js:4860
_fetchJSONWithTimeout @ _six.js:4840
_listDirEntries @ _six.js:16757
await in _listDirEntries
_listDirEntriesWithQuickRetry @ _six.js:16546
_statFileMeta @ _six.js:3433
(anonymous) @ _six.js:4235
await in (anonymous)
_maybeCheckExternalChangeOnActivate @ _six.js:4280
(anonymous) @ _six.js:10494
_six.js:4860   GET http://127.0.0.1:34499/dir?fs=%5C%5Cwsl%24%5CUbuntu%5Chome%5Cymaru%5Cwork%5CWebView2%5Csix net::ERR_CONNECTION_REFUSED
ac @ _six.js:4860
_fetchJSONWithTimeout @ _six.js:4840
_listDirEntries @ _six.js:16778
await in _listDirEntries
_listDirEntriesWithQuickRetry @ _six.js:16546
_statFileMeta @ _six.js:3433
(anonymous) @ _six.js:4235
await in (anonymous)
_maybeCheckExternalChangeOnActivate @ _six.js:4280
(anonymous) @ _six.js:10494
```

#1442
- 再起動が必要な状況が発生したら改めて報告する。一旦OK。
- 行数の大きいバッファでIME入力時にtextareaを縮小する処理を入れたが、EOF行から上に50行以上離れた位置だと勝手にスクロールしてしまう。
  - `G49k` → OK
  - `G50k` → IME入力時に1行下にスクロール(アクティブ行背景も付いてくるので整合性は取れているが)
  - `G51k` → IME入力時に2行下にスクロール
  - `G52k` → IME入力時に3行下にスクロール
  - 以後、EOF行から離れるほどスクロール量が増える(最大は画面最下行までだと思われる)

#1443
- `G50k`、`G100k`などでも強制スクロールされることはなくなった。OK。
- grepダイアログの実装に戻る。
- パス入力欄がenableになっても履歴ボタンの色がdisable色のままになっている。(動作はOK)
- 履歴ボタンのラベルを[F4履歴]に改め、`F4`で履歴ポップアップが開くようにしたい。
  - enableな履歴ボタンにフォーカスがあるときはその履歴ボタンに対して`F4`を効かせる(ポップアップを出す)
  - どの履歴ボタンにもフォーカスがないときは、TABフォーカス移動順の若い方を優先してポップアップを出す。
- ラベル「検索語(正規表現)」を「検索語」に改めよう。

#1444
- 「履歴ボタン」としか書かなかったが、検索語入力欄内のボタンも含めて3つある。
- 「履歴ボタンにフォーカスがあるときは」と書いたが、不正確だった。正しくは「履歴ボタンが置かれている入力欄にフォーカスがあるときは」だった。
  - 履歴ボタンそのものはクリックしてもフォーカスは当てなくてよい。

#1445
いいね。次はfileglobも履歴を持つようにしたい。
- 履歴の最大数はwindow.SIX_OPTION.GREP_FILEGLOB_IN_SESSIONを参照する。
- 全バッファ共通でセッション情報に含め、起動時に再現する。
- grepダイアログの入力欄にも[F4履歴]を配置し、他の履歴ボタンと同じ扱いにする。

#1446
- fileglob履歴の動作としてはOKっぽい。続いてgrepダイアログUIの調整を。
- fileglob入力欄が狭いので、例外的に履歴ボタンを欄外(入力欄の下、右寄せ)に移動しよう。
  - フォーカスやenable/disableに関して「fileglob入力欄の中に配置された履歴ボタン」としての振る舞いは継続する。
- 各履歴ボタンのラベルを"[F4履歴]"から"履歴 F4"に変更を。
- 履歴ポップアップの幅は、対応する各入力欄の幅に揃えたい。幅固定。
- fileglobポップアップもボタンのすぐ上ではなくfileglob入力欄の上に出現させる。
- grepダイアログを開いたときに各入力欄に最新の履歴(=最後に入力された内容)が最初から入力された状態にしたい。
  - 履歴がない場合は空欄のままにする。(ポップアップで"（履歴なし）"と表示されるのはそのままでよい)
- 「サブディレクトリまで検索」がfalseのとき"maxdepth"のラベルもdisable色にしたい。

#1447
- fileglob入力欄がダイアログからはみ出している。入力欄の大きさは変えずにダイアログを広げて。
  - その分、検索語入力欄も大きくする。
- fileglob入力欄が上にズレている。topのY座標をパス入力欄と揃えて。
- 履歴のあるなしに関わらず、入力欄にマウスをホバーしたらツールチップを表示して。文言は空の入力欄にプレースホルダーとして表示している文言を。
  - 検索語入力欄はツールチップ不要
- CLIで`-F`が付いたときだけ検索語は「/word/」の代わりに「"word"」や「'word'」も受け付けるようにしたい。
  - "abc'/'def"も'abc"/"def'もリテラル検索語の記述として正しい。
  - "abc'などはNG
- PATH(パスとfileglobを繋げた入力文字列)はbasedirとは別に独立して履歴を持つようにする。
  - 最大数はwindow.SIX_OPTIONS.GREP_PATH_GLOB_IN_SESSIONとし、全バッファ共通でセッション情報に含める。

#1448
- fileglob入力欄がダイアログからはみ出している。
  - おそらく#1447でダイアログを広げて、それに合わせてfileglob入力欄まで広げてしまって結局はみ出しているのではないか？
- 履歴ポップアップはいずれも古いものから上から順に並べてほしい。カーソル初期位置は一番下に。

#1449
- fileglob入力欄がまだはみ出している(添付スクショ)。
- 履歴ポップアップでカーソルが一番下の最新項目を指しているのはよいが、履歴数が多くスクロールバーが発生する状況だとスクロールアウトしてしまっている。
  - 初期状態でカーソルが見えるスクロール位置にしてほしい。

#1450
- fileglob入力欄がまだはみ出している(添付スクショ)。
- 履歴ポップアップにスクロールバーが出て一番下にカーソルがあるとき、ひとつ上に移動するだけでスクロールしている。:e popupのように中央あたりまではスクロール無しでカーソル移動したい。

#1451
GUIはOK。
- 検索語「。$」、正規表現、検索対象「ディレクトリ+ファイル指定」、basedir「C:/Users/ymaru/Webview2/six」、fileglob「*.md」、検索時A/a「混在時区別」で実行したところ、
```
CMD	:grep /。$/s -basedir C:/Users/ymaru/WebView2/six/ *.md
```
でヒット総数0件だった。オプション's'って何？

#1452
- 検索結果バッファには「grep: no files match *.md」と出ている。
- CLIで`:grep /。$/ -basedir C:/Users/ymaru/WebView2/six/ *.md`を実行したところヒット総数167件だった。
```
📄C:/Users/ymaru/WebView2/six/README.md  35件
📄C:/Users/ymaru/WebView2/six/TODO-bak.md  14件
📄C:/Users/ymaru/WebView2/six/windows.md  118件
```
- 「0件時に理由が分かるメッセージを出すようにコード側も改善」←やってみて。

#1453
- パッチ適用後に再度試したが、CLIでsオプションを除いて実行したのと同じ結果になった(ヒット総数167件)。
  - ファイル一覧を得るAPIまわりの不調か何かで一時的に出たエラーだったのだろうか。
- 検索結果バッファでCMDに内部フラグsを反映しないで。そのままCLIで実行すれば同じ結果になるのが期待動作。

#1454
OK。
- grepダイアログでの履歴の扱いをCMDバーでの履歴と同じにしたい。3つとも。
  - 履歴から選んだものを最新に持ってくる
    - 履歴から選ばずに(ポップアップすら出さずに)入力した場合でも完全一致するものが履歴にあったら最新に持ってくる
  - なので、履歴内はユニークで重複は発生しないはず

#1455
履歴はOK。
- grepダイアログから実行して検索結果バッファも生成されているのにダイアログが消えない。NG
- 検索結果バッファが生成された直後、直前にいたバッファの改行文字が残っている。NG
  - 一度他バッファに移って戻ってくると正しい改行表示になっている。

#1456
OK。
- basedir入力欄の左に"-basedir"ってラベルで表示して。
- basedir入力欄のプレースホルダー/ツールチップの文言を「フルパス、相対パスによるディレクトリ指定」に変えて。

#1457
- 「このバッファ」にしたとき「-basedir」のラベルをdisable色にして。

#1458
OK。次はCMDに機能追加したい。
- `:`で入力するコマンド全文の履歴を全バッファ共通でセッション情報に格納・再現したい。
  - 最大数はwindow.SIX_OPTIONS.CMD_HISTORY_IN_SESSIONを参照し、未定義なら無限
  - 今はsix起動中のコマンド履歴って無限に格納してたっけ？
    - セッション情報に合わせてCMD_HISTORY_IN_SESSIONをを上限としよう。未定義なら無限

#1459
OK。
- CMDバーの右端にgrepダイアログの入力欄と同じように"履歴 F4"ボタンを表示したい。
  - ボタンクリックもしくは`F4`で履歴ポップアップを出す。
  - 表示幅はCMDバーと揃えて、CMDバーがcaretより上に出現したときはポップアップはその上に、caretより下に出現したときはポップアップはその下に表示する。
  - grepダイアログの履歴ポップアップと同様に、下から最新で`↑`,`↓`,`Del`,`Space`,`Enter`が効くように。
  - `↑`,`↓`でカーソルを動かしたら、カーソル位置の内容をリアルタイムでCMD欄に反映させる
    - grepダイアログの履歴ポップアップも同じ挙動にしたい
    - ポップアップが開いている状態でCMD欄を編集したらポップアップを閉じる

#1460
- CMDバーに履歴ボタンが表示されていない(`F4`も効かない)
- grepダイアログの履歴で、ポップアップのカーソル位置の内容が入力欄に反映するが、そこで編集しようとするとポップアップを開く前の内容に対する編集になってしまう。NG

#1461
- CMDバーに"履歴 F4"ボタンが表示されて、クリックでポップアップが開くようになったが、`F4`はまったく反応しない。
  - NORMALモードから`:`でCMDバーを表示した直後でも効かない。
- 実行したコマンドが履歴内に存在していても重複して格納されるようになった。NG
  - 以前は履歴中の一致するものを最新に持ってくる挙動だったはずだが。

#1462
- CMDバーにフォーカスがあっても`F4`が無反応。ちなみに`F9`ならヘルプダイアログが開く。

#1463
CMDバーで`F4`でポップアップが開くようになった。OK。
次は小さめの不具合修正を。
- INSERTモードで`TAB`押下時に制御文字の表示が更新されない(入力したTABも見えないし改行位置も変更されない)
  - インデント幅が数字でも`TAB`でも症状同じ。

#1464
OK。次。
- コマンド入力で":slkjsdl"とか無意味な入力まで履歴に残ってしまう。
  - 存在するコマンドだがパスが見つからないなどの実行時エラーはそのまま履歴に格納するが、コマンドとして不正なものは格納しない仕様にしたい。

#1465
OK。
- バッファ毎のcaret位置ってセッション情報に含めてたっけ？ 即時終了して起動するとcaretが再現するバッファと常に行1列1になるバッファがあるようだけど？

#1466
OK。次。
- 行数の多いファイルでIME低速化を防ぐためにtextareaを縮めているが、縮めるのを縦方向(行方向)だけにすると影響する？(横スクロールバーが消えると目につきやすい)

#1467
OK、今のままでいい。次。
- URLのクリッカブル判定で日本語の句読点や「」なども区切りとして認識してほしい。
http://host/ひらがなページ
というURLが実在したとして、
http://host/ひらがなページにジャンプ、その後ボタンをクリックして…
などと文が続いている場合に、今は1行丸ごとURLと判定してクリッカブルになっている。
せめて
http://host/ひらがなページにジャンプ
までで切れた方がマシだ、という話。
句読点や括弧までURLに含めることは可能かもしれないが、現実的には少ないと思われるので。

#1468
除外文字種を増やすなど拘ってもどうせ完璧にはならないので、これでいい。OK。次。
- オーバーレイパレット内のボタンのポップアップが表示されているとき、`↑`,`↓`でカーソル移動、`Space`,`Enter`で確定、`Esc`でポップアップを閉じる動作にしたい。

#1469
NG。キーが本文textareaに渡って消費されている。

#1470
OK。次。
- タブバーの右端に表示しているposInfoで','を無くす。「行YYY, 列XX/WW」→「行YYY 列XX/WW」

#1471
OK。次はmarkdownモードの実装に取り掛かる。規模が大きいので少しずつ。
- まずはモード変数を。markdownがtrueならmarkdownモード。
  - `:set markdown?`, `:set markdown`, `:set nomarkdown`, `:set markdown!`
  - バッファ毎に値を持ち、セッション情報に含める。
  - 新規バッファ生成時の初期値は常にfalse
- 右上オーバーレイパレットにmarkdownボタンを追加する。
  - 「markdown␊OFF ON」
  - 位置は「検索時 A/a」ボタンの下

#1472
OK。まずは「一部の行だけフォントサイズ・行高を変える」実装をしていこう。
- `^# `, `^## `, `^### `, `^#### `, `^##### ` に合致する行全体に対し、フォントサイズを変更し、それに合わせて行高も変える。
  - `^### `は通常サイズ。`^## `で1段階大きく、`^# `で2段階大きく。`^#### `で1段階小さく、`^##### `で2段階小さく。
  - ガターもあわせてその行だけ行高を変える。行番号の文字自体は変えない。
  - GFM(GitHub Flavored Markdown)と比べて相違があれば指摘して。

#1473
- フォント色が白っぽくなって見にくい。window.THEME.editorTextColorにしたい。
- 細かな調整は必要だけど、#textLayerで問題なさそうだ。ズレやすいオーバーレイ要素がある以外にデメリットはある？
- 極力GFMに準拠したい。見出しに関しては完全に従おう。
  - 「見出しの前に最大3スペースを許容」
  - 「見出しレベル6」
  - 「複数スペースでも見出し扱い」
  - `# `のフォントサイズは`### `に対して何倍で表示するか、などはGFMで規定されている？
- (質問)textareaで、行内で文字色を変えることは可能？

#1474
- 見出し1を見出し3の1.5倍、見出し2を1.2倍のフォントサイズにして、行高も1.5倍/1.2倍にしたい。
  - ガター行高、本文行高も反映できる？
- 見出し4,5,6のフォントサイズは今のままで、行高は行高3と同じでいい。
- (質問)textareaで、行単位で文字色やフォントファミリー、bold等を変えることは可能？

#1475
- 行背景のグラデーションも行高を合わせてほしい。
  - 1行スクロールする度に全行のグラデーションを描き直す必要があると思うが、実用的な速度になるか試してみたい。

  - caretの高さもあわせたい。
  - caretの1文字分の移動量もフォントサイズに合わせたい。

#1476
- 今のところ重くない。ガターの行高も合わせてみて。

#1477
- 速度的に問題なさそうだ。
- IME ONで未確定文字が発生している間もmarkdown処理を効かせたい。

#1478
- IME入力中の他行の描画で左右にぶれるのはまだしも、見出し1などより下の行で行単位の二重表示になってしまうのはなんとかしたい。

#1479
- チラつき皆無になっていい感じ。未確定文字列の下線の色などはあとで調整する。
- 行数の多いバッファでIME入力中の行番号が本来の番号からかけ離れる。
  - 正しい行番号を表示するのが難しいなら、描画しない方がまだいい。

#1480
「textarea を部分テキストに差し替える最適化」に入るときに差し替え元の行番号を取得しておけば、そこからのインクリメントで差し替え中も正しい行番号に出来ない？

#1481
OK。行番号のズレは解消した。
- 見出し2～6のフォントを少し太くしてくれる？見出し1のフォントはさらに太く。
- INSERTモード中も文字色をeditorTextColorにしてほしい。

#1482
- GFM的には`^#$`は見出し1になるようだが、sixでは`#`の後ろに空白が入るまでは通常文字にしよう。
- NORMALでもINSERTでもアクティブ行の文字色が薄く見えるが、レイヤの順によるもの？editorTextColorそのものになるようにしたい。
- 次はcaretの高さと横への移動量もフォントサイズに合わせたい。

#1483
- 文字の色はeditorTextColorそのもので見えるようになったが、アクティブ行背景のグラデーションがまったく見えなくなってしまった。NG

#1484
OK。次は改行記号の描画位置計算で見出しフォントサイズを反映するようにしたい。

#1485
- 位置計算は期待通りになった。OK
- スクロールが発生したとき、list表示が付いてこない(再描画されない)。NG

#1486
OK。
- 見出し2～見出し6の、ASCII文字は良いが日本語部分だけもう少し太くしたい。
- `Alt+j`,`Alt+k`による連続スクロール時に、caret行の1つ上に黒塗りの行がチラつきながら描画されるのは何だろう？

#1487
- 「見出し2～6の日本語部分を太く」→OK。いい感じ。
- スムーズスクロール中の黒塗り行のチラつきは改善されず。

#1488
黒塗り行は現れなくなった。OK。
次は仕様追加を。
- 変数md_drafteditを追加。値はtrue/false
  - `:set md_draftedit?`, `:set md_draftedit!`, `:set nomd_draftedit`, `:set md_draftedit`
  - 対応するボタンを右上オーバーレイパレットに追加する。
    - ラベルは「markdown編集␊draft WYSIWYG」
      - md_drafteditがtrueのときが"draft", falseのときが"WYSIWYG"
    - 位置はmarkdownボタンの下
  - draftは、現在の"markdown = true"
  - WYSIWYGは、マークダウン記号を隠して見せる。"# 見出し1" → "見出し1"

#1489
- markdown編集ボタンの値"WYSIWYG"を"清書"に変更する
- 清書モードのとき、改行記号の位置計算が"### "などの分を加算してしまっている。NG
- draftモード時も、caretがある行以外は清書モードと同じ表示にしたい。

#1490
- 清書モード時、隠れた"### "などをcaret移動でスキップするようにしたい。
  - `0`で`見出し1`の行頭に飛んでから最初の`l`ですぐ"見"の後ろにcaretが移動するように。
- md_drafteditを切り替えたときcaret行の改行記号を(位置を再計算して)再描画してほしい。

#1491
OKだが、draftモード時にcaretが見出し行に来たとき・見出し行から離れたときも改行の再描画が必要。

#1492
OK。次は水平線を実装しよう。下記でGFM完全準拠のつもりだが、不足・誤りがあれば指摘を。
- `---`, `***`, `___`はいずれも同じ。3文字以上。
- 4文字以上あってもよい
- `- - -`のように空白を挟んでもよい
- 独立行での記述のみ。`^---$`など
- 清書は標準の行高でウインドウ左右に2remずつ空けてやや太い直線を行の中央に描画する。

#1493
- markdown清書モードの文字色を専用色にしたい。基本的にモノクロなので、線色も兼ねる。window.THEME.md_FGColor を使って。未定義時はyellow。
  - 濃淡をつける場合は、この色をベースにopacityで調整
- markdown清書モードの背景グラデを専用色にしたい。window.THEME.md_BGGradientStart/End を使って。未定義時はyellow。
- 水平線の色をmd_FGColorにして。
- 検証したいので、setextも実装してみて。
- setextのルール：
  - 「2つ以上の`-`」または「2つ以上の`=`」、3つ以上あってもOK(同じ)
  - 直前の行が空行ではない
  - 「2つ以上の`=`」なら直前の行がH1(見出し1)になり、「2つ以上の`-`」なら直前の行がH2(見出し2)になる。
    - H1/H2の下に下線を引く。H1/H2の区別なく一番細い線 (`---`の水平線より細い線)
- setextを無効化できるようにしたい。
  - `:set setext?`, `:set setext!`, `:set setext`, `:set nosetext`
  - バッファごとに値を持ち、セッション情報に含める。
  - 新規バッファ生成時はnosettext。
  - nosettext時は、前行が空行でなくても`---`は水平線になる。settext時はならない。
- markdownモードのIME入力でtextareaを縮める処理が働いた際に水平スクロールバーの領域が黒塗りされるようになった。NG
- markdown on/offを切り替えた際には即リドローしてほしい。今はタブを切り替えたり編集したタイミングなどでリドローされる。

### #1494
- md_FGColor/md_BGGradientStart,End適用 OK
- setext変数処理 OK
- setextのH1/H2があると、それより下の行でアクティブ行背景の描画が下にズレて下半分ほどが黒帯になる。NG
  - H1の方がH2よりズレが大きい。
- setext/nosetextによって水平線(`---`)の扱いが正しく切り替わっている。OK
- 水平スクロールバーの黒塗りは解消された OK
- markdown on/off切り替えが即、反映されるようになった。OK
- markdown時の日本語フォントを非markdown時と同じにしてほしい。
- 水平線、setextの左右の空きをそれぞれ2remから0.5remに変更したい。
- 清書モードで水平線、setextの行にcaretを持って行ったときは行内は行頭以外に移動できないようにしたい。

### #1495
- setextより下でのアクティブ行背景の描画ズレは解消した。OK
- 日本語フォントが非markdown時と同じになった。OK
- 水平線の左右、caret移動範囲の制限 OK
- markdownモードでのスムーズスクロール時にアクティブ行背景がチラつく。
  - 非markdownでは行背景(オーバーレイ)は固定でtextareaだけスクロールさせることでチラつきを回避しているが、markdownでは行背景と本文のレイヤが同じになっているのでは？
    - markdownでの行背景と本文のレイヤを分けてスムーズスクロール時に行背景を固定してチラつかないようにしたい。
- 制御文字の描画位置がsetextを反映できていない。NG

### #1496
- チラつきは無くなった。OK
- 制御文字の描画位置がsetextを正しく反映するようになった。OK
- markdownでVISUALモードに移行しても選択できなくなった。NG
- 非markdownでVISUALモードにしたままmarkdownに移行すると`o`,`O`なども効かなくなる。
- markdownモードでのスムーズスクロールをもう少しだけ速くしたい。非markdown時は4px/lineだっけ？markdown時だけ5px/lineにしてみて。

### #1497
- markdownでのVISUAL/INSERT周りの挙動は修正された。OK
- 制御文字の描画位置がsetextを反映できていない。NG
  - #1496での動作確認は勘違いだったかも。
- markdownモードでのスムーズスクロールをさらに少しだけ速くしたい。6px/lineにしてみて。

### #1498
OK、次はmarkdownから離れるが不具合修正を。
- 右上オーバーレイパレットの「インデント幅」ボタンで`TAB`を選ぶと、動作はTAB挿入に切り替わるが`:set shiftwidth?`としても`4`と表示される。NG
  - `4`はボタンでの直前の値だと思われる。
  - CLIでshiftwidthに`TAB`を設定するにはどう書けばいい？`:set shiftwidth=TAB`では何も変わらない。

### #1499
OK、次は仕様変更を。
- markdown清書モードでのmd_FGColor/md_BGGradientStart,End参照を廃止する。
- markdown/nomarkdownに関わらず、一部テーマを切り替えるコマンドを実装したい。
  - `:set theme=basic`, `:set theme=ex1`, `:set theme?`
  - themeは全バッファ共通でセッション情報に含める。
  - "basic"が選ばれたときはwindow.THEMEを参照し、未定義の色指定はyellowにする。(未定義は記述ミスなので見つけやすいように)
  - "ex1"が選ばれたときは、下記の色はwindow.EX1_THEMEから参照する。未定義ならwindow.THEMEを参照する。
    - editorTextColor
    - lineGradientStart
    - lineGradientEnd
    - inactiveGutterGradientStart
    - inactiveGutterGradientEnd
    - gutterNumberColor
    - activeLineGradientStart
    - activeLineGradientEnd
    - activeGutterGradientStart
    - activeGutterGradientEnd
    - activeEditLineGradStart
    - activeEditLineGradEnd
    - activeEditGutterGradStart
    - activeEditGutterGradEnd
    - activeLineNumberColor
    - caretGradStart
    - caretGradMid
    - caretIMEGradStart
    - caretIMEGradMid
    - editCaretGradStart
    - editCaretGradMid
    - editCaretIMEGradStart
    - editCaretIMEGradMid
  - markdown時の線色はeditorTextColor
- themeに対応するボタンを右下オーバーレイパレットに新設する。
  - ラベルは「清書カラー␊OFF ON」
  - OFFが`theme=basic`でONが`theme=ex1`
  - ボタンの位置は「即時終了ボタン」の上
- (質問)window.THEME.selectionFgとselectionBgは使われていない？VISUAL選択中の色のつもりだったけど。
- (質問)window.THEME.cmdInputFgとcmdInputBgは使われていない？CMDバーでの文字色/背景色のつもりだったけど。

### #1499
- 「清書カラー」ボタンの左右paddingを最小限まで減らしてボタン幅を縮めたい。
- _six.customizeファイルのcaret位置が即時終了・再起動で再現されないのは何故だろう？
- オーバーレイパレットが縦スクロールバーに接するように右marginを調整したが、稀に縦スクロールバーに重なっているときがある。
  - 発生条件はよくわからない
- markdownモードでマウスクリックした座標からcaret座標を計算する際にH1行などの行高を考慮していないように見える。
