### #1882
OK。次はGFMの実装に戻ってインデントコードブロックを実装したい。
- 下記がGFM仕様のはず。
  - 行頭から半角空白が4つ続いたらインデントコードブロック(のうちの1行)になる
  - 行頭からの半角空白0～3個に続いてTABコードが来たらインデントコードブロック(のうちの1行)になる
  - 空行は「前行がインデントコードブロックなら」インデントコードブロックになる
  - 上記を満たさない行が来たらその前行まででインデントコードブロックは閉じる
- インデントコードブロックの1行目と最終行は行高を2倍(=2行分)にし、その上にコードブロックをレンダリングする。
  - 通常のコードブロックの上下のフェンスを含めた高さと同等になるはずなので、marginやpaddingなど通常のコードブロックと同じようにレンダリングする。
    - yank/clipボタンも同様に配置する

### #1883
- GFMのインデントコードブロックの仕様で1つ抜けていた。
  - 行頭から半角空白が4つ続いても(un)ordered listの階層2以降ならそちらが優先(コードブロックにはならない)
- インデントコードブロックの1行目と最終行をはみ出す形でブロックが描画されている。NG
  - 2行分の行高にした1行目と最終行の中にブロックの描画が収まるのが期待動作
- 動作確認用の最小サンプルも用意してみて。

### #1884
prompt1883.md(1)～(4)のレンダリング：
- 「背景矩形の extPx(0.5ch) 伸長を抑止して“行ボックスの中に収める”ように変更」←伸長を抑止するのではなく、行高2の真ん中から伸長する。
  - 行高2倍にした1行目の真ん中から上が伸長部分
  - 行高2倍にした最終行の真ん中から下が伸長部分
- 改行文字が右にズレる。描画しない行頭インデント分だと思われる

### #1885
- draftでも清書でも、caretがブロックの先頭/末尾行に乗るとブロック領域が2倍行高フルで描画されてしまう。
- ブロック内で`$`としてもEOLの1つ手前までしか行けない。
- scrolloffの境界判定でインデントコードブロックの2倍行高が考慮されていないように見える
  - 仕様どおりかもしれないが、そうならば仕様変更で「レンダリング後の見た目で(1行分の行高 x scrolloff)」で判定してほしい。
- 2倍行高で描画している行の行番号を折り返し同様に縦方向にセンタリングして描画したい。

### #1886
prompt1883.md(1)～(4)のレンダリング：
- ブロック内で`$`としてもEOLの1つ手前までしか行けない。変わらず。
  - draftでも清書でも。バッファ内で折り返しは発生していない。
  - prompt1883.md内の全ブロックの全行で再現する
- 2倍行高のlistcharsが常に上半分の1行内に描画される
  - バッファ末尾の2倍行高では正しいがバッファ先頭の2倍行高では下半分に描画するのが期待動作

### #1887
- `$`でcaretがEOLに行くようになった。OK
- 2倍行高のブロック先頭行で下半分にlistcharsが描画されるようになった。OK
- マウスホバーで出るyank/clipボタンが2倍行高の上半分を基準にした座標になっている。NG
- fenced code blockでも以前からそうだが、ブロック内のlistcharsが少し左にズレて直前の文字に0.5chほど重なっている。
- 4)のlist継続行の4space行がindent code blockになっている。NG

### #1888
- yank/clipボタンの出現位置は良いが`Alt+j`などでスクロールしてもボタンが付いていかない・ホバーが外れても消えない
  - キー入力でマウスカーソルを消すタイミングでyank/clipボタンも消すようにしよう。
- listの継続4space行がlistと判定されるようになった。OK
- prompt883.mdで`ggG`とするとposInfoは行49だが表示範囲は[7,47]になってcaretが見えない。(6行ほど2倍行高を含む)
  - 行49の下にさらに6行+半端の余白行まで見えるのが期待動作

### #1889
- yank/clipボタンの描画はOK
- prompt883.mdで`ggG`とするとposInfoは行49だが表示範囲は[7,47]になってcaretが見えない。(6行ほど2倍行高を含む)
  - この状態でスクロールバーthumbは最下端にある
  - 行49の下にさらに6行+半端の余白行まで見えるのが期待動作

### #1890
- `ggG`で余白8行+半端まで見えるようになった。(期待値は余白6行+半端)
- `Ggg`としても表示範囲が[8,47+半端]になる(行48は2倍行高で1行目までは見えている)。
  - `l`などでcaretを動かしたり他バッファに移って戻ってくると表示範囲が[1,41+半端]になる。

- EOFが行49で表示行数がscrolloff=10なのに`gg:31`

### #1891
- `ggG`で余白8行+半端まで見える。→NGだが軽微だし何か実装する度に狂うところだからしばらく放置する。
- prompt1883.mdで`gg:38`としたあと`j`で下にcaretを動かしていくと行44からの`j`で期待とは逆向きに4行分スクロールする。NG
- `:30`などとしてから他バッファに移って戻ってくるとposInfoが行49になる(49は最終行)。表示は[8,47+半端]なのでcaretが見えない。

### #1892
- `gg:38`としたあと`j`で下にcaretを動かしていくと行44からの`j`で期待とは逆向きに4行分スクロールする。→変化なし
- `:30`などとしてから他バッファに移って戻ってきたときの表示範囲が狂ったりすることはなくなった。OK

### #1893
OK。次はmarkdown onでのレンダリングで修正を。
- ordered listで折り返しが発生しても、折り返し2行目がインデント付きで描画されない(行頭からになる)。NG
  - unordered listでは期待通りの位置から折り返し描画されている。

### #1894
OK。次はmarkdown onでのIME on時の挙動について。
- 1文字でも入力して未確定文字列が発生すると、(un)ordered listの折り返し行のインデントが消えるが、これは軽量化対策でわざとやってるんだっけ？
  - だとしても、改めてもう一度インデントが消えないようにしてみてほしい。重さを見て考える。

### #1895
変化した気はしないが、未確定文字列の有無でインデントが出たり消えたりするのを抑止したかったのが目的で、重くなった気もしないのでこれでOK。
次はmarkdown時のUI変更を。
- 右上パレット内の並びで「折り返し」ボタンを一番下に移し、markdown on時にマスクをかける
  - `:set wrap`などは有効でバッファローカルの変数は随時変更するが、markdown onのときはwrap変数に関係なく常にwrap onの動作とする(既存仕様)。
  - 何か「網掛け」のような、何のボタンかは判別出来る程度の隠し方をしたい。
  - 網掛けされている間は反応しないようにする。

### #1896
- マスク表現のハッチ表示の透明度をもう少し下げて、クロスで斜め十字になるようにしたい。
- マスク時だけ選択肢を[しない する]から[   する   ]に変えよう。全角5文字分のボタン。(押せないけど)
- マスク時はマウスホバー時の色変化(茶色っぽい色)の対象外にしたい。

### #1897
- やっぱりハッチは(クロスも)やめよう。
  - ボタン全体をベタ塗り白の透明度0.1で。
  - 「折り返し」の文字を#a0a0a0で。
  - 【する】のボタン色をmarkdownボタンの【OFF】のボタン色に。
- 不具合：
  - 【　　　する】みたいに右に寄っている。センタリングして。
  - markdown on/offを切り替えても即座にボタン描画が切り替わってない(enable/disableは効いている)。
  - markdown offのときの【する】のボタン幅が小さくなった(マージンがなくなった)ようだ。元に戻したい。
    - 

### #1898
- markdown on時の描画はOK
- markdown off時の【する】の幅が戻っていない。commit-id:7af579dでは【しない】と同じ幅だった。

### #1899
OK。
- unordered listのdiscの輪郭線の透明度を0.25にしてみてくれる？
- unordered listのsquareの輪郭線と塗り潰しを透明度0.80にしてみてくれる？

### #1900
- discの輪郭線を少し太くして透明度を0.5に。
- squareの塗り潰しだけを透明度0.60に。

### #1901
OK。次はmarkdownから離れるが、不具合の修正を。
- :e popupでTABキーによる補完が効かなくなっている。
  - commit-id:b40166c OK, commit-id:2907386 NG
- grepダイアログで検索語欄の`F4 履歴`ボタンと`F1`,`F2`,`F3`の文字色がinlineCodeFgになっている。
  - commit-idf76d10f OK, commit-id:2010fa9 NG
- ボタン内のポップアップを開いたときマウスクリックで選択できない。encodeボタン、検索時 A/aボタン、grepダイアログやCMDバーの"F4履歴"ボタン。なぜかZoomボタンはOK
  - かなり前の履歴まで辿ってみたがHEADと症状同じなので、もしかしたら実装したときからかも。

### #1902
- :e popupでTABキーによる補完が効かなくなっている。
  - commit-id:b40166c OK, commit-id:2907386 NG
- grepダイアログで検索語欄の`履歴 F4`ボタンと`F1`,`F2`,`F3`の文字色がinlineCodeFgになっている。
  - commit-idf76d10f OK, commit-id:2010fa9 NG
- ボタン内のポップアップを開いたときマウスクリックで選択できない。encodeボタン、検索時 A/aボタン、grepダイアログやCMDバーの"F4履歴"ボタン。なぜかZoomボタンはOK
  - かなり前の履歴まで辿ってみたがHEADと症状同じなので、もしかしたら実装したときからかも。

### #1903
- :e popupで`TAB`補完が効くようになった。OK
- ここで、ソース全体に対してリファクタリングしたい。方針は「想定外が起きても動く」ではなく「想定外をいち早く見つけられる」を優先して。
  - 想定外の値が来た場合に備えて変数参照をtry-catchで囲うのは極力減らしたい。
    - 想定外にcatch句に来たときはassertするなりlogを出すなりしてほしい。
    - 値参照で"|0"のように「想定外の値だったり未定義だった場合」を救うのも必要最小限にしたい。
  - システムから飛んでくるイベントのタイミングなど、バグが無くても想定外が起こりうる場合のtry-catchなどは理由をコメントしておいてほしい。

### #1904
高速化もやりたいが一旦置いておいて、今は「バグを見つけやすくする」という観点でtry-catchの握り潰しなどを削減したかった。もうなさそうなら次は不具合修正に移りたい。

### #1905
- 優先度1と2までやっておこう。
- 「スパムらない仕組み」って日本語で言うとどういう意味？

### #1906
OK。では不具合修正を。
- markdown onのINSERTで入力中の非編集のcaret移動でundo単位が分割されなくなった。
  - commit-id:2907386  OK
  - commit-id:f951a7e  NG

### #1907
OK。次は別の不具合修正を。(デグレではなく実装当初からの挙動かもしれない)
- CMDバーの【履歴 F4】ポップアップで、マウスクリックで確定できない。
  - マウスホバーには反応してカーソルは動く。が、確定が出来ない。

### #1908
CMDバーの【履歴 F4】ポップアップをマウスクリックで確定できるようになった。OK。
- grepダイアログ内の【履歴 F4】ボタン3つも同じ症状(ホバーは反応するがクリックが無視される)なので対処を。

### #1909
NG。いずれもクリックで確定できない。
- 検索語入力欄の【履歴 F4】
- -basedir入力欄の【履歴 F4】
- fileglob入力欄の【履歴 F4】

### #1910
変化なし。いずれも元々クリックでポップアップは開くしホバーに追随して選択カーソルは動く。唯一、項目をクリックしても何も起きない(ポップアップが開いたまま)。

### #1911
- grepダイアログ内の【履歴 F4】はいずれもクリックで確定できるようになった。OK
- 同様に、右上オーバーレイパレットの「encode」「検索時 A/a」ボタンも「マウスでポップアップを開いてホバーに追随してカーソルも動くが確定が出来ない」なので対処を。

### #1912
OK。次は小さいUI改善を。
- :e popupを表示している間、CMDバーの`履歴 F4`ボタンにマスクを掛けて押せないようにしたい。
  - markdown on時のwrapボタンと同じスタイルで。
- 変数themeをバッファローカルに変更する。
  - 右下オーバーレイパレットの「清書カラー」ボタンを右上オーバーレイに移動する。
    - 場所は「折り返し」ボタンの上

### #1913
- `履歴F4`のマスクはOK
- 「清書カラーボタン」の幅を右上オーバーレイパレットの他のボタンと揃えて。
  - OFF/ONの文字が小さいような。「markdown」ボタンや「制御文字表示」ボタンと大きさや間隔が揃うように。

### #1914
OK。
markdown onのときは折り返しボタンの選択肢を`  する  `にしているが、この`  する  `の塗り潰しを無しにして輪郭線だけにしたい。

### #1915
OK。次はmarkdownのインラインリンク用にsix独自の特殊UIを実装したい。
- 今はmd_drafteditが'清書'のときも、生のテキスト長で折り返し判定をして(折り返すなら)2行分の行高を確保しているが、清書時のテキストで折り返し判定をしてほしい。
- draftモードでアクティブ行になってもインラインリンクの部分は生テキストにせず清書と同じレンダリングにする。
  - draftモード時のみ、清書後のインラインリンク（[label]の部分）にcaretがあるときだけ「付近」にCMDバーのようなフローティングバー(URLバー)を表示し、そこに生テキストを表示する。
    - 「付近」はcaret行が画面中央より上にあるときはすぐ下の次行、caret行が画面中央またはそれより下にあるときはすぐ上の前行とする。
    - URLバーがcaretより上に表示されているときは`↑`、下に表示されているときは`↓`でURLバーに移る(NORMALでは`k`,`j`も)。
    - URLバーの右端に【確定↲】【キャンセル Esc】のボタンを配置する。CMDバーと同等のtextedit機能による編集が可能で、【確定↲】で初めて本文のテキストを編集内容で差し替える。
- 仕様として漏れや矛盾があったら指摘して。

### #1916
- やっぱりフローティングバー(URLバー)に表示するのは`(URL "ツールチップ")`の部分だけにしよう。
- `↑`,`↓`で行き来する仕様は無理があったので変更(というか実装してなかった？)
  - URLバーの左上に"Ctrl+E"というボタンを下1/3が被るように描画し、`Ctrl+E`でURLバーにフォーカスを移す。
- URLバーは画面内左右に4chずつpaddingを確保して横幅フル表示で。
- INSERTモードでもURLバーを表示して`Ctrl+E`で移行できるように。

### #1917
- インラインリンクを含む行内で`→`,`←`でリンク部分に入れない。NG
  - 前行や次行から`↓`,`↑`でなら入れる
  - INSERTモードでは`↓`,`↑`なら入れるがURLバーが出ない
- URLバーをcaretより下に表示するときは`Ctrl+E`の表示位置をURLバーの下側にする(上半分が被るように)
- `確定↲`,`キャンセルEsc`をURLバーに常時表示するのはやめる
  - URLバーがアクティブなときは`Ctrl+E`に代えて`確定↲`,`キャンセルEsc`を表示する
- - `確定↲`,`キャンセルEsc` を`確定 ↲`,`キャンセル Esc`に。(空白挿入)

### #1918
- 『左右(→,←)でインラインリンクの表示テキスト領域に「入らない」ように変更』←表示テキスト領域をskipしてcaret移動するようになったが、それは期待動作ではない。
  - `A[title](url)B`は`AtitleB`とレンダリングされるが、'A'から`→`でt,i,t,l,e,Bと移動できるのが期待動作
- INSERTモードでURLバーが出ない。NG
- コードブロック内でもインラインリンクがレンダリングされてしまう。NG
- URLバー内でIME ONにして1文字入力するとURLバーが消えて本文caret位置への挿入になってしまう。NG

### #1919
- INSERTモードでURLバーが出ない。NG
- コードブロック内ではレンダリングされないようになったが、インラインコードの中でもインラインリンクは無効にしたい。
- URLバー内でIME ONにして1文字入力するとURLバーが消えて本文caret位置への挿入になってしまう。NG
  - (推察)NORMALモードでのIME ONでINSERTモードに移行する処理が効いてしまっているのではないか？
    - 本文側のNORMAL/INSERTはURLバー内には関係ないはず
- URLバーがアクティブ(編集状態)のときは`↑`,`↓`などで本文側caretが動かないようにしたい。
  - 基本的に`Esc`でURLバーを非アクティブにするまで本文側は操作できないものとする。
    - 他タブへの切替やヘルプダイアログの表示は出来ていて、そのままでいい。(他の操作を無効化するうえで実装都合でタブ切替等も無効になってしまってもいい)

### #1920
- INSERTモードでURLバーが出るようになったが`Ctrl+E`が効いてない。
- インラインコード内でもレンダリングされないようになった。OK
- URLバー内でIME ONで入力してもURLバーは消えなくなったが、やはり本文caret位置への挿入になってしまう。NG
- URLバーがアクティブ(編集状態)のときでも`↑`,`↓`などで本文側caretが動いてしまう。NG

### #1921
- INSERTモードでURLバーでCtrl+Eでボタンが確定/Cancelに変わるが`↲`と`Esc`以外はcaret移動系は本文側に効いてしまい、通常文字を入れようとしてもURLバーが警告のように短くflashする。
  - URLバー内の見えないcaretが'(URL)'の次にあり、入力が禁止されているような挙動
- NORMALモードではURLバー内で`←`,`→`も有効で文字挿入も出来るし確定/Cancelも効く。ただし確定時の挿入位置がおかしい
- NORMALモードでURLバー編集中の`↑`,`↓`はURLバーごと本文caretが移動する。
- INSERTモードでURLバー編集中の`↑`,`↓`はURLバーが消えて普通に本文caretの移動になる。

### #1922
- NORMALモード：
  - 編集中に`↑`,`↓`で本文caretが動くことはなくなった。OK
  - 編集して確定できるようになったが`Esc`が効かない。
  - (...)の中を編集した場合は正しく反映されるが')'の右に例えば'abc'と入力すると`[title](URL)abc)`のようになぜか括弧が余分に挿入される。
  - 'Ctrl+E'が表示されている状態で`Enter`を押しても何も起きない。NG
    - インラインリンクを踏んだのでリンクを開くのが期待動作
    - マウスクリックでも何も起きない(caret移動にしかならない)
- INSERTモード：
  - `Ctrl+E`でURLバーをアクティブにして文字入力・確定は出来るが行末以外にcaretを動かそうとすると本文側に渡ってしまう。
  - 編集中に`↑`,`↓`は効かないようにしたい。
- インラインコードの中のインラインリンクはレンダリングされないが文字列幅(？)がレンダリング後の大きさになっているようで、caretが行末まで辿り着けない。

### #1923
- draftでインラインリンクを`Enter`で開くようになった。OK
  - 清書モードでも開くのが期待動作
    - マウスクリックであればdraftでも清書でも開く。OK
- draftでインラインリンクにcaretがいるとき、リンクを示すハイライトが1ch分長すぎる
  - マウスホバー時のハイライトは正しい

### #1924
- ズレは解消した。OK
- NORMALでもINSERTでも、URLバーで編集中に`Esc`を押しても何も起きない。

### #1925
- NORMALでもINSERTでも`Esc`が効くようになった。OK
- INSERTのとき、`Ctrl+E`でURLバーをアクティブにして文字入力・確定は出来るが行末以外にcaretを動かそうとすると本文側に渡ってしまう。

### #1926
- INSERT中に`←`,`→`はURLバー内で消費されるようになったが、`Home`,`End`は本文側に渡ってしまう。
  - `↑`,`↓`も本文側に渡ってしまう。
  - NORMAL中は`Home`,`End`,`↑`,`↓`ともURLバー内で消費される(`↑`,`↓`は何も起きない)。OK

### #1927
- INSERT中の`↑`,`↓`,`Home`,`End`はURLバー内で消費されるようになったが、`Home`の挙動が`End`と同じ「行末への移動」になっている
- INSERT中の`←`,`→`が本文側に渡るようになってしまった。

### #1928
- INSERT中の`←`,`→`がバー内で消費されるようにはなったが、`Home`でも`←`でも一瞬移動したあとすぐ行末に飛ばされるので`End`,`→`も正常に動作してるのか確認できない。

### #1929
- INSERTモードでもURLバー内でのcaret移動が正常になった。OK
- URLバーが長すぎるようで、左にはマージンがあるが右端がアプリウインドウの枠に収まっていない。NG
- 清書モードのときはインラインリンクにcaretが乗ってもURLバーは出ないが、`Ctrl+E`のボタンだけ表示するようにしよう。
  - `Ctrl+E`が押されたらURLバーを出すと同時に編集状態に入る。

### #1930
- URLバーの長さはOK
- NORMALでもINSERTでも清書モードで`Ctrl+E`のバッジがクリッカブルになっていないしキーボードからCtrl+Eを入力しても反応しない。
- 清書モードでの`Ctrl+E`バッジをあと0.3行高くらい離して。

### #1931
- `Ctrl+E`を押しても一度も反応しないしconsoleにも何も出ない。
- `Ctrl+E`バッジをクリックするとURLバーが開くが中身が空。NG
  - URLバーが開いたあとの`確定 ↲`,`キャンセル Esc`をクリックできない。
  - URLバーが開いて編集中の間は本文をクリックしてもcaret移動にならないようにしたい。
    - タブバーやオーバーレイパレットはクリック可

### #1932
- NORMALモードで、`Ctrl+E`がprefixのように2回続けて入力すると効くようだ。`Ctrl+E`,`Ctrl+E`のように。
  - INSERTモードでは続けて入力しても効かない。
- 清書モードでURL編集中は他の行をクリックしても無視されるが、ダブルクリックするとcaretが移動してしまう。NG
- draftモードでもURL編集中は他の行をクリックしても無視するようにしたい。
- draftモードでURL編集中の`確定 ↲`,`キャンセル Esc`のバッジがクリックできない。NG

### #1933
- NORMALモード清書で`Ctrl+E`を2回入力しないとURLバーが開かない。NG
  - INSERTモードでは続けて入力しても効かない。
- URL編集中に他の行をダブルクリックしても無視されるようになったが、トリプルクリックすると行選択されてしまう。NG
- 清書でもdraftでもURL編集中に`確定 ↲`も`キャンセル Esc`もクリックできない。キーボードからは効く。

### #1934
markdown onでインラインリンクのレンダリング結果'title'部分にcaretを持って行ったときの処理を実装中。
- 清書・NORMALモードで`Ctrl+E`と入力してもURLバーが開かない。NG
  - 続けてもう一度`Ctrl+E`と入力すると開く
- 清書・INSERTモードでは何度`Ctrl+E`と入力してもURLバーが開かない。NG
- 清書でもdraftでもNORMALでURL編集中に`確定 ↲`も`キャンセル Esc`もマウスクリックを受け付けるが、INSERTで`Ctrl+E`を1度押して何も起きなかったあとだとNORMALに戻ってもURL編集中にバッジクリックが効かなくなる。

### #1935
- NORMALで`Ctrl+E`1回でURLバーが開くが、2度目以降は`Ctrl+E Ctrl+E`と2回入力しないと開かなくなる。
  - 一度他のバッファに移って戻ってくると、1回目だけは`Ctrl+E`1回で開く。
- INSERTでは`Ctrl+E`はキー入力でもマウスクリックでも効かずURLバーを開けない。

### #1936
- バッジの表示位置はOK。
- draftでインラインコードの中のインラインリンクはただの文字列としてリンク判定しないでほしい。
- Markdown書式になっていなくてもURLリンクらしき文字列をSix独自でリンク扱いしているが、インラインコードの中では無効にしてほしい。
  - 清書モードでリンク判定になっている。draftではインラインリンク判定が優先されているがどちらも無効にしたい。

### #1937
- インラインコード内のリンク判定無効化はOK
- draftでも清書でもインラインリンクはレンダリング表示しているが、`w`,`b`,`W`,`B`でインラインリンク部分を飛び越えられない。NG

### #1938
`abc[page 1](https://yahoo.co.jp "らべる")xyz`という行で見た目は"abc`page 1`xyz"となっている。
- draftで`0w`で'p'にcaretが移動し、そのあと`w`を何度押してもcaretが動かない。
  - 清書だと`0w`で'e'の後ろの空白に飛び、そのあと`w`で'1'→'EOL'と飛ぶ。
- draftで`0W`で'1'→'x'とcaretが移動する
  - 清書だと`0W`で'1'にcaretが移動し、そのあと`W`で次行以降に飛ぶ。
  - 見た目に従ってほしいので清書の動きが正解？
- draftで`$b`で'x'にcaretが移動し、そのあと`b`を何度押してもcaretが動かない。
  - 清書だと`$b`で'1'にcaretが移動し、次の`b`で行頭に飛ぶ。
- draftで`$B`で'x'にcaretが移動し、そのあと`B`を何度押してもcaretが動かない。
  - 清書だと`$B`で'1'にcaretが移動し、そのあと`B`で行頭に飛ぶ。

### #1939
- インラインリンクを跨いだ`w`,`b`,`W`,`B`の挙動はOK。
- 次はmarkdownに限定しない基本仕様の変更・追加を。
  - sixでは`e`や`E`を実装せず`w`や`W`の挙動を一部変更していたが、やはり`e`,`E`,`ge`,`gE`を実装してVimに合わせたい。
    - まずは現状の`w`,`W`の挙動を修正してみて。`cw`や`dw`も含めて。

### #1940
OKだが、漢字と片仮名・平仮名に関するsix独自ルールってどうしてたっけ？
- "関東ステレオひらがなabc"だと'関'から`w`で'a'まで行くが'a'から`b`で'ひ'に飛び、そこから`b`で'関'に飛んでいる。
  - 「漢字+平仮名」を1wordにするsix独自ルールを入れた記憶はあるけど、実際は「漢字+{平仮名または片仮名}」で1wordにしてたっけ？
    - だとしたら'a'から`b`で'関'まで戻らないのは何故だろう？

### #1941
了解。仕様を決めたときの詳細を忘れたが、ルール変更したい。
- 片仮名を漢字と同格に格上げする。なので「漢字+平仮名」で1word、「片仮名+平仮名」で1wordとする。
  - 形態素解析までは実施しないが、ひらがなは助詞であることが多い想定でなるべく助詞まで含めた文節を移動単位にしたい意図。
  - "にほん関東ステレオひらがな新宿"内を移動するときの期待動作：
    - '関'から`w`で'ス'まで飛ぶ
    - 'ス'から`w`で'新'まで飛ぶ
    - '新'から`b`で'ス'まで飛ぶ
    - 'ス'から`b`で'関'まで飛ぶ

### #1942
OK。次は`e`,`E`,`ge`,`gE`を実装しよう。
- help.03.mdとhelp.05.mdを更新して。

### #1943
`ge`,`gE`の挙動が`e`,`E`と同じに見える。NG

### #1944
OKだけど、仕様を変更したい。
Vim/vi標準と異なりEOLまでcaretが移動する仕様にしているので、それに合わせて`e`,`E`ともに「word/WORDの末尾の次」への移動とする。
"にほん 関東ステレオひらがな 新宿"内を 移動するときの期待動作：
- 'に'から`e`で「'ん'の後ろの空白」→`e`で「ス」→`e`で「'な'の後ろの空白」
