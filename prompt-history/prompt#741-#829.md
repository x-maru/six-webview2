chatは常に日本語で。

#741
OK、jar化はせず現行のままとする。
- install.cmdでsix_wrap.ps1へのショートカットを生成するときにsix_shortcut.pngをアイコンとして設定してほしい。

#742
方針変更。最初からicoで持っておくようにする。存在しない場合のフォールバックは不要。エラー終了でいい。
six_shortcut.pngから複数サイズ対応のicoファイルを生成できる？(six_shortcut.ico)

#743
エラーなくgen-ico.ps1を実行出来たけど、生成されたsix_shortcut.icoは白いデフォルトアイコンに見える。NG

#744
外部ツールにてicoファイル化できたので、png→ico変換はもう不要。install.cmdでsix_shortcut.icoを使って。
(install_debug.cmdは今のままでいい)

#745
archive.shはsixを他PCなどに持って行って動かすための一式をアーカイブするものだが、冒頭のVERSION定義を書き換えて_six.js冒頭で定義しているVERSIONを参照するように書き換えてくれる？

#746
うまく抽出できていないようだ。
```
ERROR: VERSION not found in _six.js
```

#747
変わらず。うまく抽出できていないようだ。
```
ERROR: VERSION not found in _six.js
```
_six.jsで定義しているVERSIONは数値定数のつもりだから「"1.2"」とか「'0.9'」は想定しなくてよい。「10.1」はありうるが。

#748
エラー無く終了するが、tarファイルが生成されていない。

#749
次は外部更新検出をブラッシュアップしよう。
- ずっと表示したままのタブのファイルを外部で更新した場合、他タブに移って戻ってくるまで検出されない。定期的にポーリングする必要はないが、アプリが最小化したり他アプリの裏にまわって再び手前に来たときに検知するようにしたい。
  - 自前caretやマウスカーソルを変化させるために「再び手前に来た」タイミングは今でも検出してるはず。

#750
OK, 検出されなかった原因はよくわからないが、重いプロセスがいてWindows全体の動きがスローになっていた(正常でなかった)ためだろう。次。
- 開いているファイルがsixの外部で削除された場合、更新検出のタイミングでスルーされている。
  - 削除も検出してモーダルダイアログを出したい。
    - 「このファイルはsixの外部で削除されました。保存しますか？」[保存][キャンセル]
      - 別名保存などしたければキャンセルして`:e name`などすればよいので、最低限の周知でいい。
      - キャンセルを選んだ場合、そのバッファに対しては即時終了するまで外部削除通知のダイアログは出さない。
        - 次回起動時は再び最初に検知した際に同様にモーダルダイアログを出す。

#751
- NG。検出されない。他タブから戻ってきてもセッション情報からレジューム起動しても検出されない。

#752
- NG)レジューム起動時 →検出されない
- NG)アプリウインドウが非アクティブからアクティブ →検出されない
- OK)他タブから戻ってきたとき →検出される
  - OK)キャンセルしたあとは他タブから戻ってきてもダイアログは出ない
  - OK)キャンセルしたあと即時終了して再び起動すると他タブから戻ってきたときのダイアログが復活する

#753
- OK)レジューム起動時 →5/5で検出された
- OK)アプリウインドウが非アクティブからアクティブ →5/5で検出された
- NG)他タブから戻ってきたとき →検出率2/5くらい
  - #752でもそれくらいだったかも。

#754
外部削除の検出自体はOKと思われる。
- 「保存しますか？」で保存を選んだあとINSERTモードで編集し、`Esc`でNORMALに移行した際に「キャンセルしました」とトーストが出てそのセッション中は外部削除を検出しなくなる。
  - 外部削除の検出は完璧じゃなくてもいいとは思っているが、NORMALに移行した際のトーストは意味不明なので防ぎたい。

#755
- OK)外部削除検出で保存したあとの編集・モード切替で「キャンセルしました」となる現象は出なくなった
- NG)外部削除検出時のダイアログで「保存」を選んで実行してもmodifiedフラグがリセットされない

#756
- NG)外部削除検出時のダイアログで「保存」を選んで実行してもタブの"*"が即座に消えない。
  - `:q`でも警告が出ないし他タブに行って戻ると"*"が消えているのでmodifiedリセット自体はされていて表示の問題だけと思われる

#757
OK。
- :e popupを開いた直後にカーソルを一切動かさずに先頭候補の"../"をそのままEnterで選べないようにしてる理由はなんだっけ？

#758
「"../"にカーソルがあるのに`Enter`が無視されるのは嫌だ」「"/…"や"//…"を頻繁に入力する」の2つを両立できればベストだな。
- :e popupを開いた直後のみ、popupカーソル直下の候補すなわち".."をコマンド入力欄に補完した上で選択状態にしたい。
  - `:e ..`のうち".."だけが文字列選択された状態。なのでそのまま`Enter`でも`/`でも他の文字でも受け付ける。
  - popup内でカーソルを動かせば選択状態は消える。

#759
- ':e ..'のうち".."を文字列選択状態にしたとき、".."をユーザが入力したのではなくpopupから自動補完されたものとして扱いたい。なのでpopup内で".."にマッチする候補は"../"しかなくてもカーソル移動できるように。

#760
- カーソルは動くようになった。OK
- サブフォルダに降りた直後もpopupの一番上の候補"../"にカーソルがあるのだからコマンド入力欄にpopupから".."を自動補完してほしい。
  - popupを開いた直後以外は「コマンド入力欄で".."を文字列選択された状態にする」処理は不要。

#761
- NG。変化なし。
- 追加で、popupを開いた直後の、"../"からカーソルを動かしもしていない状態では絞り込み処理はスキップしてほしい。
  - 「絞り込みしない」＝全候補が通常色(または不正名の赤)で表示される。「マッチしないグレー」にはならない。

#762
- :e popupでサブフォルダに降りたとき、カーソルを`↓`,`↑`と動かしたのと同じ結果になるようにしたい。
  - 入力欄には".."が補完される

#763
- 変わっていない。
- :e popupでサブフォルダに降りたとき、カーソルを`↓`,`↑`と動かしたのと同じ結果になるようにしたい。
  - サブフォルダに降りると同時に入力欄には".."が補完されてほしい

#764
- clickのときは入力欄に".."がappendされるが`Enter`ではappendされない。NG

#765
- 改善せず。`Enter`でサブフォルダに移った際、popupのカーソルは一番上の候補の"../"を指しているが入力欄に".."がappendされていない。

#766
少し挙動が変わった(悪化)
- ":e foo/sub"→`Enter`→":e foo/sub/"だったものが":e "になり、sub/の候補は表示されているがカーソルが動かない(".."で絞り込まれてしまう)。期待動作は":e foo/sub/.."

#767
フィルタは発動せずカーソルが動くようにはなったが、入力欄が期待動作になっていない。カーソルを動かすと":e "→`↓`→":e sub/1.txt"→`↑`→":e "となる。

#768
- :e popupを開いたところから2段階サブディレクトリに降りると一覧を取得できなくなる
- チャットは日本語で返して。

#769
- NG。先に仕様変更しよう。
  - :e popupの一覧に疑似候補"../"を加えるのをやめる。
  - :e popupを開いた直後だけの例外で入力欄への補完部分を選択状態にする仕様はそのまま。".."ではない先頭行からの補完文字列が対象。
  - `Alt+U`で親ディレクトリに移動できるようにしよう。

#770
- NG。"C:/Users/ymaru/WebView2/foo.txt"がカレントバッファのときに`:e `で開いたpopupで`Alt+U`とすると"C:/"直下の一覧がpopup内に列挙される。

#771
- NG。"C:/Users/ymaru/WebView2/foo.txt"がカレントバッファのときに`:e `で開いたpopupで`Alt+U`と入力しても何も起きない。
  - サブディレクトリ"six"には移動できたが、そこから`Alt+U`とすると"C:/Users/"直下の一覧がpopup内に列挙される。

#772
変わらない。
- C:/Users/ymaru/WebView2/foo.txt"がカレントバッファのときに`:e `で開いたpopupで`Alt+U`と入力しても何も起きない。
  - その状態で`/`とタイプすると"C:/"直下の一覧が表示され、`Esc`,`Esc`で閉じたあと再度`:e `で開くと`Alt+U`でC:/User/直下の一覧がpopup内に列挙される。

#773
`Alt+U`も`Alt+↑`も効かない。`↑`,`↓`でカーソルが移動するが、`Alt+↑`は`↑`と同じ挙動になっている。
  - キーコード取得の問題なら、とりあえず親ディレクトリへの移動を`F1`にして。
  - `:e `でpopupを開いたあと`↓`で移動すると入力欄が`:e C:/Users/ymaru/WebView2/six`などとなるが、フルパスへの変換などしないでほしい。

#774
`F1`が無反応。`F9`でヘルプや`F12`でDevToolsなどは効いているので、F1～F8を消費する処理に負けているのではないか？
他は特に変化はないように思う。

#775
- 変化なし。`F1`も効かず親ディレクトリに移るには入力欄を編集するしかない状態。
- `:e `でpopupを開いた直後は入力欄は":e "のまま →NG
  - カーソル位置が"sample/"なら即座に":e sample/"となり"sample/"の部分が選択状態になるのが期待動作。
    - `↑`と入力するとカーソルは動かないが入力欄が":e C:/Users/ymaru/WebView2/sample"となる。

#776
- 1. popupを開いた時入力欄に何も補完されていない。NG
- 2. `↑`,`↓`で絶対パスに変化してしまう。NG
- 3. `F1`が効かない。NG
- 4. サブディレクトリ移動時に入力欄には'/'しかappendされていない。NG
- 5. `:e foo/sub`,`Enter`時の期待動作：("foo/sub"の下は"sub2dir/"と"file1.txt"があるものとする)
  - 入力欄は`:e foo/sub/sub2dir`
  - popupは"sub2dir/"にカーソル
  - 絞り込みは効いていない状態
  - `Enter`を押すと"popupは"foo/sub/sub2dir/"の中の一覧が出る

#777
1. NG)popup表示直後に"name/"が補完され選択状態になっているか →YES。だが期待動作は"name/"ではなく"name"が補完されること。
2. OK)`↑`,`↓`でカーソル移動しても相対パスをキープ
  - ただしディレクトリを表す"/"まで含めて補完しているのはNG
3. NG)`F1`を押しても無反応
4. NG)サブディレクトリをカーソルで選択して`Enter` →無反応
5. OK)入力欄末尾に'/'がない状態にしてから'/'を入力すればサブディレクトリに入れる

#778
- OK)popup表示直後や他のディレクトリにカーソルを持って行ったとき末尾の'/'が入力欄にappendされない
- NG)入力欄に補完した文字列を選択状態にするのはpopup表示直後の1回のみ。カーソルを動かして補完文字列を切り替える際はもう選択状態にはしない。
- 最初`F1`も`Alt+U`も無反応だったが、何度かpopup内でフォルダ移動などしていたら効くようになった
- OK)サブディレクトリにカーソルを合わせて`Enter`でサブに移動できるようになった(選択状態にするのは不要だが)
- チャットの回答は常に日本語でお願い。

#779
1. NG)期待動作が違う。「補完&選択状態」は手入力ではないので絞り込みは発動しないのが期待動作。現状は絞り込みになっているようだ。
2. NG)最初`F1`も`Alt+U`も無反応だったが、何度かpopup内でフォルダ移動などしていたら効くようになった。条件がよくわからない
3. NG)期待動作が違う。入力欄を選択状態にするのはpopupを開いた直後の1回のみ。サブディレクトリに降りたときは補完のみ。

#780
1. OK)初回popupから`↓`で選択解除されて末尾の'/'なしで補完
   NG)`↓`で移動した先の候補が_fileFilterにセットされるようで、マッチするものにしか移動できなくなる
2. NG)`F1`,`Alt+U`は無反応。`Alt+↑`は`↑`として反応
3. OK)カーソル移動するまではOK
4. OK)popup出現時以外は選択状態にはならない。OK。ただし_fileFilterが効いてしまっている。
- 「相対モード中はキー入力（タイプ）でのみ _fileFilter を更新し、矢印移動時は更新しない方針への統一。」→元々その仕様のつもりだった。相対パスかどうかに関わらず。

#781
1. OK
2. NG
- 「ポップアップ未表示（:e 入力直後）でも F1 / Alt+U / Alt+↑ で親ディレクトリへ移動できるよう拡張」→`:e`のあと`space`入力前(popup表示前)にそのような拡張は不要。
3. OK
4. OK

#782
ポップアップ表示中でも`F1`や`Alt+U`が一切効かない。`Alt+↑`はただの`↑`になっている。
- 'Alt+↑`での親ディレクトリ移動は不要

#783
- 変化なし。`F1`も`Alt+U`も効かない。(なお`Alt+U`は`Alt+u`のつもりだった。`Shift`は押下しない)
- なおpopup表示中でも`F9`でヘルプダイアログが開く。仕様的には開いても開かなくてもどちらでもいいが。

#784
- popup表示直後は`F1`,`Alt+U`が効かないが、1度サブディレクトリに移ると以後`F1`も`Alt+U`も効くようになる
  - 一度効くようになるとpopupを閉じて再度`:e `でpopupを表示した直後でも効く。
  - 即時終了して再起動するとまた効かなくなる(1度サブディレクトリに移動すれば効くようになる)。
- 最上位からさらに上に移動しようとした際のトーストは正しく表示されている。OK

#785
変化なし。1度サブに移ればアプリ終了するまでは大丈夫だけど。

#786
1度サブに移っても`F1`,`Alt+U`が効かなくなった。
パッチを破棄したので、今はまた「1度サブに移ればアプリ終了するまで`F1`などが効く」状態。

#787
また悪化した。一度サブに移れば`F1`などが効くけど「ひとつ親」ではなく常にルートまで戻ってしまう。
パッチを破棄した。

#788
変化なし。1度サブに移ればアプリ終了するまでは一気に最上位まで飛ぶこともなく大丈夫だけど。

#789
変化なし。デバッグログを入れてみようか。

#790
- 一度サブに移るまで`Ctrl+Shift+F1`も効かない。
  - サブに移ったあとも"parentNav debug: OFF"にしかならない

#791
何も変わっていない。
- 一度サブに移るまで`Ctrl+Shift+F1`も効かない。
  - サブに移ったあとも"parentNav debug: OFF"にしかならない
― chatは常に日本語にして。

#792
1.consoleには:e関連でどんな操作をしても一切何も出ていない。
2._fileParentDebug宣言は行11265にあり、行5の`(function(){`から行13446(EOF行)の`})();`の中。
デバッグ用のトーストは「表示されたかされていないか」の確認に使うにとどめたい。基本的にはデバッグプリントはConsoleを使って。
- earlyParentKey定義部分(行12482～行12510)
```
  // 早期キャプチャ: 他ハンドラに奪われる前に F1 / Alt+U を検出
  try{
    const earlyParentKey = (e)=>{
      try{
        const popupVisible = (function(){ try{ return typeof _filePopupVisible==='function' && _filePopupVisible(); }catch{ return false; } })();
        const domVisible = (function(){ try{ return bufpopup && bufpopup.dataset && bufpopup.dataset.kind==='file' && bufpopup.style.display!=='none'; }catch{ return false; } })();
        const ctxOk = (_mode==='CMD' && (popupVisible || domVisible));
        if (!ctxOk){ _fileParentLog({ phase:'early-context-miss', mode:_mode, popupVisible, domVisible }); return; }
        // デバッグトグル / ログ表示
        if (e.key==='F1' && e.ctrlKey && e.shiftKey && !e.altKey && !e.metaKey){
          e.preventDefault(); e.stopPropagation(); _fileParentDebug=!_fileParentDebug; _fileParentLog({ phase:'debug-toggle', enabled:_fileParentDebug }); try{ toast && toast('parentNav debug: '+(_fileParentDebug?'ON':'OFF'),1500); }catch{} return; }
        if ((e.key==='u' || e.key==='U') && e.ctrlKey && e.shiftKey && !e.altKey && !e.metaKey){
          e.preventDefault(); e.stopPropagation(); const last=_fileParentLogs.slice(-5).map(r=>r.phase).join(', '); try{ toast && toast('parentNav logs: '+(last||'(none)'),1800); }catch{} return; }
        const now = Date.now();
        if (typeof window._fileParentNavGuardUntil!=='number') window._fileParentNavGuardUntil = 0;
        const guarded = (now < window._fileParentNavGuardUntil);
        const wantF1 = (!e.altKey && !e.ctrlKey && !e.metaKey && e.key==='F1');
        const wantAltU = (e.altKey && !e.ctrlKey && !e.metaKey && (e.key==='u' || e.key==='U' || e.code==='KeyU'));
        _fileParentLog({ phase:'key-check', wantF1, wantAltU, guarded, popupVisible, domVisible });
        if (!guarded && (wantF1 || wantAltU)){
          window._fileParentNavGuardUntil = now + 160;
          e.preventDefault(); e.stopPropagation(); _fileParentLog({ phase:'trigger', key:e.key }); _fileNavParent(); return;
        }
        if (wantF1 || wantAltU){ _fileParentLog({ phase:'guarded-skip', key:e.key }); }
      }catch{ _fileParentLog({ phase:'early-exc' }); }
    };
    // capture=true で最優先（window のみに限定し二重発火を避ける）
    window.addEventListener('keydown', earlyParentKey, true);
  }catch{}
```
- _fileParentDebug宣言部付近(行11261～11270)
```
  // 直前に“実際に画面表示していた”一覧（レースや未確定ホスト操作時の復元に使う）
  let _fileVisibleEntries = [];
  let _fileVisibleBaseKey = null;
  // 親ディレクトリ移動デバッグフラグ / ログバッファ
  let _fileParentDebug = false;
  let _fileParentLogs = [];
  function _fileParentLog(entry){
    try{
      if (!_fileParentDebug) return;
      const ts = new Date().toISOString();
```
- ワークスペースのルートはずっと_six.jsのあるディレクトリだが？(wsl.localhost/Ubuntu/home/ymaru/work/WebView2/six)

#793
`:e`でpopupを開いた直後に`Ctrl+Shift+F1` →無反応
- PCメーカ製アプリなどにキーが奪われてる可能性もあるので、`Ctrl+Shift+F1`をやめて`@`にしてみてくれる？

#794
- `:e `でpopupを出した状態では`@`はファイル名入力に奪われてしまう。
  - やはり`@`はやめよう。コマンド":parentnav"だけでトグル動作にしよう。

#795
:parentnav後、:e popupで`F1`が無反応のときはConsoleに何も出ない。
- サブディレクトリに移ってから`F1`を押したときのコンソール：
```
_six.js:13431 [six] boot done
_six.js:11283 [parentNav] {ts: '2025-11-22T16:49:26.975Z', phase: 'debug-toggle', enabled: true}
_six.js:5741 [parentnav toggle] true
_six.js:11283 [parentNav] {ts: '2025-11-22T16:49:43.720Z', phase: 'key-check', wantF1: true, wantAltU: false, guarded: false, …}
_six.js:11283 [parentNav] {ts: '2025-11-22T16:49:43.720Z', phase: 'trigger', key: 'F1'}
_six.js:11283 [parentNav] {ts: '2025-11-22T16:49:43.722Z', phase: 'key-check', wantF1: true, wantAltU: false, guarded: true, …}
_six.js:11283 [parentNav] {ts: '2025-11-22T16:49:43.722Z', phase: 'guarded-skip', key: 'F1'}
```
- `Ctrl+Shift+U`は何か出ているがメモってる暇もないし、出すならConsoleにして。

#796
1. `F1`反応せず、Consoleに出ない
2. `Alt+U`反応せず、Consoleに出ない
3．`Ctrl+Shift+U`反応せず、Consoleに出ない
4. サブ移動後の`F1`：
```
_six.js:12505 [parentNav raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:11283 [parentNav] {ts: '2025-11-22T17:02:06.237Z', phase: 'key-check', wantF1: true, wantAltU: false, guarded: false, …}
_six.js:11283 [parentNav] {ts: '2025-11-22T17:02:06.237Z', phase: 'trigger', key: 'F1'}
_six.js:12505 [parentNav raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:11283 [parentNav] {ts: '2025-11-22T17:02:06.239Z', phase: 'key-check', wantF1: true, wantAltU: false, guarded: true, …}
_six.js:11283 [parentNav] {ts: '2025-11-22T17:02:06.239Z', phase: 'guarded-skip', key: 'F1'}
```
- popupが出ていないときはF1でタブ切替が正常に動いているので、OSなどに取られている可能性はないのでは？
- なお`Ctrl+Shift+F1`はNGだった(`Ctrl+F1`はHHKBキーボードに取られるので取得不可)

#797
初回popupで`F1`押下時のログ：
```
_six.js:591 [parentNav global-raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:605 [parentNav global-trigger] {key: 'F1'}
_six.js:606  [parentNav global-trigger error] ReferenceError: _fileNavParent is not defined
    at _parentNavGlobalKey (_six.js:606:14)
_parentNavGlobalKey @ _six.js:606
```

#798
初回popupで`F1`押しても何も起きないときのConsole:
```
_six.js:591 [parentNav global-raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:605 [parentNav global-trigger] {key: 'F1'}
```
サブに移ってから`F1`を押下したときのConsole:
```
_six.js:591 [parentNav global-raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:605 [parentNav global-trigger] {key: 'F1'}
_six.js:12544 [parentNav raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:11320 [parentNav] {ts: '2025-11-22T17:21:12.628Z', phase: 'key-check', wantF1: true, wantAltU: false, guarded: true, …}
_six.js:11320 [parentNav] {ts: '2025-11-22T17:21:12.628Z', phase: 'guarded-skip', key: 'F1'}
```

#799
何も変わっていない。初回popupで`F1`押しても何も起きないときのConsole:
```
_six.js:591 [parentNav global-raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:605 [parentNav global-trigger] {key: 'F1'}
```
サブに移ってから`F1`を押下したときのConsole:
```
_six.js:591 [parentNav global-raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:605 [parentNav global-trigger] {key: 'F1'}
_six.js:12540 [parentNav raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:11320 [parentNav] {ts: '2025-11-22T17:31:48.803Z', phase: 'key-check', wantF1: true, wantAltU: false, guarded: true, …}
_six.js:11320 [parentNav] {ts: '2025-11-22T17:31:48.803Z', phase: 'guarded-skip', key: 'F1'}
_six.js:12540 [parentNav raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:11320 [parentNav] {ts: '2025-11-22T17:31:48.804Z', phase: 'key-check', wantF1: true, wantAltU: false, guarded: true, …}
_six.js:11320 [parentNav] {ts: '2025-11-22T17:31:48.804Z', phase: 'guarded-skip', key: 'F1'}
```

#800
何も変わっていない。初回popupで`F1`押しても何も起きないときのConsole:
```
_six.js:591 [parentNav global-raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:605 [parentNav global-trigger] {key: 'F1'}
```

#801
初回popupで`F1`押しても何も起きないときのConsole:
```
_six.js:591 [parentNav global-raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:604 [parentNav global-trigger] {key: 'F1'}
_six.js:609 [parentNav global-call-path] {haveDirect: false, haveRef: false}
_six.js:613 [parentNav global-call defer]
_six.js:614 [parentNav global-deferred-check] {h1: false, h2: false}
_six.js:614 [parentNav global-deferred-absent]
```

#802
初回popupで`F1`押しても何も起きないときのConsole:
```
_six.js:595 [parentNav global-raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:608 [parentNav global-trigger] {key: 'F1'}
_six.js:613 [parentNav global-call-path] {haveDirect: true, haveRef: true}
_six.js:614 [parentNav global-call direct]
_six.js:585 [parentNav stub pending init]
_six.js:585 [parentNav stub pending init]
```

#803
:e popupを開いてから2回目の`F1`で2つ上の親まで飛んでしまう。
例）"C:/Users/ymaru/foo/bar/a.txt"のバッファ上で:e popupを出した場合：
- 1回目の`F1` → "C:/Users/ymaru/foo/"
- 2回目の`F1` → "C:/Users/"

#804
起動途中で初期化に失敗するようになった。
Consoleには何も出ていないが画面最下部にメッセージが出ている。
```
six: JavaScript の初期化に失敗しました。F12でコンソールを開き、Ctrl+F5 で再読込してください。
```
パッチを撤回してソースを1つ前に戻した。#803を再掲する
- :e popupを開いてから2回目の`F1`で2つ上の親まで飛んでしまう。
  - 例）"C:/Users/ymaru/foo/bar/a.txt"のバッファ上で:e popupを出した場合：
    - 1回目の`F1` → "C:/Users/ymaru/foo/"
    - 2回目の`F1` → "C:/Users/"

#805
起動途中で初期化に失敗する。Consoleには何も出ていないが画面最下部にメッセージが出ている。
```
six: JavaScript の初期化に失敗しました。F12でコンソールを開き、Ctrl+F5 で再読込してください。
```
パッチを撤回してソースを#803の直前に戻した。
下記を慎重に修正して。
- :e popupを開いてから2回目の`F1`で2つ上の親まで飛んでしまう。
  - 例）"C:/Users/ymaru/foo/bar/a.txt"のバッファ上で:e popupを出した場合：
    - 1回目の`F1` → "C:/Users/ymaru/foo/"
    - 2回目の`F1` → "C:/Users/"

#806
:e popupでの`F1`で2つ上の親まで飛んでしまう。
例）"C:/Users/ymaru/foo/bar/a.txt"のバッファ上で:e popupを出した場合：
- 1回目の`F1` → "C:/Users/ymaru/"
- 2回目の`F1` → "C:/"

#807
セッション情報を再現せず空バッファで起動するようになった。改めて即時終了して再起動してもセッション情報が再現されない。

#808
ソースが色々おかしくなったので少し前の状態に戻した。
:e popupでの`F1`で2つ上の親まで飛んでしまう。
例）"C:/Users/ymaru/foo/bar/a.txt"のバッファ上で:e popupを出した場合：
- 1回目の`F1` → "C:/Users/ymaru/"
- 2回目の`F1` → "C:/"

#809
`F1`で正常にひとつ上に移動できるようになった。OK
- カーソルを動かすとカーソル位置の項目が入力欄に補完されるが、この挙動はサブディレクトリに移動したときにも(カーソルを動かす前から)適用されるべき。

#810
NG。サブに降りたとき補完されないままだし`F1`で2つ上に移動してしまうバグが再発した。
パッチを破棄したので改めて慎重に対処して。
- カーソルを動かすとカーソル位置の項目が入力欄に補完されるが、この挙動はサブディレクトリに移動したときにも(カーソルを動かす前から)適用されるべき。

#811
サブに降りたときの挙動はOK
- `F1`での親への移動が「six起動後1回のみ」しか効かなくなった
  - 一度サブに移動すると回数制限は無くなる。即時終了して再起動するとまた1回制限状態になる。
    - 回数制限がなくなった状態で、ときどき「1度の`F1`で2つ上の階層に移動する」現象が発生する
      - 発生条件はよくわからない

#812
サブに降りたときの挙動はOK
- `F1`での親への移動が「six起動後1回のみ」しか効かない現象は変わらず。NG
  - 一度サブに移動すると回数制限は無くなる。即時終了して再起動するとまた1回制限状態になる。
    - 回数制限がなくなった状態で、ときどき「1度の`F1`で2つ上の階層に移動する」現象は発生しなくなったようだ(検証不十分だが)。

#813
- `F1`での親への移動が「six起動後1回のみ」しか効かない現象は変わらず。NG
  - 一度サブに移動すると回数制限は無くなる。即時終了して再起動するとまた1回制限状態になる。
  - 回数制限が無くなるのと同時にpopupを閉じるまでの間`Esc`が効かなくなる(この版からなのかは不明)。

#814
変わらず。
- `F1`での親への移動が「six起動後1回のみ」しか効かない現象は変わらず。NG
  - 一度サブに移動すると回数制限は無くなる。即時終了して再起動するとまた1回制限状態になる。
  - 回数制限が無くなるのと同時にpopupを閉じるまでの間`Esc`が効かなくなる。

#815
- `F1`での親への移動が「six起動後1回のみ」しか効かない現象は変わらず。NG
  - 一度サブに移動すると回数制限は無くなる。即時終了して再起動するとまた1回制限状態になる。
  - 回数制限が無くなるのと同時にpopupを閉じるまでの間`Esc`が効かなくなる現象は発生しなくなった。OK

#816
- 初回の:e popupでの`F1`
```
_six.js:616 [parentNav stub pending init]
_six.js:633 [parentNav stub fallback attempt] {inCmd: true, domVisible: true, dataKind: 'file'}
_six.js:657 [parentNav stub fallback done] {entries: 6}
```
- 続けて2回目の`F1`
```
_six.js:616 [parentNav stub pending init]
_six.js:633 [parentNav stub fallback attempt] {inCmd: true, domVisible: true, dataKind: 'file'}
_six.js:635 [parentNav stub fallback skipped already-done]
```
- サブディレクトリに降りてからの`F1`
```
_six.js:12704 [parentNav install real]
_six.js:12708 [parentNav auto-run skip after-fallback]
_six.js:12704 [parentNav install real]
_six.js:12639 [parentNav attempt] {mode: 'CMD', allow: true, popupVisible: true, domVisible: true, haveBase: true, …}
_six.js:12663 [parentNav path-info] {fullPath: '/C:/Users/ymaru/WebView2/six/tmp/', trimmed: '/C:/Users/ymaru/WebView2/six/tmp'}
_six.js:12696 [parentNav done] {prevBase: 'file:///C:/Users/ymaru/WebView2/six/tmp/', newBase: 'file:///C:/Users/ymaru/WebView2/six/', entries: 42}
_six.js:12735 [parentNav raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:12614 [parentNav real skip multi-fire]
_six.js:12735 [parentNav raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
```

#817
１. OK。fbCountの増加と共に1つ上に移動できている。
- ただし「今いたディレクトリ」が入力欄に補完されなくなった。NG
  - 「A.対象ディレクトリをひとつ親に変更」「B.今いたディレクトリにカーソル位置を合わせる」「C.カーソル位置の候補を末尾の'/'を除いて入力欄に補完」という3つの処理に分割すると、`F1`での処理はAとBまでで、あとはサブディレクトリに移動した直後と共通の「新ディレクトリに来たときはCの処理」という整理でよいのでは？
2. よくわからないが`F1`を高速連打しても動作に不具合はないように見える。
3. OK

#818
1. NG)親ディレクトリに移動したとき常にカーソルが一番上に現れてその位置のディレクトリ名が補完されている
  - '/'を省くなど補完の様子や`F1`での移動は問題ないので、移動直後のカーソル位置だけだと思われる。
2. OK
3. OK

#819
1. NG)親ディレクトリに移動したとき常にカーソルが一番上に現れてその位置のディレクトリ名が補完されている
  - ただし一度サブディレクトリに移動すると、sixを再起動するまでカーソル位置が「直前ディレクトリ」になる OK

#820
概ねOK。最上位の1つ手前までしか登れなくなったのがNG

#821
- Cドライブの場合) `F1`で"C:/Users"までしか登れない。NG
- wslパスの場合) 手入力で":e //wsl.localhost/Ubuntu/home/ymaru/"と入力してディレクトリ"ymaru"にいても`F1`を押下すると":e Ubuntu/home/"になってしまい、最終的に":e "が最上位になっている。
  - "wsl.localhost"が決め打ちのダミードメインだからという原因だと思うが、表示上は維持して"//wsl.localhost/"もしくは"//wsl.localhost/Ubuntu/"が最上位になればいい。

#822
OK。次は:e popupの仕様変更を。
- 入力欄に補完したあと文字列選択状態にするのは「popupを表示した初回のみ」という仕様だったが、これを改めて「親でも子でも階層を移動した直後」としたい。
  - popup表示した初回も維持。

#823
- NG。「今いたディレクトリ」の補完がされなくなった。移動前のディレクトリで文字列選択状態にしていた文字数を採用してしまっている。
期待動作例) "foo/bar/hoge/"の一覧がpopupに表示され"abc.txt"にカーソルがあるとき`F1`→"foo/bar/"の一覧が表示され"hoge/"にカーソルが当たり"hoge"が入力欄にappendされて"foo/bar/hoge"になり、補完した"hoge"が文字列選択状態になる
- サブディレクトリへの移動でも同じ挙動にする。
期待動作例) "foo/bar/"の一覧がpopupに表示されているとき、"hoge"に移動→"foo/bar/hoge/"の一覧が表示され先頭の候補、たとえば"abc.txt"にカーソルが当たり"abc.txt"が入力欄にappendされて"foo/bar/hoge/abc.txt"になり、補完した"abc.txt"が文字列選択状態になる

#824
NG
- NG)親ディレクトリへの移動時に「今いたディレクトリ」が入力欄に補完されていない。
期待動作例) "foo/bar/hoge/"の一覧がpopupに表示され"abc.txt"にカーソルがあるとき`F1`→"foo/bar/"の一覧が表示され"hoge/"にカーソルが当たり"hoge"が入力欄にappendされて"foo/bar/hoge"になり、補完した"hoge"が文字列選択状態になる
- NG)サブディレクトリへの移動時にカーソル位置の候補が入力欄に補完されていない。
期待動作例) "foo/bar/"の一覧がpopupに表示されているとき、"hoge"に移動→"foo/bar/hoge/"の一覧が表示され先頭の候補、たとえば"abc.txt"にカーソルが当たり"abc.txt"が入力欄にappendされて"foo/bar/hoge/abc.txt"になり、補完した"abc.txt"が文字列選択状態になる

#825
1.OK
2.OK
3.OK
- 親(または子)ディレクトリに移った直後、入力欄への補完が自動で文字列選択されている状態で文字入力したとき、マッチする候補があるのに"(loading...)"になって戻ってこない。
  - :e popupを開き直しても"(loading...)"のままだが、別タブに移って:e popupを出すと正常に一覧が表示され、元のタブに戻っても正常に戻っている。

#826
1."(loading...)"のまま(10秒以上)のときと即"(no entries)"になるときがある。条件不明だが必ずどちらかになる。
  - :e popupを開いた直後のまま親や子のディレクトリに移動していないときは問題ない

#827
"C:/Users/ymaru/WebView2/"から上に移動して"WebView2"が文字列選択状態のとき、"Favorites"を選ぶつもりで`F`とタイプすると入力欄が"C:/Users/"になる
- six起動直後の再現性は100%だと思うが、いつの間にか期待動作になっていたりする。

#828
1キー入力毎に5秒ほど待って試してもNG
ただし`Esc`でpopupを閉じて`:e `で開き直すところからやると2度目は素早く操作しても問題ない。
- ログ拡張してみて。

#829
選択状態からキー入力したときフィルタリング処理が正しくない現象は少なくとも1回目はOKだった。
- 1度親ディレクトリに移ってそこからサブディレクトリに移ると、それ以降親ディレクトリに移れなくなる。
  - 下記は起動直後の:e popupから`F1`→サブディレクトリに移動→`F1`のログ。
```
_six.js:13854 [six] boot done
_six.js:10711 [e-input-raw] {v: ':e', mode: 'CMD', prefillPrefix: null, prefillSeg: null, prefillAge: null, …}
_six.js:10711 [e-input-raw] {v: ':e ', mode: 'CMD', prefillPrefix: null, prefillSeg: null, prefillAge: null, …}
_six.js:10796 [e-input-e-branch-start] {raw: ':e ', typedDir: '', filter: ''}
_six.js:12165 [filePopup auto-retry] {startElapsed: 1763887654775, navElapsed: 2, retry: 0}
_six.js:616 [parentNav stub pending init]
_six.js:633 [parentNav stub fallback attempt] {inCmd: true, domVisible: true, dataKind: 'file'}
_six.js:691 [parentNav stub fallback done] {entries: 42, fbCount: 1, base: 'file:///C:/Users/ymaru/WebView2/'}
_six.js:12255 [e-prefill-check] {base: 'file:///C:/Users/ymaru/WebView2/', typedRaw: 'C:/Users/ymaru/WebView2/', filter: '', sel: 1, desired: 'six'}
_six.js:12262 [e-prefill-forcedName] {bestName: 'six', before: ':e C:/Users/ymaru/WebView2/'}
_six.js:12285 [e-prefill-meta-store] {prefix: 'C:/Users/ymaru/WebView2/', seg: 'six'}
_six.js:12286 [e-prefill-applied-forced] {value: ':e C:/Users/ymaru/WebView2/six'}
_six.js:685 [parentNav stub fallback done] {entries: 6}
_six.js:12883 [parentNav install real]
_six.js:12890 [parentNav auto-run pending]
_six.js:12815 [parentNav attempt] {mode: 'CMD', allow: true, popupVisible: true, domVisible: true, haveBase: true, …}
_six.js:12883 [parentNav install real]
_six.js:12255 [e-prefill-check] {base: 'file:///C:/Users/ymaru/WebView2/six/', typedRaw: 'C:/Users/ymaru/WebView2/six/', filter: '', sel: 0, desired: null}
_six.js:12323 [e-prefill-meta-store] {prefix: 'C:/Users/ymaru/WebView2/six/', seg: '.wv2-profile'}
_six.js:12324 [e-prefill-applied-list] {value: ':e C:/Users/ymaru/WebView2/six/.wv2-profile'}
_six.js:12815 [parentNav attempt] {mode: 'CMD', allow: true, popupVisible: true, domVisible: true, haveBase: true, …}
_six.js:12914 [parentNav raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
_six.js:12790 [parentNav real skip multi-fire]
_six.js:12914 [parentNav raw] {key: 'F1', alt: false, ctrl: false, shift: false, meta: false, …}
```
