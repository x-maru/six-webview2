#194
- 未編集時の:wで不要なジャンプは発生しなくなった。OK
- `:`と1文字入力した直後にEscで抜けると`G`と同じ挙動でEOFに飛んでしまう。NG
  - `:100`などでジャンプする際にも一瞬`G`と同じ挙動でEOFに飛んでしまう。NG
- `six.ps1 _six.js`は正常にオープンできるようになった。OK
- `:wq!`も対象はカレントバッファのみとする。

#195
- 不要なEOFへのジャンプは抑止された。OK
- 引数無しで起動した際のバッファで`G`で末尾に飛ぶと縦スクロールバーが2つ表示される。
  - "_six.js"などを開いて`G`しても発生しない
- 行番号ガターの行高が高すぎるようで、少しずつ差が開いて行400あたりでちょうど1行分ずれてしまう。(引数無しで起動した際のバッファに表示される内容だとわかりやすい。添付画像)

#196
- 縦スクロールバーが2つ表示される問題は発生しなくなった。OK
- 行番号ガターと本文とのずれの量は変わっていないように見える。行1でも揃っていないので行高だけでなく開始Y座標が違っているのでは？たぶん行高と両方だと思う。

#197
- 引数無し起動時のバッファで、ggの状態でtop座標も行高も揃っている。:368でも揃っている。だが:369とすると1行分近くずれる。
:368のときの表示範囲は[336,399]で、:369のときの表示範囲は[337,400]。EOFは行400。:368から:369としても即ずれるし、ずれた状態から:368とするとずれは解消する。

#198
- ズレは小さくなったが、まだ「:368ではズレないのに:369で急にズレる」症状は続いている(添付スクショ2枚)。

#199
変わらない。:368ではズレないのに:369で急にズレる。

#200
変わらない。:368ではズレないのに:369で添付スクショのようにズレる。

#201
- OK。行番号と本文とのズレは解消した。
- caret(赤)のY座標が本文行に対して少し上にずれている。行高と一致するようにしたい。

#202
- 引数無し起動時のバッファで、行400(EOF行)が見えていない間はcaretの描画は良い感じだが、行400が見えた途端に上方にずれる。

#203
- Caretの表示位置がずれなくなった。500%まで拡大してもずれない。OK。
- 次は、コマンド入力欄の履歴を辿れるようにしたい。HTA版と同じ仕様で、直前とまったく同じ場合は履歴に格納しない。遡った履歴を編集して実行もできるが編集した内容は最新履歴として追加され、過去履歴は更新しない。

#204
- :eの最後がclickだった場合に履歴に入らない (履歴に入ってほしい)
- :e の最後がEnterによる「補完&確定」だった場合に、補完前の文字列が履歴に入る (補完後の実際に実行された文字列が履歴に入ってほしい)
- :eより前の履歴を表示する術がないので、Alt-P/Alt-Nも↑/↓と同様、履歴の辿り/前進のキーとしたい
- :e ポップアップ中の↑↓で上下1行しか移動できなくなってしまった。

#205
- :e popup click/Enterで確定後のコマンド文字列が履歴に入るようになった (OK)
- :e popupでコマンド入力欄を編集するとpopup内の↑↓が効かなくなる
  - 最初にpopupから入力欄に保管されたファイル名をbackspaceで1文字削除しただけ(なのでpopup候補にマッチ継続)でも↑↓不可になる。
- やっぱりAlt-P / Alt-Nでの履歴移動は廃止する。代わりに↑↓の仕様を変更し、コマンド履歴を:eまで辿ってもすぐにはpopupを表示しないようにする。
  - 「popupが出ていない」かつ「コマンド入力欄の先頭が:e」のときにTabキーが押下されたらpopupを出す。
    - そのときのTabキーは「popupを出す」操作として消費される(同時に補完までは実施しない)
  - 「popupが出ていない」かつ「コマンド入力欄の先頭が:e」のときEnterキーが押下されたらファイルオープンとして:e動作を確定する。
- popupが出ていない状態なら引き続き↑↓での履歴移動は有効。

#206
- :e popupでコマンド入力欄を編集するとpopup内の↑↓が効かなくなる(元の位置から上下に1行だけが移動可能範囲になる)
- :e履歴を表示しただけではpopupを出さない、Tabでpopup、あたりの挙動はOK
- popup内でのPageUp/PageDownはOK
- 履歴内でのPageUp/PageDownは↑↓と同じ挙動＋何回か動かすと効かなくなる．．． が、そもそも履歴を辿る際のPageUp/PageDownは廃止でいい。

#207
- :e popup内での↑↓移動はOK
- 仕変だが、:e同様:bも履歴から表示したときは即popupは出さずTabが押されたらpopupを出すようにしたい。
- 履歴と関係ないが、:bのポップアップが出ているときにTabキーを押すとフォーカスがどこかに移ってしまうようだ。popupが開いてる間に限らずコマンド入力中はTabキーの通常の「フォーカスを切り替える」効果は抑止したい。

#208
:bの扱いも含めコマンド履歴対応はOK。
- 水平スクロールバーが出ているとき、EOF行が隠れて見えない。EOF行の下側に常に余白を持たせたい。本文1行分で十分。カーソルが乗ることはない領域なので行番号も不要で、textareaのpaddingなど何でもよい。

#209
縦スクロールバーで余白部分が見えるようにスクロールすればEOF行が丸ごと見えるようになるが、GでEOFに飛んだときは見えない。
- Gで飛んだときも余白まで見えるように表示範囲を調整してほしい。EOF行の下の余白は横スクロールバーに隠れてしまってよい。
- scrolloffの設定行数に足りずカーソル自体が下に移動し始めるとき、最初の1行分は「余白が見えるようにスクロールアップ」にしてほしい。EOF行の下の余白は横スクロールバーに隠れてしまってよい。

#210
EOF下の余白の実装完了。OK。
- 次は`f`や`b`などのカーソル移動系を実装したい。実装目標は「HTA版sixと同じ」になること。

#211
間違えた。`f`じゃなくて`w`だった。基本はVim準拠だがHTA版sixでVimの`f`は未実装だったはず。HTA版sixで実装してた移動系コマンドは「h,j,k,l,←,↓,↑,→,w,b,^,0,$,{,},gg,G」。ほとんどのコマンドは数字前置でカウントを指定できる(5lで右に5つなど)
日本語対応などHTA版sixの実装を参考にして移植して。

#212
- `w` / `b`: トークンの定義がHTA版sixと異なっている (NG)
- `G`直後の`b`でカーソルが動くときと動かないときがある。間が空くと駄目か？
- `{` / `}`: OK (HTA版sixはバグが残っているので合わせなくてよい)
- 他、数字前置などの挙動はOK

#213
- `G`直後の`b`はOK
- `w` / `b`トークンの定義が直ってない。
- 「これにより、記号が連続していても w/b は1文字ずつ刻んで進む/戻るようになります（HTA 版の感覚に寄せる）」
  - 逆。ちゃんとHTA版sixのソースを参照して。

#214
HTA版 sixの w/b でのトークンの定義をまとめて。
https://github.com/x-maru/six.git

#215
OK。
次は削除系の編集コマンドを実装しよう。挙動のお手本はHTA版six。
- x, d+モーション, dd, dgg, dG

#216
- x : OK
- dw, db : OK
- d0, d^ : OK
- d$ : NG 削除されない
- d{, d} : NG 削除されない
- dh, dl : OK
- dj, dk : たぶんOK (よくわからない。。まず使わないコマンドなのでよしとしておく)
- dgg, dG : NG 削除されない
- dd : OK

#217
改善されず。
- d$ : NG 削除されない
ーd{, d} : NG 削除されない
- dgg, dG : NG 削除されない

#218
- d$ : NG 削除されない
ーd{, d} : NG 削除されない
- dgg: OK
- dG : NG 削除されない

#219
IMEはずっとOFFのまま。
- d$ : NG 削除されない ($ : Shift+4)
ーd{, d} : NG 削除されない ({ : Shift+[, } : Shift+])
- dG : NG 削除されない (G : Shift+g)

#220
いずれも改善せず。
dのあとのタイムアウトを廃止する。キーコードは見ず、`$`の文字コードで判断する。
(そもそもJISキーボード専用には作っていない。ASCIIキーボードも対象である)

#221
いずれも改善せず。
キー入力は検知しているようで、削除すべき範囲の末尾にcaretが移動する。
- `d$`としたときに行末にcaretが移動する(が何も削除されない)など。

#222
OK.
次はUndo / Redoを入れよう。

