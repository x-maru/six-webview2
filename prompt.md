#284
OK。
次は:sの実装を。実装範囲や細部の仕様はHTA版sixにあわせる。

#285
- "  return $null"という行の先頭で`:s/r`と入力すると正しく"return"の先頭の'r'がハイライトされるが、'e'とタイプして`:s/re`になると数行先の"foreach..."の"re"に飛んでしまう。
- 確認ダイアログをQuit(q)で抜けたときコマンド入力欄にフォーカスが当たってしまう。本文textareaに当たるべき。
- "Replace this match?"の前行に"7 matches left."みたいに表示してほしい。数字は「今Allを実行したらいくつ対象になるか」を表す。

#286
- 確認モーダルを閉じたときのフォーカス処理はOK
- 例えば"function"にマッチする箇所が行2,5,10,15にあったとして、`gg`直後に`:%s/function`と入力すると最初の"f"で行2、次の"fu"で行5、その後"fun"で行10、"func"で行15というように1文字ずつ次候補に移ってしまう。
 
#287
「次に着手できる候補」は全部あと回し。先にバグを潰して基本動作を正常化しないと他の動作試験もできない。
- テキストの状態例）
  - 行2：function
  - 行3：funny
  - 行4：function
  - というとき、`gg`,`:%s/f`で行2の"f"がハイライトされ、`:%s/fu`で行3の"fu"がハイライトされ、`:%s/fun`で行4の"fun"がハイライトされる。これは正常動作ではない。

#288
- 置換対象が正しく選択できない現象は解消した。OK
- 例えば4 matches状態で最初の候補をNo(n)としたら2つ目で関係のない行がハイライトされる。
  - 空行なのに検索語の幅でハイライトされている
- 確認モーダルを出す瞬間にscrolloffの処理なのか、スクロールが発生するのを抑止したい。
- 確認のモーダルダイアログをドラッグ可能にできないか？

#289
- 行36に"function"があり、caretが行35にあるときに`:%s/function/x/gc`で確認モーダルが出た瞬間にスクロールが発生して空白文字列がハイライト表示される
  - 表示範囲[308,355]で行333にcaretとハイライトがある
    - caretとハイライトはスクロール前のものがそのまま残留していると思われる

#290
- 「確認ポイント」
  - 「確認モーダルが出る瞬間にスクロールしないこと」→NG 表示範囲[308,355]
  - 「プレビュー矩形が空行など関係ない行に表示されないこと」 →OK?（該当マッチ位置が画面外になってしまい確認不能）
  - モーダルのドラッグ →OK

#291
改善せず。表示範囲[11,59]でcaretが行35にあり、行36の"function"にマッチするはずのところ表示範囲[308,355]になってしまう。

#292
モーダル出現時に表示範囲が狂う件は解消した。4 matchesでNoで次に進んでも正常。OK
- "Substitute"→"Substitute(置換)"とし、その右に入力されたコマンドを表示してほしい。例）"Substitute(置換)  :%s/function/x/gc"
- HTA版sixに準じて確認モーダル内にUndo(u)を追加してほしい。

#293
- 確認ダイアログ内でのUndoの仕様が異なる。置換対象が複数あって2つ目以降が対象になっているときに1つ前の'y'/'n'を取り消してそこに戻る、という動作。
- "Substitute(置換)"と入力したコマンドの間を4remほど空けてほしい。

#294
概ねOK。
- 置換候補1つ目のときはUndo(u)ボタンはdisable状態でグレー表示にして。
- 1つも置換しなかったときのトーストが"no match"になっているが、matchしてないわけではないので"replaced: 0"でいい。

#295
挙動はOK。disable状態のUndoボタンでは文字色をgrayにして。

#296
OK。
- `:e!`って実装してなかったっけ？ 実装したい。「:e! ファイル名」は不要。現バッファの変更を破棄して読み込み直すだけでいい。
- :e popupを実装したときは`:e`では開かず`:e `とスペースを入れたところで初めてpopupが開く仕様だったはずだが、いつのまにか`:e`だけで開くようになっている。不具合とも言えないが、今後他のコマンドも増えるだろうしやはり`:e `まで入力したところで初めてpopupを出す仕様に戻そう。

#297
NG。`:e!`で元バッファは何の変化もなく新たに「!」という名前のバッファが生成されてしまう。

#298
変わらず。NG。「!」というバッファができてしまう。
また`:e`だけで空白を入れていないのにpopupが開くようになってしまった。NG。

#299
OK。
次はVISUALモードで選択中の範囲を対象とした:sを。

#300
VISUALモード選択中に`:`を入力しても何も起きないが？

#301
- VISUALモード選択中に`:`を入力したら「:'<,'>」が自動挿入されるべき。
- VISUALモード選択中に`:`を入力しても選択ハイライトはクリアされないように。

#302
- `:`入力でVISUAL選択のハイライトが消える
  - 構わず'<, '>s/foo/bar/g を実行するとヒットするのにreplaced:0になる
- 逆にEscでコマンド入力を抜けるとVISUAL選択のハイライトが復活してしまう。リセットされるべき。

#303
- `:`入力でVISUAL選択のハイライトが消える
  - 構わず'<, '>s/foo/bar/g を実行すると選択していた範囲内で正しく全置換された。なのでハイライトが見えないだけで効いてはいるようだ。
- Escでコマンド入力を抜けてもVISUAL選択のハイライトは復活しない。OK。

#304
- VISUAL選択→「:」→色が変わるが選択範囲は見えている。OK。
  - : 後の選択範囲の色もTHEMEで指定できるようにして。
- 「:'<,'>s/old/new/gc の確認モーダル操作時にも選択が視認できるか」→NG。モーダルが出ると消える。
  - ただしインクリメンタル・プレビューの矩形は見えている
- 「EscでCMDを抜けた後、選択が復活しないか」→NG。: 後の選択範囲の色がそのまま残る。離れた箇所から`v`でVISUALモードに入るとそこで新たにVISUAL選択のハイライトが表示される。
- 「:'<,'>s/old」の入力中のインクリメンタルサーチがVISUAL選択範囲外でマッチする(caretもそちらに飛ぶ)ただし置換を実行するとVISUAL選択範囲内だけで実行される

#305
- 選択状態の描画はOK。
- 確認モーダル上のUndoで1つ前のy/nを戻せるが、戻すだけでなく「次の置換候補」も戻したいのに戻っていない。
- VISUALでも%(全体)でも、置換対象が3つ以上になるケースでオプション"c"を付け、y,y,u,qで抜けたあとuを押してもUndoが効かない。
 
#306
- 「全体置換で c フラグ付き:候補1: y、候補2: y、候補3: u → 候補2に戻る（ハイライトも戻る）さらに u → 候補1に戻る（Undoボタンは最初の候補では無効表示）」→OK
- 「VISUALでも%(全体)でも、置換対象が3つ以上になるケースでオプション"c"を付け、y,y,u,qで抜けたあとuを押してもUndoが効かない。」→NG。改善されず
- 引数無しで起動した直後、:%s/quick/x/ Enterとしたら「4, 18, 32, 45, 59, 73, 87,...」行目の"quick"が置換されずに残る。
- 引数無しで起動した直後、:s/quick/x/ Enterとしたあとscrolloffによるスクロールが発生する。置換終了直後の1回はスクロールしないでほしい。

#307
- 「VISUALでも%(全体)でも、置換対象が3つ以上になるケースでオプション"c"を付け、y,y,u,qで抜けたあとuを押してもUndoが効かない。」→NG。改善されず
- 『引数無しで起動した直後、:%s/quick/x/ Enterとしたら「4, 18, 32, 45, 59, 73, 87,...」行目の"quick"が置換されずに残る。』→未置換が残る現象は解消したが、"Replaced: 400"のあとcaretが行間の中途半端な位置に表示されキー入力を受け付けない(caret位置が正しくて中途半端なスクロールをしているのかも。添付スクショ) ←%を付けずに実行しても同じ。

#308
改善していない。一つずつ潰していこう。
再現手順：
- 引数無しでsix.ps1を起動
- :%s/quick/x/c Enter
  - 確認モーダルで y, y, u, q
  - 確認モーダルが閉じた直後に u →NG。何も起きない

#309
y,y,u,qで抜けたあとのuが効かないという現象は改善していない。
よりシンプルな例で修正・確認しよう。
- 行の内容が"x   aaa  aaa  aaa "で、caretが先頭xの位置にあるときに`:s/aaa/b/gc`で確認モーダルが出たときに`y`,`y`と入力したときの行内容は"x   b b aaa "になる。(期待動作)
- 続けて`u`と入力すると"x   b aaa aaa "となる。ひとつめの"aaa"がハイライトされている状態。Undoボタンはまだenable。(期待動作)
- 続けて`u`と入力しても行内容もハイライトも変化しない (NG)
- その状態で`q`で確認モーダルを抜けたあと、undoが効かない。

#310
- "x   aaa  aaa  aaa "の'x'にcaretを置いて`:s/aaa/b/gc`で確認モーダルを開いた場合：
  - モーダル内のUndoの挙動はOK。
  - `y`,`y`,`u`,`q`で抜けたあとの`u`で"x   b  aaa aaa "までしか戻らない症状は変わらず。NG
  - 確認モーダルで最後の候補を`y`で置換して抜けると、直後に`u`が効かない。続けて`u`とすると正しくUndoされる。最後の候補を`n`で抜けていた場合には直後から`u`が有効。
  - 確認モーダルをAllで抜けたときINSERTモードになってしまう

#311
引数無しで起動した直後(*1)から：
- :10, O, "x   aaa  aaa  aaa ", Esc, :w foo.txt とした(*2)あと、
  - `0`で'x'にcaretを置いて`:s/aaa/b/gc`で確認モーダルを開く
  - `y`,`y`,`u`,`q`で抜けたあと`u` → (*1)の状態まで戻ってしまう。タブバーの*は消え、:qで確認無しに終了する
    - 再度sixを起動してfoo.txtを開くと(2)の状態になっている

- Allで抜けたあとINSERTになってしまう症状は解消した。OK。

#312
- y,y,u,q,uで起動直後の状態まで戻ってしまう症状は解消していない。NG。ただしタブバーの*は残っている。続けて`u`を入力しても何も起きないが。

#313
OK。:sでの不具合は解消した。
- :sのオプションでnも実装したい。
- 実装前のnもだが、不正なオプションがあっても無視するだけの挙動になっているが、エラーを示したうえで置換は実行しない方がよい。

#314
- nオプションでも実際に置換されてしまっている。NG
- xオプションでも置換が実行されてしまう。
- nのメッセージはHTA版sixにあわせて(というか元はVimにあわせて)"X matches on Y lines"にしよう。

#315
- nオプションなのに置換が実行される挙動は解消した。OK
- nオプションのメッセージが"replaced: N"になってしまっている。NG
- VISUALモード時もnオプションで置換されることはなく正しくカウントされている。OK
- 無効なオプションをはじくようになったのはよいが、Nオプションは単に無視するだけで置換が実行されてしまっている。大文字/小文字も厳密に判定してNなどは不正オプション扱いとしてほしい。

#316
OK。nオプションの実装完了。
次はcを実装したい。ch,cw,c$など、HTA版sixで実装したものをそのまま持ってきたい。

#317
(HTA版sixではcは未実装だったね)
今回の実装に不具合はないようだが、いくつか変更したい。
- dオペレーションに沿った実装とのことだけど、Vimではdwとcwで削除する範囲が異なっているようだ。
  - "abc ="の'a'にcaretがあるときdwでは"abc "が削除されて"="になるがcwでは"abc"だけ削除されて" ="になる(Vim)。
    - sixも同じ挙動にしたい。
- VimではVISUALモードで選択中のときにcとタイプすると即座に選択範囲が削除されてINSERTモードに移行するようだ。
  - sixも同じ挙動にしたい。

#318
OK。次はNcwの挙動をVimにあわせたい。2cw, 3cwなど。

#319
OK。次はcW,cBを追加しよう。dやvなど語単位のw,b指定ができるところは同様にW,Bを追加。

#320
OK。C/Wの実装完了。
次は:helpを実装したい。Vimとはかなり違う独自仕様で、HTA版sixをベースに新たに作っていきたい。
- まずは:helpでヘルプウインドウを表示する。
  - ヘルプウインドウはモーダル。閉じるまで他の操作はできない。
  - ウインドウ内はタブ表示で、タブは[全体][コマンド][NORMAL][INSERT][VISUAL]とする。
    - [全体]タブには起動時オプションくらい。
    - ヘルプウインドウを開いた時は常に[コマンド]タブが開くようにする。
  - ヘルプバー内のタブバーの下部分はスクロール対象。
  - タブエリアの下(最下行)はスクロール対象外の1行を確保し、ヘルプウインドウ内の操作方法を表示する(j/k/↓/↑/SPACE/Shift+SPACE/gg/G/Tab/Shift+Tab)。SPACEは↓と同じ。Shift+SPACEは↑と同じ。
    - 操作方法行の右端にクリッカブルなボタンを用意する。[閉じる(Esc)]
    - Escでヘルプウインドウを閉じるが、ボタンに表示しているので操作方法の箇所には記載しない。

#321
- 最下行の操作方法・ボタンの下に余白があるのを無くしたい
- ヘルプ本文と操作方法1行内のキー入力部分は<kbd>:e</kbd>みたいにして。
- 色をTHEMEで指定できるようにして。
  - 選択タブの文字・背景、非選択タブの文字・背景
  - モーダル全体の背景
- [閉じる(Esc)]のボタンはUnsaved changesダイアログの[Save]と同じ色・形(角丸)がいい。

#322
- :help実行時、本文textareaで一瞬`G`の動きをした直後にscrolloffに合わせた位置にスクロールしている。
  - :help時のスクロール一切を無くして。
- ヘルプダイアログで`Tab` / `Shift Tab`でタブを切り替えると移動前のタブにキーボードフォーカスの枠が取り残される。NG。枠不要。
- 色をTHEMEで、というのは_six-theme.jsで定義しているwindow.THEMEに追加して、という意味だった。追加して。
- [閉じる(Esc)]のボタンがUnsaved changesダイアログの[Save]と同じ色・形(角丸)になっていない。NG
- <kbd>のstyleはHTA版(下記)を踏襲して。
```
#help kbd {
    background: rgb(95,143,223);
    color: #000;
    border-radius: 0.18rem;
    padding: 0 0.22rem;
  }
```
- "ヘルプダイアログ"と呼ぶのが一番しっくりくるが、変？ウインドウ？モーダル？

#323
- `:help`時に`G`の挙動で末尾を表示した状態のままヘルプダイアログが開いている。NG
- ヘルプダイアログ内で、
  - アクティブタブの文字を色以外に属性を変えてる？boldっぽく見えるが。タブサイズが若干揺れて違和感があるので文字色だけにして。
  - Escで閉じたあと、キーボードフォーカスが失われる。
    - 本文textareaをクリックしてもフォーカスが当たらない
    - コマンド入力欄をクリックすると文字入力・編集は可能だがEnterを押しても無視される
- アプリウインドウのクローズボタンで閉じてもsix.ps1プロセスが終了しない
- ヘルプダイアログ内の文字全部を1段階大きくして。

#324
- ヘルプダイアログを開く際に不要な本文スクロールが発生していた件は解消した。OK
- ヘルプダイアログ内で、
  - タブ文字boldの件は解消した。OK
  - タブ本文の部分をクリックしたあと何かキーを押すとタブ領域の周囲にフォーカスを表すような枠が表示される。NG
  - ボタンでも関係ない箇所でも、一度でもマウスでクリックするとダイアログを閉じた時点でGの挙動で末尾に飛ぶ。NG
  - 「閉じる(Esc)」ボタンの色もwindow.THEMEで指定したい。
- 次はタブ内のヘルプ文言を書いて。抜粋とかではなく漏れのないように。
  - 全体タブの後半は「変更可能なパラメータの初期値」をまとめて。scrolloffの値とか。

#325
- タブ本文の部分をクリックしたあと何かキーを押すとタブ領域の周囲にフォーカスを表すような枠が表示される。
  - →OK。修正された
- ボタンでも関係ない箇所でも、一度でもマウスでクリックするとダイアログを閉じた時点でGの挙動で末尾に飛ぶ。NG
- ヘルプ本文[全体]
  - 1行目の「全体」は「起動後に変更可能なパラメータの初期値」と「ウインドウ」に分けよう
    - 起動後にk変更可能なパラメータの初期値
      - スクロールオフは"scrolloff = 3"にしよう
      - 検索ハイライトはOK
    - ウインドウ
      - 「エディタ拡大率はCtrl+ホイールで変更可能で、前回終了時の拡大率を維持」にしよう
      - 1行の文字数：110 (半角)
      - 行数：32
      - キャレット太さは要らない（固定扱い）
  - 「基本操作」は不要。「NORMAL」タブに詳細を書く。
  - 「コマンドの例」は不要。「コマンド」タブに詳細を書く。
- ヘルプ本文[コマンド]を詳細に書いてくれる？

#326
- 動作はOK。
- ヘルプ本文[コマンド]を詳細に書いてくれる？

#327
- ヘルプ本文領域で`gg`は効くが`G`が効かない。NG
- ヘルプ本文[コマンド]
  - 先頭の「コマンド」は不要なので削って。
  - 下記のフォントサイズを1段階大きくしてboldにして。
    - 「置換」「読み込み」「保存・終了」「検索・ハイライト」「表示」「その他」
- `:reload`なんて実装してた？:e!と同じ？ Vimにreloadなんてコマンドないし、使わないので廃止して。
- キーバインドとして表示しない「隠し機能」として下記を実装して。
  - `q`, `Q`, `F1`：ヘルプダイアログを閉じる
  - `Ctrl+I`, `Ctrl+Shift+I`をそれぞれ`Tab`, `Shift+Tab`と同等に。

#328
- ヘルプ本文[コマンド]
- 「置換」セクション以下の下記セクションは「サブセクション」なので、インデントを1つ下げて。
  - 「書式」「フラグ」「確認モーダル操作」「メッセージ」「エラー」「例」
- ヘルプ本文[NORMAL]
  - [コマンド]同様に詳しく書いて。

#329
- ヘルプ本文[コマンド]
  - "ジャンプ","検索ハイライト"のセクションを追加したので、他のセクションと同じ文字装飾にして(フォントサイズ、ウェイト)。
- ヘルプ本文[NORMAL]
  - 先頭の「NORMAL」は不要なので削って。
  - 移動sectionに`{`と`}`を追加して。
  - オペレータsectionの"d / y c"の次行でインデントを下げてモーションを全種類記述して。

#330
ヘルプダイアログは随時書き足していくと思うが一旦OK。
次はオーバーレイパレットを実装する。
- 本文textarea右下に重なるオーバーレイ。オーバーレイの中にボタンを複数配置する。
  - 最初はオーバーレイ右下に【ヘルプ^F1,Esc】というボタンを設ける。'^'は改行を表す。つまりボタン内の上段が"ヘルプ"で下段が"F1,Esc"となる。
  - click, Esc, F1でヘルプダイアログを開く(Escは既存,clickとF1を新設)。

#331
間違えた。"F1,Esc"じゃなくて"F1,:help"だった。(既に直した)
- オーバーレイパレットにボタンを1つ追加する：
  - 【検索ハイライト^OFF ON】
  - 表示位置はヘルプボタンの左
  - hlsearchのON/OFFをリアルタイムに反映
    - "OFF"の背景をグレー、"ON"の背景を明るい緑で描画
  - clickは`:set hlsearch!`と同じ挙動

#332
hlsearch状態の反映・変更の挙動はOK。
- 検索ハイライトボタンをclickしたあとキーボードフォーカスが変更されてしまう。click前のフォーカスを維持してほしい。
- オーバーレイパレットの表示位置として0.5remほど上に、0.5remほど左にずらして。
- オーバーレイパレットを塗りつぶしにしよう。角丸淵なしでrgba(0,15,0,0.3)で。
- ボタン間の隙間を今の半分くらいにしたい。

#333
- six.ps1起動後に_six.htmlと同じディレクトリに置いたTODO.mdが:e popupの一覧に現れない。NG
  - `:e TODO.md`で正常に開けるが、その後に再度:e popupを出しても一覧に含まれていない。
- 本文textareaをclickしてもcaretが移動しない。NG
  - clickだけでなくドラッグして1文字でも選択するとcaretが移動する

#334
- six.ps1起動後に_six.htmlと同じディレクトリに置いたTODO.mdが:e popupの一覧に現れない。NG
  - `:e TODO.md`で正常に開けるが、その後に再度:e popupを出しても一覧に含まれていない。
- 本文textareaをclickしてもcaretが移動しない。
  - 修正された。OK。

#335
:e popupのリフレッシュの件はOK。
- :e でpopupを開いたとき、カレントバッファのパスが基準になっていない。_six.htmlの場所だと思われる。NG
  - 以前は正しく動作していたと思うが。。

#336
色々:eの動作が壊れているようだ。
- c:\Users\ymaru\WebView2\six\six.ps1 を引数無しで起動
  - :e popupの一番上の"../"をclick → 一覧中央あたりの"six.ps1"が選択される NG

#337
変化なし。
- c:\Users\ymaru\WebView2\six\six.ps1 を引数無しで起動
  - :e popupの一番上の"../"をclick → 一覧中央あたりの"six.ps1"が選択される NG

#338
変化なし。
- c:\Users\ymaru\WebView2\six\six.ps1 を引数無しで起動
  - :e popupの一番上の"../"をclick → 一覧中央あたりの"six.ps1"が選択される NG
  - popup内でカーソルを動かすと動的に入力欄に反映されるが、"../"にカーソルを持って行っても反映されない(ひとつ下の候補になったまま)。
    - なのでpopup内で"../"にカーソルを持って行ってEnterを押してもひとつ下の候補が選択される。
    - 入力欄で手入力で"../"を加えてEnterを押すとpopupは親ディレクトリ内の一覧に更新されるが入力欄は".../WebView2/../Downloads/"などと単純な文字列連結された状態になる。

#339
- c:\Users\ymaru\WebView2\six\six.ps1 を引数無しで起動
  - :e popupの一番上の"../"をclick → 一覧中央あたりの"six.ps1"が選択される NG
    - 症状は変わらないが、改めて観察すると、入力欄の文字列は正しく更新されているっぽい
      - → 親ディレクトリに移ったあとpopupとしては「今いたディレクトリ」を選択状態にする→入力欄は".../WebView2/six"になる(OK) → なのにpopupの一覧が".../WebView2/six/*"のまま更新されない(親ディレクトリ内での"six/"の位置が"six/six.ps1"の位置だったというだけ)。

  - popup内でカーソルを動かすと動的に入力欄に反映されるが、"../"にカーソルを持って行っても反映されない(ひとつ下の候補になったまま)。
    - →入力欄に反映されるようになった。
    - `:e `まで入力してpopupが開いた直後はpopup内では"../"にカーソルがあるのに入力欄は":e "のままとなっているが、これはわざとなんだっけ？というか、こうしないと困るケースがあったんだっけ？ 特にないなら、popupを開いた直後から「popupから入力欄への反映」が働いて即座に入力欄が":e .."になっても良いと思う。

#340
- :e popupが出て最初の一覧で「不在ファイルのときの赤っぽいカーソル」になっている。
  - 上下に動かして選択を変えても赤っぽいカーソルのまま。
  - 一度親かサブのディレクトリに移動すると通常カーソルになる
- popupで"../"にカーソルを持って行ったとき入力欄が".../foo/bar/.."になるときと".../foo/bar"になるときがある。法則性がよくわからない。どちらでもよいが統一されないのは気持ち悪い。
- 再現条件が不明だがpopup表示が明らかに変になることがある。カーソルが2つ描画されたり．．．(添付スクショ)
  - この状態になったらEsc→:e と何度やっても再現する
  - カーソルを1行下に動かしたら正常なpopup表示になったが、そのまま選択せずにEscで閉じてすぐ:e で開くと再発する
  - なお発生したときのパスは"C:/Users/ymaru/.android/"
- "C:/Users/ymaru/WebView2/six/six.ps1"を引数無しで起動した直後、`:e `で開いたpopupから"../"を2回選択して2つ上に移動すると"C:/Users/ymaru/WebView2/ymaru"という名前の空バッファとして開いてしまう。

#341
- "C:/Users/ymaru/WebView2/six/six.ps1"を引数無しで起動した直後：
  - 最初に開く:e popup → カーソル色や入力欄への".."追加 → OK
  - "../" Enter → OK → "../" にカーソルを合わせる → 入力欄が".../Users/ymaru" →NG (".../Users/ymaru/.."になるようにしたのでは？)
    - 入力欄が".../ymaru"でEnterを押すと空バッファになる。
    - Enterではなく入力欄で"/"をappendしてやると「popupは"../"にカーソルがあるが入力欄は".../Users/ymaru/"という状態になり、ここでEnterを押すと"(loading...)"になったままになる(1分待っても変わらない)。
  - 最初からG状態(表示範囲[353,400])
- popup内のカーソルが2つ描画されるような現象は確認できていない (検証十分とは言えないが)

#342
- "C:/Users/ymaru/WebView2/six/six.ps1"を引数無しで起動した直後：
  - 最初の一覧で赤っぽいカーソルになる件は修正された。OK
  - popup内で"../"にカーソルを持って行ったときに入力欄は".../foo/.."になる →OK
  - popup内の"../"をclick → "(loading...)"のまま1分待っても変化しない NG
    - 初回の:eから再現率100%。Escで抜けて再度`:e `とするとpopupは"********"だけになる
- popup内のカーソルが2つ描画されるような現象は確認できていない (検証十分とは言えないが)

#343
- `e: `と入力しただけで`e: ..`とならないようにしてた理由を思い出した。`:e //wsl...`と入力する際に毎回コマンドラインを編集するのが面倒だからだった。
  - popup出現直後だけでなく入力欄で`Backspace`などで".."を消して`:e `になった瞬間にも`:e ..`に戻されてしまう。NG
  - caretを".."の前に持ってきて"//"と挿入してから".."を削れば入力できるが、そんな仕様は嫌だ。
- 一度`:b`のpopupを出すと、その後`:e `としても`:b`のpopupが出てしまうことがある。発生条件は不明だが一度発生するとずっとそのままのようだ。

#344
- :e popup内でディレクトリ移動した直後(=一覧を走査する対象が変わった直後)だけ「popup内候補を入力欄に反映する処理」をskipするようにしよう。
  - `:e `でpopupを開いたときもだし、その後サブディレクトリや"../"に移動したときも。
  - その状態でEnterを押しても何もしない(入力欄が"e: "か"e: .../"の状態なので)
  - Tabによる補完は有効。

#345
- `:e `でpopupが開いた直後(カーソルは"../")：
  - 入力欄に".."をappendしない →OK
  - Tab押下で入力欄に".."をappendする →NG
  - Enter押下は無視 →NG
  - 手入力で`.`と入力したらpopup内で"../"にマッチせず".edge-profile/"などにマッチする →NG
- popupを開いてからサブディレクトリに移ったあとカーソルが"../"に来ると入力欄に".."がappendされるが、続けて`/`と手入力しても"...foo/../"になるだけでpopupが変化しない(親ディレクトリに移動しない) →NG

#346
- `:e `でpopupが開いた直後(カーソルは"../")：
  - 入力欄に".."をappendしない →OK
  - Tab押下で入力欄に".."をappendする →OK
  - Enter押下は無視 →NG
    - popupが"(loading...)"になったまま1分経っても変わらない
    - Tabで".."をappendしたあと`/`とタイプした場合や、一度カーソルを下に動かして再度"../"に合わせた場合のEnterでは正常に親ディレクトリの一覧が瞬時に出るので、FileSystem等からの取得に時間がかかるという話ではないはず。
  - 手入力で`.`と入力したらpopup内で"../"にマッチせず".edge-profile/"などにマッチする →改善した。OK

#347
- #344の「その状態でEnterを押しても何もしない(入力欄が"e: "か"e: .../"の状態なので)」という仕様どおりになっていない。
  - サブディレクトリ移動直後でpopup上は"../"にカーソルがあるが入力欄への反映はしていない状態。
- `:e `直後のpopupで"../"から下にカーソルを移動して再度"../"にカーソルを合わせた状態でEnterだと正常に移動するがclickだと"(loading...)"のままになってしまう。→NG

#348
下記の仕様に沿っていない。NG。
- 仕様「:e popup内で新たに一覧の走査が完了したタイミングではpopup内候補を入力欄に反映しない」
- 仕様「入力欄が空白またはパス区切り文字('/'および'\'および':')のときEnterを押しても何もしない」

#349
- 「:e popup内で新たに一覧の走査が完了したタイミングではpopup内候補を入力欄に反映しない」という仕様に沿った動きになっているが、親ディレクトリに移動したタイミングでは入力欄の".../foo/.."という文字列を".../foo"に更新する処理は必要(今は抜けている)。
  - Enterの代わりに`/`を手入力した際には期待動作になっている

#350
- 「Enter/Tab/パス区切り文字入力」によるフォルダ移動 →OK
- popup内のディレクトリ候補("../"やサブディレクトリ)をclick → "(loading...)"のまま1分待っても変化なし →NG
  - キー操作では即座に移動できるのでFileSystemの反応等は関係ないと思われる

#351
- :e popupの挙動は修正されたようだ。OK
- yyが動作しなくなってしまっているようなので、修正したい。

#352
yy他yank動作OK。ヘルプダイアログも既に「y モーション」として書いてあるので追記不要。
次はVISUALモード中の挙動を修正したい。
- vでもVでも「EOF方向へのモーションが1回分しか効かない」状態に見える。`j`, `l`, `w`,`W`,`}`など。いずれもNG。
  - 「ファイル先頭方向へのモーション」は問題ないように見える。`k`, `h`, `b`, `B`, `{`など。

#353
VISUALモード中の移動の修正 →OK
次はタブ切替時の挙動を修正したい。
- タブAから他タブに移って再びタブAに戻って来たとき：
  - 先頭行が常に行1になってしまう → 移動前の表示範囲(先頭行)を再現したい
  - caret位置(行,桁)が常に(1,1)になってしまう → 移動前の位置を再現したい

#354
- タブ毎にcaret位置は再現されるようだが表示範囲(先頭行)が再現されないので、caretが表示範囲外になって見えなかったりする。
  - `j`,`k`などで行が変わる操作をすると表示範囲が更新されてcaret位置の行番号が正しいことは確認できる。

#355
VS Codeのクラッシュ・再開などでprompt番号が飛ぶことがあるが、指定したprompt番号より前は実装済み前提でよい。
- 表示範囲とcaret位置を憶えているのは最後に読み込んだバッファのみになっている (NG)
  - 引数無しで起動したときの"①(untitled)"と`:e`で開いたバッファ②の2バッファのとき、②の表示範囲とcaretだけ再現される。
  - さらに`:e`でバッファ③を開くとバッファ②の表示範囲とcaretは先頭行に毎回リセットされてしまい、③の表示範囲とcaretだけ再現されるようになる。

#356
- caret位置は全バッファ保存されるようになった。OK
- 表示範囲は最後のバッファしか覚えていない。
- caretが中抜き表示にしかならず、マウスカーソルが消えなくなった。

#357
- 表示範囲は最後のバッファしか再現されない。
  - 他のバッファも再現はされていて、直後に`G`の動作でスクロールしているのかもしれない(毎回Gの画面になる)。
- caretとマウスカーソルを排他的に表示・非表示を切り替える挙動は修正された。OK
  - アプリウインドウが非アクティブからアクティブになったときcaretをアクティブにすることはできないか？

#358
- 表示範囲が最後のバッファしか再現されない現象は解消していないが、他バッファで改めて`G`,`gg`,`:50`などと
表示範囲を何度か変更すると、最終的に全バッファとも表示範囲が再現されるようになった。
  - 初回だけ表示範囲の記憶に失敗しているかのような挙動？
- アプリウインドウが非アクティブからアクティブになったときcaretが塗り潰し描画されるようになった。OK。

#359
- 変わらない。
  - ① (untitled) → :30
  - ② :e foo.txt → :40
  - ③ :e bar.txt → :50
  - ここで①～③を何度切り替えても①と②はEOF画面、③は:50時の表示範囲
  - 続けて①で:30, ②で:40
  - ①～③を何度切り替えてもそれぞれ:30,:40,:50の画面が正しく再現される。
- caretの塗り潰し・中抜き表示はOK

$360
- 変わらない。
  - ① (untitled) → :30
  - ② :e foo.txt → :40
  - ③ :e bar.txt → :50
  - ここで①～③を何度切り替えても①と②はEOF画面、③は:50時の表示範囲

$361
タブ切替時の画面再現は修正された。OK。
- 本文textareaで"、、"と読点を2つ並べた上にcaretを持って行くと"、、"で1文字分になり、その後ろに半角1文字分の空白があるかのようなcaret描画になる

#362
- `、`が2連続してもcaretの幅が変わらない。`、、`実際にtextarea上で2文字目が左に寄っているように見える(そういうもの？)。
  - 続く「`」の位置のcaretは右にずれて幅が異様に細い。
    - `、`　　`、、`WMWMWMWM

#363
変わらない。#362のスクショのまんま。表示倍率は100%。

#364
50%から300%まで全表示倍率で下記文字列の上でcaretが正常に描画された。OK
`、、`  `。。`  `、、`  `，，`  `．．`
- 次は下記の2点に対応したい。
  - eof行下に設けている余白行を1行→5行に増やす
  - Ctrl-Zでウインドウ最小化
