#284
OK。
次は:sの実装を。実装範囲や細部の仕様はHTA版sixにあわせる。

#285
- "  return $null"という行の先頭で`:s/r`と入力すると正しく"return"の先頭の'r'がハイライトされるが、'e'とタイプして`:s/re`になると数行先の"foreach..."の"re"に飛んでしまう。
- 確認ダイアログをQuit(q)で抜けたときコマンド入力欄にフォーカスが当たってしまう。本文textareaに当たるべき。
- "Replace this match?"の前行に"7 matches left."みたいに表示してほしい。数字は「今Allを実行したらいくつ対象になるか」を表す。

#286
- 確認モーダルを閉じたときのフォーカス処理はOK
- 例えば"function"にマッチする箇所が行2,5,10,15にあったとして、`gg`直後に`:%s/function`と入力すると最初の"f"で行2、次の"fu"で行5、その後"fun"で行10、"func"で行15というように1文字ずつ次候補に移ってしまう。
 
#287
「次に着手できる候補」は全部あと回し。先にバグを潰して基本動作を正常化しないと他の動作試験もできない。
- テキストの状態例）
  - 行2：function
  - 行3：funny
  - 行4：function
  - というとき、`gg`,`:%s/f`で行2の"f"がハイライトされ、`:%s/fu`で行3の"fu"がハイライトされ、`:%s/fun`で行4の"fun"がハイライトされる。これは正常動作ではない。

#288
- 置換対象が正しく選択できない現象は解消した。OK
- 例えば4 matches状態で最初の候補をNo(n)としたら2つ目で関係のない行がハイライトされる。
  - 空行なのに検索語の幅でハイライトされている
- 確認モーダルを出す瞬間にscrolloffの処理なのか、スクロールが発生するのを抑止したい。
- 確認のモーダルダイアログをドラッグ可能にできないか？

#289
- 行36に"function"があり、caretが行35にあるときに`:%s/function/x/gc`で確認モーダルが出た瞬間にスクロールが発生して空白文字列がハイライト表示される
  - 表示範囲[308,355]で行333にcaretとハイライトがある
    - caretとハイライトはスクロール前のものがそのまま残留していると思われる

#290
- 「確認ポイント」
  - 「確認モーダルが出る瞬間にスクロールしないこと」→NG 表示範囲[308,355]
  - 「プレビュー矩形が空行など関係ない行に表示されないこと」 →OK?（該当マッチ位置が画面外になってしまい確認不能）
  - モーダルのドラッグ →OK

#291
改善せず。表示範囲[11,59]でcaretが行35にあり、行36の"function"にマッチするはずのところ表示範囲[308,355]になってしまう。

#292
モーダル出現時に表示範囲が狂う件は解消した。4 matchesでNoで次に進んでも正常。OK
- "Substitute"→"Substitute(置換)"とし、その右に入力されたコマンドを表示してほしい。例）"Substitute(置換)  :%s/function/x/gc"
- HTA版sixに準じて確認モーダル内にUndo(u)を追加してほしい。

#293
- 確認ダイアログ内でのUndoの仕様が異なる。置換対象が複数あって2つ目以降が対象になっているときに1つ前の'y'/'n'を取り消してそこに戻る、という動作。
- "Substitute(置換)"と入力したコマンドの間を4remほど空けてほしい。

#294
概ねOK。
- 置換候補1つ目のときはUndo(u)ボタンはdisable状態でグレー表示にして。
- 1つも置換しなかったときのトーストが"no match"になっているが、matchしてないわけではないので"replaced: 0"でいい。

#295
挙動はOK。disable状態のUndoボタンでは文字色をgrayにして。

#296
OK。
- `:e!`って実装してなかったっけ？ 実装したい。「:e! ファイル名」は不要。現バッファの変更を破棄して読み込み直すだけでいい。
- :e popupを実装したときは`:e`では開かず`:e `とスペースを入れたところで初めてpopupが開く仕様だったはずだが、いつのまにか`:e`だけで開くようになっている。不具合とも言えないが、今後他のコマンドも増えるだろうしやはり`:e `まで入力したところで初めてpopupを出す仕様に戻そう。

#297
NG。`:e!`で元バッファは何の変化もなく新たに「!」という名前のバッファが生成されてしまう。

#298
変わらず。NG。「!」というバッファができてしまう。
また`:e`だけで空白を入れていないのにpopupが開くようになってしまった。NG。

#299
OK。
次はVISUALモードで選択中の範囲を対象とした:sを。

#300
VISUALモード選択中に`:`を入力しても何も起きないが？

#301
- VISUALモード選択中に`:`を入力したら「:'<,'>」が自動挿入されるべき。
- VISUALモード選択中に`:`を入力しても選択ハイライトはクリアされないように。

#302
- `:`入力でVISUAL選択のハイライトが消える
  - 構わず'<, '>s/foo/bar/g を実行するとヒットするのにreplaced:0になる
- 逆にEscでコマンド入力を抜けるとVISUAL選択のハイライトが復活してしまう。リセットされるべき。

#303
- `:`入力でVISUAL選択のハイライトが消える
  - 構わず'<, '>s/foo/bar/g を実行すると選択していた範囲内で正しく全置換された。なのでハイライトが見えないだけで効いてはいるようだ。
- Escでコマンド入力を抜けてもVISUAL選択のハイライトは復活しない。OK。

#304
- VISUAL選択→「:」→色が変わるが選択範囲は見えている。OK。
  - : 後の選択範囲の色もTHEMEで指定できるようにして。
- 「:'<,'>s/old/new/gc の確認モーダル操作時にも選択が視認できるか」→NG。モーダルが出ると消える。
  - ただしインクリメンタル・プレビューの矩形は見えている
- 「EscでCMDを抜けた後、選択が復活しないか」→NG。: 後の選択範囲の色がそのまま残る。離れた箇所から`v`でVISUALモードに入るとそこで新たにVISUAL選択のハイライトが表示される。
- 「:'<,'>s/old」の入力中のインクリメンタルサーチがVISUAL選択範囲外でマッチする(caretもそちらに飛ぶ)ただし置換を実行するとVISUAL選択範囲内だけで実行される

#305
- 選択状態の描画はOK。
- 確認モーダル上のUndoで1つ前のy/nを戻せるが、戻すだけでなく「次の置換候補」も戻したいのに戻っていない。
- VISUALでも%(全体)でも、置換対象が3つ以上になるケースでオプション"c"を付け、y,y,u,qで抜けたあとuを押してもUndoが効かない。
 
#306
- 「全体置換で c フラグ付き:候補1: y、候補2: y、候補3: u → 候補2に戻る（ハイライトも戻る）さらに u → 候補1に戻る（Undoボタンは最初の候補では無効表示）」→OK
- 「VISUALでも%(全体)でも、置換対象が3つ以上になるケースでオプション"c"を付け、y,y,u,qで抜けたあとuを押してもUndoが効かない。」→NG。改善されず
- 引数無しで起動した直後、:%s/quick/x/ Enterとしたら「4, 18, 32, 45, 59, 73, 87,...」行目の"quick"が置換されずに残る。
- 引数無しで起動した直後、:s/quick/x/ Enterとしたあとscrolloffによるスクロールが発生する。置換終了直後の1回はスクロールしないでほしい。

#307
- 「VISUALでも%(全体)でも、置換対象が3つ以上になるケースでオプション"c"を付け、y,y,u,qで抜けたあとuを押してもUndoが効かない。」→NG。改善されず
- 『引数無しで起動した直後、:%s/quick/x/ Enterとしたら「4, 18, 32, 45, 59, 73, 87,...」行目の"quick"が置換されずに残る。』→未置換が残る現象は解消したが、"Replaced: 400"のあとcaretが行間の中途半端な位置に表示されキー入力を受け付けない(caret位置が正しくて中途半端なスクロールをしているのかも。添付スクショ) ←%を付けずに実行しても同じ。

#308
改善していない。一つずつ潰していこう。
再現手順：
- 引数無しでsix.ps1を起動
- :%s/quick/x/c Enter
  - 確認モーダルで y, y, u, q
  - 確認モーダルが閉じた直後に u →NG。何も起きない

#309
y,y,u,qで抜けたあとのuが効かないという現象は改善していない。
よりシンプルな例で修正・確認しよう。
- 行の内容が"x   aaa  aaa  aaa "で、caretが先頭xの位置にあるときに`:s/aaa/b/gc`で確認モーダルが出たときに`y`,`y`と入力したときの行内容は"x   b b aaa "になる。(期待動作)
- 続けて`u`と入力すると"x   b aaa aaa "となる。ひとつめの"aaa"がハイライトされている状態。Undoボタンはまだenable。(期待動作)
- 続けて`u`と入力しても行内容もハイライトも変化しない (NG)
- その状態で`q`で確認モーダルを抜けたあと、undoが効かない。

#310
- "x   aaa  aaa  aaa "の'x'にcaretを置いて`:s/aaa/b/gc`で確認モーダルを開いた場合：
  - モーダル内のUndoの挙動はOK。
  - `y`,`y`,`u`,`q`で抜けたあとの`u`で"x   b  aaa aaa "までしか戻らない症状は変わらず。NG
  - 確認モーダルで最後の候補を`y`で置換して抜けると、直後に`u`が効かない。続けて`u`とすると正しくUndoされる。最後の候補を`n`で抜けていた場合には直後から`u`が有効。
  - 確認モーダルをAllで抜けたときINSERTモードになってしまう

#311
引数無しで起動した直後(*1)から：
- :10, O, "x   aaa  aaa  aaa ", Esc, :w foo.txt とした(*2)あと、
  - `0`で'x'にcaretを置いて`:s/aaa/b/gc`で確認モーダルを開く
  - `y`,`y`,`u`,`q`で抜けたあと`u` → (*1)の状態まで戻ってしまう。タブバーの*は消え、:qで確認無しに終了する
    - 再度sixを起動してfoo.txtを開くと(2)の状態になっている

- Allで抜けたあとINSERTになってしまう症状は解消した。OK。

#312
- y,y,u,q,uで起動直後の状態まで戻ってしまう症状は解消していない。NG。ただしタブバーの*は残っている。続けて`u`を入力しても何も起きないが。

#313
OK。:sでの不具合は解消した。
- :sのオプションでnも実装したい。
- 実装前のnもだが、不正なオプションがあっても無視するだけの挙動になっているが、エラーを示したうえで置換は実行しない方がよい。

#314
- nオプションでも実際に置換されてしまっている。NG
- xオプションでも置換が実行されてしまう。
- nのメッセージはHTA版sixにあわせて(というか元はVimにあわせて)"X matches on Y lines"にしよう。

#315
- nオプションなのに置換が実行される挙動は解消した。OK
- nオプションのメッセージが"replaced: N"になってしまっている。NG
- VISUALモード時もnオプションで置換されることはなく正しくカウントされている。OK
- 無効なオプションをはじくようになったのはよいが、Nオプションは単に無視するだけで置換が実行されてしまっている。大文字/小文字も厳密に判定してNなどは不正オプション扱いとしてほしい。

#316
OK。nオプションの実装完了。
次はcを実装したい。ch,cw,c$など、HTA版sixで実装したものをそのまま持ってきたい。

#317
(HTA版sixではcは未実装だったね)
今回の実装に不具合はないようだが、いくつか変更したい。
- dオペレーションに沿った実装とのことだけど、Vimではdwとcwで削除する範囲が異なっているようだ。
  - "abc ="の'a'にcaretがあるときdwでは"abc "が削除されて"="になるがcwでは"abc"だけ削除されて" ="になる(Vim)。
    - sixも同じ挙動にしたい。
- VimではVISUALモードで選択中のときにcとタイプすると即座に選択範囲が削除されてINSERTモードに移行するようだ。
  - sixも同じ挙動にしたい。

#318
OK。次はNcwの挙動をVimにあわせたい。2cw, 3cwなど。

