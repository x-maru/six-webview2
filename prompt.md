#194
- 未編集時の:wで不要なジャンプは発生しなくなった。OK
- `:`と1文字入力した直後にEscで抜けると`G`と同じ挙動でEOFに飛んでしまう。NG
  - `:100`などでジャンプする際にも一瞬`G`と同じ挙動でEOFに飛んでしまう。NG
- `six.ps1 _six.js`は正常にオープンできるようになった。OK
- `:wq!`も対象はカレントバッファのみとする。

#195
- 不要なEOFへのジャンプは抑止された。OK
- 引数無しで起動した際のバッファで`G`で末尾に飛ぶと縦スクロールバーが2つ表示される。
  - "_six.js"などを開いて`G`しても発生しない
- 行番号ガターの行高が高すぎるようで、少しずつ差が開いて行400あたりでちょうど1行分ずれてしまう。(引数無しで起動した際のバッファに表示される内容だとわかりやすい。添付画像)

#196
- 縦スクロールバーが2つ表示される問題は発生しなくなった。OK
- 行番号ガターと本文とのずれの量は変わっていないように見える。行1でも揃っていないので行高だけでなく開始Y座標が違っているのでは？たぶん行高と両方だと思う。

#197
- 引数無し起動時のバッファで、ggの状態でtop座標も行高も揃っている。:368でも揃っている。だが:369とすると1行分近くずれる。
:368のときの表示範囲は[336,399]で、:369のときの表示範囲は[337,400]。EOFは行400。:368から:369としても即ずれるし、ずれた状態から:368とするとずれは解消する。

#198
- ズレは小さくなったが、まだ「:368ではズレないのに:369で急にズレる」症状は続いている(添付スクショ2枚)。

#199
変わらない。:368ではズレないのに:369で急にズレる。

#200
変わらない。:368ではズレないのに:369で添付スクショのようにズレる。

#201
- OK。行番号と本文とのズレは解消した。
- caret(赤)のY座標が本文行に対して少し上にずれている。行高と一致するようにしたい。

#202
- 引数無し起動時のバッファで、行400(EOF行)が見えていない間はcaretの描画は良い感じだが、行400が見えた途端に上方にずれる。

#203
- Caretの表示位置がずれなくなった。500%まで拡大してもずれない。OK。
- 次は、コマンド入力欄の履歴を辿れるようにしたい。HTA版と同じ仕様で、直前とまったく同じ場合は履歴に格納しない。遡った履歴を編集して実行もできるが編集した内容は最新履歴として追加され、過去履歴は更新しない。

#204
- :eの最後がclickだった場合に履歴に入らない (履歴に入ってほしい)
- :e の最後がEnterによる「補完&確定」だった場合に、補完前の文字列が履歴に入る (補完後の実際に実行された文字列が履歴に入ってほしい)
- :eより前の履歴を表示する術がないので、Alt-P/Alt-Nも↑/↓と同様、履歴の辿り/前進のキーとしたい
- :e ポップアップ中の↑↓で上下1行しか移動できなくなってしまった。

#205
- :e popup click/Enterで確定後のコマンド文字列が履歴に入るようになった (OK)
- :e popupでコマンド入力欄を編集するとpopup内の↑↓が効かなくなる
  - 最初にpopupから入力欄に保管されたファイル名をbackspaceで1文字削除しただけ(なのでpopup候補にマッチ継続)でも↑↓不可になる。
- やっぱりAlt-P / Alt-Nでの履歴移動は廃止する。代わりに↑↓の仕様を変更し、コマンド履歴を:eまで辿ってもすぐにはpopupを表示しないようにする。
  - 「popupが出ていない」かつ「コマンド入力欄の先頭が:e」のときにTabキーが押下されたらpopupを出す。
    - そのときのTabキーは「popupを出す」操作として消費される(同時に補完までは実施しない)
  - 「popupが出ていない」かつ「コマンド入力欄の先頭が:e」のときEnterキーが押下されたらファイルオープンとして:e動作を確定する。
- popupが出ていない状態なら引き続き↑↓での履歴移動は有効。

#206
- :e popupでコマンド入力欄を編集するとpopup内の↑↓が効かなくなる(元の位置から上下に1行だけが移動可能範囲になる)
- :e履歴を表示しただけではpopupを出さない、Tabでpopup、あたりの挙動はOK
- popup内でのPageUp/PageDownはOK
- 履歴内でのPageUp/PageDownは↑↓と同じ挙動＋何回か動かすと効かなくなる．．． が、そもそも履歴を辿る際のPageUp/PageDownは廃止でいい。

#207
- :e popup内での↑↓移動はOK
- 仕変だが、:e同様:bも履歴から表示したときは即popupは出さずTabが押されたらpopupを出すようにしたい。
- 履歴と関係ないが、:bのポップアップが出ているときにTabキーを押すとフォーカスがどこかに移ってしまうようだ。popupが開いてる間に限らずコマンド入力中はTabキーの通常の「フォーカスを切り替える」効果は抑止したい。

#208
:bの扱いも含めコマンド履歴対応はOK。
- 水平スクロールバーが出ているとき、EOF行が隠れて見えない。EOF行の下側に常に余白を持たせたい。本文1行分で十分。カーソルが乗ることはない領域なので行番号も不要で、textareaのpaddingなど何でもよい。

#209
縦スクロールバーで余白部分が見えるようにスクロールすればEOF行が丸ごと見えるようになるが、GでEOFに飛んだときは見えない。
- Gで飛んだときも余白まで見えるように表示範囲を調整してほしい。EOF行の下の余白は横スクロールバーに隠れてしまってよい。
- scrolloffの設定行数に足りずカーソル自体が下に移動し始めるとき、最初の1行分は「余白が見えるようにスクロールアップ」にしてほしい。EOF行の下の余白は横スクロールバーに隠れてしまってよい。

#210
EOF下の余白の実装完了。OK。
- 次は`f`や`b`などのカーソル移動系を実装したい。実装目標は「HTA版sixと同じ」になること。

#211
間違えた。`f`じゃなくて`w`だった。基本はVim準拠だがHTA版sixでVimの`f`は未実装だったはず。HTA版sixで実装してた移動系コマンドは「h,j,k,l,←,↓,↑,→,w,b,^,0,$,{,},gg,G」。ほとんどのコマンドは数字前置でカウントを指定できる(5lで右に5つなど)
日本語対応などHTA版sixの実装を参考にして移植して。

#212
- `w` / `b`: トークンの定義がHTA版sixと異なっている (NG)
- `G`直後の`b`でカーソルが動くときと動かないときがある。間が空くと駄目か？
- `{` / `}`: OK (HTA版sixはバグが残っているので合わせなくてよい)
- 他、数字前置などの挙動はOK

#213
- `G`直後の`b`はOK
- `w` / `b`トークンの定義が直ってない。
- 「これにより、記号が連続していても w/b は1文字ずつ刻んで進む/戻るようになります（HTA 版の感覚に寄せる）」
  - 逆。ちゃんとHTA版sixのソースを参照して。

#214
HTA版 sixの w/b でのトークンの定義をまとめて。
https://github.com/x-maru/six.git

#215
OK。
次は削除系の編集コマンドを実装しよう。挙動のお手本はHTA版six。
- x, d+モーション, dd, dgg, dG

#216
- x : OK
- dw, db : OK
- d0, d^ : OK
- d$ : NG 削除されない
- d{, d} : NG 削除されない
- dh, dl : OK
- dj, dk : たぶんOK (よくわからない。。まず使わないコマンドなのでよしとしておく)
- dgg, dG : NG 削除されない
- dd : OK

#217
改善されず。
- d$ : NG 削除されない
ーd{, d} : NG 削除されない
- dgg, dG : NG 削除されない

#218
- d$ : NG 削除されない
ーd{, d} : NG 削除されない
- dgg: OK
- dG : NG 削除されない

#219
IMEはずっとOFFのまま。
- d$ : NG 削除されない ($ : Shift+4)
ーd{, d} : NG 削除されない ({ : Shift+[, } : Shift+])
- dG : NG 削除されない (G : Shift+g)

#220
いずれも改善せず。
dのあとのタイムアウトを廃止する。キーコードは見ず、`$`の文字コードで判断する。
(そもそもJISキーボード専用には作っていない。ASCIIキーボードも対象である)

#221
いずれも改善せず。
キー入力は検知しているようで、削除すべき範囲の末尾にcaretが移動する。
- `d$`としたときに行末にcaretが移動する(が何も削除されない)など。

#222
OK.
次はUndo / Redoを入れよう。

#223
- Undoしてmodifiedカウントが0に戻ったはずなのにタブの'*'が消えず[未保存]扱いになっている。
- undo/redoと関係ないかもしれないが、引数無し起動時の"(untitled)"バッファを編集して:qして"Save"を選んだときエラーにならない。編集内容はどうなったのか？

#224
OK.
アプリウインドウのクローズボックスを押したときは`:qa`の動作になるようにしたい。

#225
クローズボックスで:qaのダイアログが出る前に「アプリから移動しますか？．．．[移動][キャンセル]」というダイアログが表示されてしまうのを抑止したい。

#226
変わらず。ブラウザネイティブの方が先に出てしまう。

#227
変わらず。ブラウザネイティブの方が先に出てしまう。

#228
変わらず。ブラウザネイティブの方が先に出てしまう。
ホストはsix.ps1の中でC#で簡易httpサーバを立てているのではなかったか(_six.cs)。

#229
変わらず。ブラウザネイティブの方が先に出てしまう。
ブラウザ単体起動はしていないし考慮する必要なし。
- 「必要なら six.ps1 での C# インラインの形（Add-Typeベース）に合わせた具体的な差分も書きます。」
  - six.ps1, _six.csを精査して差分をお願い。

#230
ローカルDLL同梱は避けたいので、ブラウザネイティブのダイアログを抑止するのは諦める。
- ブラウザネイティブダイアログの文言を変えることは不可？
- ブラウザネイティブダイアログには【移動】【キャンセル】という2つのボタンが配置されているが、1つにすることは不可？
- ブラウザネイティブダイアログには「このページで追加のダイアログを作成できないようにする」というチェックボックスが配置されているが、これをチェックすると具体的にどうなる？

#231
- 未保存のバッファがないときはブラウザネイティブダイアログが出ないようにしたい。
- ローカルDLLが存在する環境で動作させることもあるし、ブラウザネイティブダイアログを抑止するコード自体は残しておこう。
失敗したときのフォールバックと併せて。

#232
OK。
sixのアプリアイコンの指定ってできる？このフォルダ内のicon.pngにしたいんだけど。

#233
icon.pngは廃止。代わりに1024x1024.png, 512x512.png, 256x256.pngを置いたので、最適なものを使うようにして。

#234
OK。ところで各iconファイルに対応する"*:Zone.Identifier"というファイルが生成されたようだけど、これは何？ソース(リソース)として持っておかないといけないもの？

#235
次は挿入系の編集コマンドを。HTA版sixで実装したもののみを厳選して移植。「o, O, a, A, i, I, p, P」

#236
- 引数無しで起動した直後に`i`,`I`でINSERTモードに移行して文字入力するとEOFの位置に挿入されてしまう。
  - ほぼ最初のバージョンから`i`の挙動はずっとそうだった。今回直るかと思っていたが変化なし。
  - `o`や`O`で空行の挿入位置やその後のcaret位置は正しいのに文字入力するとEOFの位置に挿入される
  - `p`,`P`は数字前置も含め期待動作。OK。

#237
- insert位置がEOFになる現象は解消した。OK
- 非modified状態から削除したあと`p`や`P`で元通りになったときmodifiedがリセットされてしまうが、そうなるべきではない。
- `o`や`O`での「空行挿入」とそのあとの「文字入力」が別々にUndoバッファに入り、2度undoしないと空行挿入前まで戻らない。不具合ではないかもしれないが1セットで扱うようにしたい。

#238
OK。次は赤Caretの描画を調整したい。
- 塗り潰し：文字ボックスの左端から右へのグラデーション。開始色はTHEME.caretGradientStartのままでよいが終了色caretGradientEndは廃止し、rgba(255,0,0,0.0)固定とする。
- 幅：文字ボックス幅の90%の位置まで。全角文字の上に乗ったときは文字ボックスも全角サイズであり、その90%。
- _six-theme.jsでwindow.THEMEとして定義している変数名を使わずリテラル値で指定している箇所があったら修正してほしい。

#239
- caretのグラデーションで中間も指定できる？等間隔でいいが左端をcaretGradientStart、中間をcaretGradientMid、終点は変わらずrgba(255,0,0,0.0)にしたい。
  - THEME.caretGradientMidをrgba(255,0,0,0.2)にしてみて。
- 全角文字で終わる行の行末(改行文字の位置)にcaretがあるときも全角幅になるのは違和感がある。半角幅がいい。
  - 半角か全角かの二択でいいので、半角は'W'の幅で計算すればいい。もしくはフォントメトリックで正規の幅が得られるならそれでもいい。
- caretに限らず、THEMEで指定しているのにリテラル値を使っている箇所はTHEME変数に置き換えたい。

#240
caretの描画OK。
activeLineBgが縦スクロールバーの上に描画されるのが気になる。バーは存在しないこともあるからBgの長さは変えずにZ-indexでバーが上に来るようにできないか？

#241
ActiveLineBgと縦スクロールバーのz-index調整はOK。
- バグ発見。`^`で移動した直後に`b`を押すと固まってしまう。インデントがなく`^`が`0`と同じ挙動になったときは大丈夫っぽい。

#242
Bug fix OK。
- CTRL+ホイールで拡大縮小ができるが、タブバーやコマンド入力欄まで一律で拡大されてしまう。本文Textareaと行番号ガター部分だけを対象に拡大縮小できないか？CTRL+ホイールで出来れば理想だが無理なら専用コマンドを検討したい。

#243
- 行番号の表示位置がおかしい(添付スクショ)。
- 全体が拡大縮小されていたときは右上に【110% ー＋[リセット]】のようなオーバーフローが表示されていた。似たコントロールをタブバーの上にオーバーラップして表示したい。
  - タブバーの描画は変えない
  - 拡大率が変化したら3秒間表示して自動で消える
  - 表示内容は「現在の表示倍率」と縮小ボタン、拡大ボタン、リセットボタン。【110% [―][＋] [リセット] 】

#244
- 行番号の表示位置は改善せず。拡大率はsixを終了しても次回起動時に復元されるが、その際は行番号も正しく表示される。
- ズームHUDが表示されていない。
- 拡大率はどこに保存してるの？
- _six.htmlと同じ場所にある.edge-profileというディレクトリは削除してはいけないもの？

#245
- 行番号も概ね正しい位置に表示されるようになった。
  - 倍率により、若干ずれることがある(添付スクショ)
    - リニアにずれていくわけではなく、1段階拡大または縮小すると解消する。
- ズームHUDはまだ見えない。
  - DevToolsで_six.jsの行2256にbreak pointを置いて確認したところではCtrl+ホイールの際の'e.ctrlKey'は'true'。

#246
行番号のズレも解消し、Zoom HUDも見えるようになった。
- Zoom HUDを操作したあと、キーボードフォーカスがどこかに行ってしまう。Zoom HUDはキーボードで操作できなくてよいので一切フォーカスを奪わないようにしたい。

#247
ズーム対応 OK。
本文の各行を行ごとに上から下へのグラデーションにしたい。開始終了の色はTHEMEで指定できるように。初期値はrgb(243,251,243)からrgb(237,243,237)。

#248
背景グラデーションはいい感じ。OK
- 本文の文字色と行番号文字色もTHEMEに追加して。
- 本文の文字サイズをもう1段階大きくして。(拡大率100%のとき)
- caretのある行の背景も縦グラデーションにしたい。rgb(231,255,231)からrgb(219,243,219) (THEME)
- アクティブなガター背景も縦グラデーションにしたい。THEMEに追加して。rgb(231,255,231)からrgb(219,243,219)

#249
- エディタの文字サイズはそのままで、行間を少し広げて。0.2remくらい。
- 非アクティブなガター背景も縦グラデーションにしたい。THEMEに追加して。rgb(235,239,235)からrgb(219,227,219)
- アクティブな行番号文字色もTHEMEに。rgb(143,31,95)

#250
- 拡大縮小倍率を固定したい。50,75,90,100,110,133,180,250,300 (%)
- font-familyを変更して。
  - font-family: "Cascadia Code", "Consolas", "游ゴシック Light", "Yu Gothic", "Meiryo", "MS Gothic", monospace;
- cssやTHEME変数を弄ったあと一番手っ取り早く反映させる方法は？

#251
- HTA版sixと同じフォント設定にしたいのだが、ASCII文字のフォントが異なるようだ(HTA版より太い)。_six.cssでの指定がどこかで上書きされてない？ またはHTA版の方も_six.cssでのfont-family指定が最終的な指定になっていないのか？
- 倍率300%のとき、ホイールでのスクロール量が行高にフィットしていないようだ(添付スクショ) ※300%に限った話ではなさそう
- 本文の任意の位置をマウスでクリックしたときcaretがそこに移動するようにしたい。

#252
- フォントに変化なし。ASCII文字が太い。添付画像2枚(HTA版.png, WebView2版.png)は同じファイルを開いたところ。
- ホイールでのズレは解消せず。行番号と本文テキストとはズレていない。揃って「中途半端な行高分」だけスクロールしている。スナップ処理がうまくいっていないのか？
- ガターの右側に隙間(padding?)があるが、行番号の文字は良いが背景のグラデーションは隙間部分まで効いてほしい(ガター右の隙間.png)。

#253
- Consolas 400になっているが、やっぱり太い。HTA版もhtmlをChromeで開くとmonospaceになってしまい、hta起動時と異なるので確認にならない。IE11の癖かもしれず「HTA版にあわせる」のは忘れてWebView2版独自に「より細いフォント」に変更してほしい。
- ホイールスクロール時のズレはなくなったが、ガター側の背景が1行スクロールする度に「一度動いたあと修正」を繰り返していてチラつく。本文側の背景グラデーションは完全に固定されているのに、同じロジックでガター側も固定できないのか？
- ガター右端の隙間までグラデーションが塗られるようになった。OK

#254
- font-weightは200がいい。
- ガター背景までスクロールしてしまう症状は改善せず。
- 行高をさらに0.2rem増やしたい。

#255
- font-weightよし。OK
- ガター背景のスクロールよし。OK
- 行高よし。OK
- CTRL+ホイールで拡大・縮小するとき、ゆっくり最小量だけホイールしたつもりでも90%の次が110%になったり、プリセットを辿るのが難しい。調整可能？
- マウスカーソルが本文およびガター上にあるとき、ゆっくり点滅してしまう。自前の赤caretの点滅が影響している？ できれば抑止したい。

#256
- ホイール動作OK
- マウスカーソルの点滅は改善せず。元々Iビームで安定はしていた。赤caretが消えて再び表示されるときに一瞬マウスカーソルが消えているように見えるが、ずっと放置していると連動しないテンポになることもあるような。
- コマンド入力欄の文字色と背景色をTHEMEで指定したい。とりあえずwhite/blackで。

#257
- ほぼ点滅はしなくなったが、
  - 1秒ほど何もしない時間をおいてから赤caretを動かす際に1回だけマウスカーソル(Iビーム)が一瞬消える。
  - h/j/k/lなどを押しっぱなしにしてオートリピートが効いている間、マウスカーソル(Iビーム)が白くなる。
- よく見るUIとして、キーが押されたらマウスカーソルが消え、少しでもマウスが動いたら再び見える、というのは実装可能？
- 赤caretの高さを行高に合わせたい。現在はtopは一致しているがbottomの下に(caret行の)行背景が見えている。

#258
- Ctrlなどを押しただけでマウスカーソルが消えるのに少し違和感があるので、マウスカーソルを消すのは「キー入力があったとき」ではなく「赤caretが動いた」or「textが編集された」or「本文行の表示範囲が変化した」のいずれかにしたい。
- マウスカーソルが消えてる間は赤caretの点滅を復活させたい。
- マウスカーソルが表示されている間は赤caretを「グラデーションなし(塗り潰しなし)、枠だけ、点滅なし」にしたい。枠の色はcaretGradientStartでいい。
- THEME.caretColorってまだ使われてる？使われてなければTHEME定義から消しといて。

#259
- 「赤caretが動いた」or「textが編集された」ときに赤caretもマウスカーソルも変化していない。NG
- 「本文行の表示範囲が変化した」の場合は反応している。
  - あくまでもcaret移動によって表示範囲が変化するケースを想定していた。スクロールバーを掴んで動かした場合は該当しない方がいい。

#260
OK。
次はyankを実装したい。

#261
いいね！yank実装完了。
次はVISUALモードを実装しよう。選択中の表示はTHEME指定で、とりあえずcyan地にwhite文字かな。

#262
- `v` / `V`でVISUALモードに入るが、コマンド入力欄左の表示が"[NORMAL]"のまま変わらない
- VISUALモード中の`gg`, `G`が効かない。
- 他の`v`(h,j,k,l,0,^,$,{,})やVでの行単位切替え、yank, deleteなどは期待動作になっているようだ。

#263
OK。
次は検索(:s)を実装したい。実装する範囲・仕様はHTA版sixに準拠したい。

#264
- 確認ダイアログでボタンは効くがキーボードフォーカスを当てても`y`,`q`など直接のキー入力が受け付けられない。
  - 確認ダイアログを抜けたあとコマンド入力欄にキーボードフォーカスが残る。本文textareaに移ってほしい。
- 次）「:s入力時のインクリメンタル・プレビュー」を。

#265
- 確認ダイアログでUndoした直後の置換対象がひとつ前に戻らず先に進んでしまう。
- 確認ダイアログのボタンが1-9で反応するのは廃止して。何が起きたのかわからなくて混乱する。
- 確認ダイアログのボタンを`y`,`n`などで選択したらそのボタンにフォーカスを移してほしい(フォーカスはそのままでいい)。
- インクリメンタルサーチ風に'%:s/'のあとの入力にマッチする位置にcaretが飛んでほしい(飛んでいない)
- インクリメンタルサーチ風に'%:s/'のあとの入力にマッチする最初の文字列を別色表示してほしい(なってない)

#266
- 確認ダイアログでUndoした直後の置換対象がひとつ前に戻らず先に進んでしまう。
- 確認ダイアログで1-9ショートカット廃止OK、閉じた後のフォーカスOK
- 確認ダイアログを途中quitで抜けたあと選択表示が残る(インクリメンタル時とは異なるvisualモードの色)。
- 確認ダイアログでいくつか置換して閉じたあとUndoが効かない
- プレビューの色はTHEME化を。

#267
- 広範囲に壊れたっぽいので#263前に戻した。
  - 戻す前の症状：
    - 行番号の行間が狭い
    - caret位置と本文表示がずれてる(行末の桁が全然あわないなど)
    - Gで行番号は400になっても本文は行303あたりが表示されていたりする
- :sの実装中に壊れたようなので、再度慎重に。
- 先に`/`, `?`での検索を実装しよう。:sの実装は待ち。既存の`:e`とか壊さないよう慎重に。

#268
- NORMALモードで`/`や`?`を入力しても何も起きない。

#269
- NORMALモードで`/`や`?`を入力しても何も起きない。

#270
- `/function`や`?return`、およびその後の`n` ; `N`も正常に動作した。OK
- 次は入力中のインクリメンタル・プレビューを実装しよう

#271
- 例えば"function"にマッチする箇所が行2,5,10,15にあったとして、`gg`直後に`/function`と入力すると最初の"f"で行2、次の"fu"で行5、その後"fun"で行10、"func"で行15というように1文字ずつ次候補に移ってしまう。

#272
OK。
- 「ミニマップ連動」ってどういうもの？
- 「ハイライトの複数候補表示」ってVimにもある？

#273
- 簡易のミニマップは要らないな。段階3のフルミニマップは欲しいけど、先に実装したい機能が色々あるのでTODO入り。わりと優先度は低い。
- 入力中の全候補ハイライトまでは要らない。hlsearchは優先度高めのTODOだな。
- 次は:sの再実装だけどその前に、行番号の行間がsubpixelレベルで本文とズレているようで、400行あると蓄積して1,2pixel分のズレが目視でわかる(添付スクショ)。これを抑止したい。
  - ただし、ズレが目視できるケースでも表示倍率を変えるとわからなくなったりする。全倍率で抑止するのは難しい？

#274
ズレはなくなったが．．．拡大時に行間(行高)の計算が狂うようになってしまった(添付スクショ)。

#275
直っていない。添付スクショ。

#276
OK。拡大時も含め行番号のズレは解消した。
- hlsearchは`set hlsearch`, `set nohlsearch`の他に`set hlsearch!`でトグルも出来るようにしたい。
- hlsearchによるハイライトはTHEMEで色指定できるように。デフォルトはyellowで。

