#532
OK。IME関連の修正は完了。
次は、コマンドバーをフローティングに変更する
- 普段は消えていて、`:`や`/`,`?`などが入力されたときに「caret行付近」にオーバーレイ表示する。
  - caretが画面の上半分にいるときは3行下に表示、下半分にいるときは3行上に表示する。完全中央のときは3行下。
  - 幅は、本文textarea(ガター除く)よりやや短く、左右とも0.8remマージンがあるようにセンタリング表示で。
  - 今の常時表示コマンドバーでは"command（e.g. :100, :q）"というメッセージが出ていて入力欄が空になったら復活するようになっているが、あのメッセージは不要。
  - `Esc`で消える。
  - :e popupや:b popupの出現位置は画面中央に固定し、その下にフローティングコマンドバーを表示する。
    - :eや:bの際は編集中の本文が隠れても問題ないので。
    - popupを閉じたときはコマンドバーの位置はそのままでいい。
- 何か仕様に漏れ・矛盾があったら指摘して。

#533
- caret近傍の出現位置 →OK
- コマンドフロートの幅 →右側marginに縦スクロールバーの幅を加えてほしい。
- :e popup / :b popupとの相対位置
  - #532で「その下に表示」と書いたのはz-indexではなくY座標のつもりだった。
    - 重ならない位置に表示するので、コマンドフロートのzは変更不要
  - popupを画面中央と指定したが、上寄りに変更しよう。
      - :e popupの上マージンは本文行高で2.5行に固定。縦方向センタリングを廃止。
      - :b popupの上マージンも本文行高で2.5行に固定。
  - :e popupの高さを固定にする。
    - 下マージンを最低2.5行確保するのを優先して、本文表示行数の75%をpopupの固定高とする
- コマンドフロート表示中にF1～F8がsixで消費されずにEdgeに渡ってしまっている。NG
- コマンドフロート出現位置の例外仕様を追加したい。
  - VISUALモードで選択中だった場合は、caret近傍ではなくVISUAL選択範囲の下に1.5rem空けた位置にする。
    - コマンドフロートの下マージンを1.5rem確保するのを優先し、両立しない場合はVISUAL選択範囲の上に重なる位置にする
- コマンドフロート表示中にヘルプダイアログを出し、閉じたときもコマンドフロート表示は維持してほしい。
  - 現状でタブを切り替えるとコマンドフロートは`Esc`押下扱いだが、それでよい。

#534
- コマンドフロートの幅 →右側marginに縦スクロールバーの幅を加えてほしい。→OK
- :e popup / :b popupとの相対位置 →なってない。NG
  - :e popupを開く際にコマンドフロートをpopupの下に移動する。←request
    - :e popup下端から1.5rem空ける ←request
- :e popupの上マージンは本文行高で2.5行に固定。→OK
- :b popupの上マージンも本文行高で2.5行に固定。→正しく実装されているが、変更する
  - :b popupの縦幅はバッファ数に応じて可変にする。←現状維持
  - :b popupを表示する際にコマンドフローは動かさない ←request
    - コマンドフローが画面上半分にあるなら:b popupはコマンドフローの下側に表示する ←request
      - コマンドフロー下端からpopup上端までの空きを1.5remに固定する
    - コマンドフローが画面下半分にあるなら:b popupはコマンドフローの上側に表示する ←request
      - コマンドフロー上端からpopup下端までの空きを1.5remに固定する
- :e popupの高さを固定にする。→なってない。NG
  - 下マージンを最低2.5行確保するのを優先して、本文表示行数の75%をpopupの固定縦幅とする →popup表示時はOKだがディレクトリを移動すると縦幅が変化してしまっている →NG
- コマンドフロート表示中にヘルプダイアログを出し、閉じたときにフォーカスがコマンドフロートに戻らない →NG

#535
- :e popupで縦スクロールバーの長さや動きが変。キーボードでカーソルを動かしたときの連動が正しくない NG
- コマンドフローの影をもう少し暗くできる？
- いつからの現象かは不明だが、:e popup内の一覧にマッチしなくなる文字を入力してpopup内のカーソルが赤っぽくなった状態でカーソルを動かすとカーソル下の候補が入力欄に補完されて「存在するファイルを指している状態」になるのにカーソルが赤っぽいまま。NG

#536
OK。コマンドフロートの実装は完了。
- req)パレット表示ボタンの「🎨」のサイズを1.5倍にしたい
- req)パレット表示ボタンの右端を縦スクロールバーに接するようにしたい(今は重なっている)
- req)パレット表示ボタンの下端を横スクロールバーに接するようにしたい(今は重なっている)
- req)オーバーレイパレットの表示位置を調整して右端をパレット表示ボタンに合わせたい。

#537
- req)オーバーレイパレットの表示位置を上方向にずらしてパレット表示ボタンと重ならないようにして。
- req)パレット表示ボタンを少し左にずらして「オーバレイパレット内の右端のボタンの右端」とパレット表示ボタンの右端を揃えて。
- NG)`Ctrl+F9`が効かなくなっている

#538
- req) 隠し機能として`Ctrl+9`も`Ctrl+F9`と同じパレット表示ボタンの機能に割り当てといて。
- req) パレット表示ボタン内の改行を無くしてボタン縦幅を縮小したい
  - その分、オーバーレイパレットの表示位置も下にずらす。パレット表示ボタンとの隙間ゼロがいい

#539
- req)`yy`などでyankバッファに格納する内容が空(文字列長0)だった場合はyankバッファを更新しないようにしたい
- req)xはyankバッファに格納しないようにする(undoバッファには入る)
  - ただし2以上の前置数字が付いた場合はyankバッファに格納する

#540
- req)`YY`などでWindowsクリップボードに格納する内容が空(文字列長0)だった場合はWindowsクリップボードを更新しないようにしたい
- req)`x`,`yy`,`YY`それぞれ、空ならバッファを更新しない旨をヘルプに反映して。

#541
- `/^foo`は「行頭からの"foo"」を表すのではないの？「行1の行頭からの"foo"」という意味になっているように思える

#542
- req)ヘルプダイアログに「正規表現」タブを追加して、sixで使える正規表現の詳細説明を書いてほしい。

#543
`/`や`?`でTAB文字を検索するにはどうやればいい？ヘルプ文言の該当箇所に例として書き加えてほしい。

#544
ヘルプ文言の「デリミタとエスケープ」の前に「文字クラス」を追加して、代表的な例を書いて。

#545
- NG)必ずでもなく再現条件は不明だが、`:w`で実際に保存された直後にcaretがEOFに飛ぶことがある。
  - 表示範囲は`:w`実行直前のまま変わらない
  - INSERTモードでの編集だけで保存したときは発生しないように思う(検証不十分)
  - `dd`で削除したあとは再現率が高い(100%かも)。

#546
- `:w`でEOFに飛ぶ現象が発生していたようなケースでは「一瞬EOFに飛んですぐ元の画面に戻る」現象に変わった。
  - チラつくだけの問題にはなったが、元々発生しないケースでは一瞬もEOFに飛ぶことはなくチラつかないため、さらに対処できそうならやってほしい。

#547
- `:w`でチラつく現象は変わらない。
- `x:w`でもチラつくが「`cl`,`Esc`,`:w`」ではまったくチラつかない。保存後の処理に差があるのはなぜ？

#548
`:w`でのチラつきは解消した。OK。
- (req)カレントバッファの「FileSystem上の内容と最後に同期したときのmtime」をConsoleに出力するデバッグコマンド(`:lastsynctime`)を実装して。
  `:lastsynctime`実行時には同期しない。あくまでも「最後に同期したmtime」であって、一度も同期してなければ"null"でいい。

#549
- 外部更新検出の実装を中断していた認識だが、`:w`時にmtimeを同期する処理は入っていない？
  - `:w`で保存したあとに実行してもmtimeがnullと表示されるが、':lastsynctime`の実装が間違ってる？
  - req)まだ`:w`時にmtimeを同期する処理が入っていないなら、実装を。
- Six起動後の最初の`F12`では何も起きず、2度目でDevToolsが起動する。3度目以降は問題ないが即時終了して起動し直すとまた初回の`F12`が無視される。NG

#550
- Six起動後、一切キー入力しないまま3分間放置したのち`F12`を押しても無反応で、2度目以降はDevToolsが起動した。
  - Six起動後、caretを動かしてからであれば`F12`で即座にDevToolsが開く。
- req)バッファがパスを持っている場合は下記のタイミングでlocalStorageで記憶しているmtimeを更新する
  - Six起動時のセッション再現時（ただし外部更新無視フラグが立っていたら更新しない）
  - バッファ保存時
  - `:e`/`:e!`相当の、バッファを読み込んだ時

#551
- `F12`問題は解決した。OK
- `:lastsynctime`でnull以外の値を見たことがない。`:e`直後でも`:w`直後でも。
  - localStorageにmtimeを保存できていないのか、`:lastsynctime`の実装が悪いのか。その両方か。
- `:e C:/Users/ymaru/HTA/six/80x40.txt`で例外発生：
```
L.2400
ReferenceError: b is not defined
```

#552
- 起動直後も`:w`から5秒後も`:lastsynctime`の出力は下記でまったく同じ
```
{
    "name": "80x40.txt",
    "path": "file:///C:/Users/ymaru/HTA/six/80x40.txt",
    "mtime": null,
    "size": null,
    "ignored": false
}
```
  - これはmtime/sizeそのものがAPI側で提供されていないということか？
    - 提供されていないことを確認する手段は他にある？
    - 提供されていなければ代替手段はないということ？

#553
- `:e`での例外は発生しなくなった。OK
- :api? →"api = http://127.0.0.1:52222/
:statmeta!
```
{
    "parent": "file:///C:/Users/ymaru/HTA/six/",
    "baseName": "80x40.txt",
    "entry": {
        "name": "80x40.txt",
        "isDir": false,
        "url": "file:///C:/Users/ymaru/HTA/six/80x40.txt",
        "size": null,
        "mtime": null
    }
}
```
- API実装側の拡張とは、_six.csのこと？ であればやってみてほしい。

#554
six.ps1起動時にエラーになってしまった。
```
six.ps1 starting in: C:\Users\ymaru\WebView2\six
Add-Type : c:\Users\ymaru\AppData\Local\Temp\izus4zw0.0.cs(142) : 名前空間にフィールドやメソッドのようなメン
バーを直接含めることはできません。
c:\Users\ymaru\AppData\Local\Temp\izus4zw0.0.cs(141) :
c:\Users\ymaru\AppData\Local\Temp\izus4zw0.0.cs(142) : >>> app.MapGet("/dir", (HttpRequest req) =>
c:\Users\ymaru\AppData\Local\Temp\izus4zw0.0.cs(143) : {
発生場所 C:\Users\ymaru\WebView2\six\six.ps1:99 文字:5
+     Add-Type -TypeDefinition $code -Language CSharp -IgnoreWarnings - ...
+     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidData: (Microsoft.Power...peCompilerError:AddTypeCompilerError) [Add-Ty
   pe]、Exception
    + FullyQualifiedErrorId : SOURCE_CODE_ERROR,Microsoft.PowerShell.Commands.AddTypeCommand
```

#555
- six起動直後(セッション復帰した直後)のlast-sync:
```
{
    "name": "80x40.txt",
    "path": "file:///C:/Users/ymaru/HTA/six/80x40.txt",
    "mtime": 1763006774385,
    "size": 2904,
    "ignored": false
}
```
- 編集して`:w`で保存した直後のlast-sync:
```
{
    "name": "80x40.txt",
    "path": "file:///C:/Users/ymaru/HTA/six/80x40.txt",
    "mtime": 1763006774385,
    "size": 2904,
    "ignored": false
}
```
- mtimeもsizeも変化ないのは不具合？
  - 1文字挿入して保存したのでsizeも増えるはず

#556
- `:api?`はOK。
- `:e`で開いた直後と1文字挿入して`:w`で保存した直後とでmtime/sizeに変化がない。

#557
- `:e`→`:lastsynctime`：
```
{
    "name": "110x40.txt",
    "path": "file:///C:/Users/ymaru/HTA/six/110x40.txt",
    "mtime": 1762735502125,
    "size": 4260,
    "ignored": false
}
```
- 1文字挿入→`:statmeta!`：
```
{
    "parent": "file:///C:/Users/ymaru/HTA/six/",
    "baseName": "110x40.txt",
    "entry": {
        "name": "110x40.txt",
        "isDir": false,
        "url": "file:///C:/Users/ymaru/HTA/six/110x40.txt",
        "size": 4262,
        "mtime": 1763024485583
    }
}
```
- utf8-unixのファイルで先頭に'X'と挿入しただけで保存したけど2バイト増えてて良いの？
- EOF付近に改行が1つ余分に表示される現象が再発しているようだ。

#558
- `:e`→`:lastsynctime`：
```
{
    "name": "110x40.txt",
    "path": "file:///C:/Users/ymaru/HTA/six/110x40.txt",
    "mtime": 1763029826144,
    "size": 4260,
    "ignored": false
}
```
- 1文字挿入→`:w`→`:statmeta!`：
```
{
    "parent": "file:///C:/Users/ymaru/HTA/six/",
    "baseName": "110x40.txt",
    "entry": {
        "name": "110x40.txt",
        "isDir": false,
        "url": "file:///C:/Users/ymaru/HTA/six/110x40.txt",
        "size": 4261,
        "mtime": 1763029969924
    }
}
```
- EOF付近の余分な改行は無くなったっぽい。(しばらくは注意しておく)
- req)「Shift_JISの読み込みサポートを広げる場合は、別途バイト返却APIを足すなどの拡張が必要です」→sixは必ず_six.csとセットで実行する仕様なので、APIを実装してみて。

#559
現在の状況の確認。
- 外部更新検出の実装を進めているところ
  - mtime/sizeを取得するためにC#サーバ側で対処する必要があって_six.csに手を入れていた
    - mtime/sizeを正常に取得できるようになった
    - C#側に手を入れるついでにShift_JISの読み書きに必要な修正も加えた
- 先にShift_JISまわりをfixしよう。
  - NG)'utf-8 unix'のテキストを'shift_jis dos'に変換して保存し、`:e!`で読み直すと'utf-8 dos'になり、日本語部分がすべて"??"になってしまう。

#560
- NG)sixで'shift_jis dos'に変換すると画面上では問題なく見えるが`:w`で正しく保存できてないようだ。他アプリで開くと文字化けする。
- 手元に「正しいShift-JISのファイル」がないので確認しずらいんだけど、生成してくれる？
- うっかりgit管理してないTODO.mdを変換・保存して壊しちゃったんだけど、過去の内容を憶えてたりしない？

#561
- 'sample_sjis_crlf.txt'を読み込むと'utf8 unix'になって文字化けする
- sixで'utf8-unix => shift_jis dos'に変換して保存したファイルが文字化けする
  - `C:\Users\ymaru\HTA\six\80x40.txt`

#562
```
PS C:\Users\ymaru\WebView2\six> curl "http://127.0.0.1:<port>/probe?fs=C:\Users\ymaru\HTA\six\80x40.txt"
Invoke-WebRequest : パラメーター 'Uri' をバインドできません。値 "http://127.0.0.1:<port>/probe?fs=C:\Users\y
maru\HTA\six\80x40.txt" を型 "System.Uri" に変換できません。エラー: "無効な URI: 無効なポートが指定されてい
ます。"
発生場所 行:1 文字:6
+ curl "http://127.0.0.1:<port>/probe?fs=C:\Users\ymaru\HTA\six\80x40.t ...
+      ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidArgument: (:) [Invoke-WebRequest]、ParameterBindingException
    + FullyQualifiedErrorId : CannotConvertArgumentNoMessage,Microsoft.PowerShell.Commands.InvokeWebRequest
   Command
```
```
PS C:\Users\ymaru\WebView2\six> .\six.ps1
six.ps1 starting in: C:\Users\ymaru\WebView2\six
Add-Type : c:\Users\ymaru\AppData\Local\Temp\zvfhskbr.0.cs(200) : ローカルの変数 'eol' をこのスコープで宣言
することはできません。これは、'親またはカレント' スコープで別の意味を持つ 'eol' の意味が変更されるのを避ける
ためです。
c:\Users\ymaru\AppData\Local\Temp\zvfhskbr.0.cs(199) :               bool hasBom = enc.EndsWith("-bom", Stri
ngComparison.OrdinalIgnoreCase);
c:\Users\ymaru\AppData\Local\Temp\zvfhskbr.0.cs(200) : >>>               string eol = DetectEol(data);
c:\Users\ymaru\AppData\Local\Temp\zvfhskbr.0.cs(201) :               long? size = data.LongLength;
発生場所 C:\Users\ymaru\WebView2\six\six.ps1:56 文字:5
+     Add-Type -TypeDefinition $code -Language CSharp -IgnoreWarnings - ...
+     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidData: (Microsoft.Power...peCompilerError:AddTypeCompilerError) [Add-Ty
   pe]、Exception
    + FullyQualifiedErrorId : SOURCE_CODE_ERROR,Microsoft.PowerShell.Commands.AddTypeCommand
```

#563
下記は、Shift-JIS保存ができていないということ？

```
PS C:\Users\ymaru\HTA\six> $api = 'http://127.0.0.1:22487'
```
```
PS C:\Users\ymaru\HTA\six> $fs  = 'C:\Users\ymaru\HTA\six\80x40.txt'
PS C:\Users\ymaru\HTA\six> $q   = [uri]::EscapeDataString($fs)
PS C:\Users\ymaru\HTA\six> Invoke-RestMethod "$api/probe?fs=$q"


encoding       : utf-8
eol            : dos
bom            : False
size           : 2929
mtime          : 1763084564983
utf8           : True
cp932Roundtrip : True
```
```
PS C:\Users\ymaru\HTA\six> $fs  = 'C:\Users\ymaru\HTA\six\80x40.txt'
PS C:\Users\ymaru\HTA\six> $q   = [uri]::EscapeDataString($fs)
PS C:\Users\ymaru\HTA\six> # 本文はUTF-8で送信。変換不能文字があればstrict=1で400に。
PS C:\Users\ymaru\HTA\six> Invoke-WebRequest -Method Post -ContentType 'text/plain; charset=utf-8' `
>>   -InFile .\80x40.txt `
>>   -Uri "$api/write?fs=$q&enc=sjis&eol=dos&strict=1"


StatusCode        : 200
StatusDescription : OK
Content           : {"ok":true,"reencoded":true}
RawContent        : HTTP/1.1 200 OK
                    Access-Control-Allow-Origin: *
                    Pragma: no-cache
                    Connection: close
                    Content-Length: 28
                    Cache-Control: no-store, no-cache, must-revalidate
                    Content-Type: application/json; charset=ut...
Forms             : {}
Headers           : {[Access-Control-Allow-Origin, *], [Pragma, no-cache], [Connection, close], [Content-Len
                    gth, 28]...}
Images            : {}
InputFields       : {}
Links             : {}
ParsedHtml        : mshtml.HTMLDocumentClass
RawContentLength  : 28



PS C:\Users\ymaru\HTA\six> # 保存後に検証
PS C:\Users\ymaru\HTA\six> Invoke-RestMethod "$api/probe?fs=$q"


encoding       : utf-8
eol            : dos
bom            : False
size           : 2929
mtime          : 1763084688349
utf8           : True
cp932Roundtrip : True
```

#564
ファイルは1行目に日本語を入れてあり、保存に失敗している(#563)。
- Shift-JIS/CRLFの"sample_sjis_crlf.txt"を:eで開くと"utf8 unix"になり文字化けする
- UTF8-LFの"body-utf8.txt"のバッファを"shift_jis dos"に変換して:wで保存するとutf8,crlfになる(メモ帳で確認)

#565
- 「本文はUTF-8で送る前提のため」←これは、six開発初期の「cs側にSJIS対応を入れる前」の方針ではなかったか？SjisでもUtf8でもない場合のフォールバックとしては良いが、それ以外に「本文はUTF-8で送る」が該当するのはどのようなケース？
- 下記で生成したShift-JIS,CRLFのファイルをsixで読み込むと"utf-8 dos"になる。
```
$f='sjis-crlf.txt';$f=Join-Path $PWD.Path $f;[IO.File]::WriteAllLines($f,@('〒100-0001 東京都千代田区千代田1-1'),[Text.Encoding]::GetEncoding('shift_jis'))
```
- UTF8-LFの"utf8-lf.txt"のバッファを編集して:wで保存したものが下記ワンライナーではShift-JISだと判定される
```
$f="utf8-lf.txt";$p=Join-Path $PWD.Path $f;$b=[IO.File]::ReadAllBytes($p);$enc=if($b[0..2] -join ' ' -eq '239 187 191'){'UTF-8 BOM付き'}elseif($b[0..1] -join ' ' -eq '255 254'){'UTF-16 LE BOM付き'}elseif($b[0..1] -join ' ' -eq '254 255'){'UTF-16 BE BOM付き'}else{try{[Text.Encoding]::GetEncoding('shift_jis').GetString($b)|Out-Null;'Shift-JISっぽい'}catch{'不明'}};$crlf=($b -join ',' -split '13,10').Count-1;$lf=($b -join ',' -split '10').Count-1;"Encoding=$enc; CRLF=$crlf; LF=$lf"
```

#566
PowerShell 5.x上での確認でエラーが出るが、sixで読み書き出来ていれば問題なし？
```
- sjis-crlf.txt
PS C:\Users\ymaru\WebView2\six> $api = 'http://127.0.0.1:43486'
PS C:\Users\ymaru\WebView2\six> $fs  = 'C:\Users\ymaru\HTA\six\sjis-crlf.txt'
PS C:\Users\ymaru\WebView2\six> $q   = [uri]::EscapeDataString($fs)
PS C:\Users\ymaru\WebView2\six> Invoke-RestMethod "$api/probe?fs=$q"   # encoding=cp932, eol=dos 期待
Invoke-RestMethod : {}
発生場所 行:1 文字:1
+ Invoke-RestMethod "$api/probe?fs=$q"   # encoding=cp932, eol=dos 期待
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : InvalidOperation: (System.Net.HttpWebRequest:HttpWebRequest) [Invoke-RestMeth
   od]、WebException
    + FullyQualifiedErrorId : WebCmdletWebResponseException,Microsoft.PowerShell.Commands.InvokeRestMethodC
   ommand
```
  - ただしsixでsjis-crlf.txtを読み込んだところ文字化けもなく"shift_jis dos"となった。
- sixで新規バッファを開き(utf-8 unix)、日本語で埋めて"shift_jis dos"に変換して保存 →OK
```
encoding       : cp932
eol            : dos
bom            : False
size           : 42
mtime          : 1763102330425
utf8           : False
cp932Roundtrip : True
asciiOnly      : False
ambiguous      : Fals
```
- sixで新規バッファを開き(utf-8 unix)、日本語で埋めて"utf-8 dos"に変換して保存 →OK
```
encoding       : utf-8
eol            : dos
bom            : False
size           : 38
mtime          : 1763104009263
utf8           : True
cp932Roundtrip : False
asciiOnly      : False
ambiguous      : False
```

#567
OK。次は不具合対応を。
- NG)右端以外のタブを`:q`で閉じてもタブが消えない
  - より左側にあるタブに移った瞬間に正しくタブが詰められている
    - `:q`の瞬間に詰めて次のタブがアクティブになるべき。

#568
タブ削除の動作はOK。
- req)wsl側のファイルは':'などNTFSでは許されない文字を含んでいることがあるが、読み込めなくてよいが:e popupの一覧では文字化けしないようにしたい
  - 具体的例としてこのリポジトリ内のファイル、`//wsl.localhost/Ubuntu/home/ymaru/work/WebView2/six/256x256.png:Zone.Identifier'など。
  - 出来ればpopup一覧内で赤文字で描画して選択不可にしたい。

#569
NG。
- 一覧を取得する段階で、NTFS非許容文字の部分が文字化けしているようだ(添付スクショ)。
  - その結果、判定材料となる無効文字が含まれないことになっていると思われる。
  - 文字化けを解消した上で非許容名として扱うのがベストだが、文字化け文字もそのまま無効文字に加えて非許容名として扱う、が次善策。

#570
変化なし(添付スクショ)。

#571
- 赤文字の対象にはなったが、そもそもやりたかったことは「:なら:のまま表示」というもので．．． 文字化け前のファイル名で表示することは無理？
- `Enter`でも開けないがpopupもコマンドフロートも閉じてしまう。`Enter`/clickではツールチップと同じ文言をトーストで出してpopupは継続してほしい。

#572
- (OK)赤文字ファイルをclickしたらトースト表示とvisualbellのみでpopupは継続する
- 赤文字ファイルを`Enter`で確定したら...
  - (NG)popupの一覧内でカーソルが赤文字ファイルに差し掛かる直前のファイルに対する確定処理になっている
    - 直前のファイル名がコマンド入力欄にappend済であり、その内容に対する`Enter`としては正しい挙動ではある
    - requestの検討が不十分だった
    - (仕様変更)popup内でカーソルが赤文字ファイルに乗ったときもコマンド入力欄にappendする。そのうえで、`Enter`に対してclickと同じ挙動にする

#573
- NG)「:を:のまま表示」されていない。
- NG) popup内で赤項目にカーソルを持って行ってもコマンド入力欄に反映されない
  - そのためか、`Enter`がコマンド入力欄のファイルに対する指示としてファイルオープンされる
- req)popupが開いているときだけvisualbellの挙動を変更し、popup内だけでフラッシュするようにしたい。

#574
- NG)「:を:のまま表示」されていない。
```
Windows Terminalでlsの出力からコピペ
256x256.png:Zone.Identifier
```
```
sixのコマンド入力欄からコピペ
:e 256x256.pngZone.Identifier
```
- 赤項目を選択して`Enter`で普通にバッファ生成できている
  - 読み書き出来ないと思い込んでいたが、wsl上のファイルなので問題なしか。だが:eの処理でwslパスのときだけ'/'や末尾空白なども許すように実装するのは大変なので「NTFSでNGなファイル名はたとえwsl上であってもNG扱い」を仕様とする。

#575
- OK)「:を:のまま表示」出来ている
- NG)赤項目を選択して`Enter`で普通にバッファ生成できている
  - 「:を:のまま表示」出来ていなかったときは正常に読み込んで中身も見えていたが、':'に置き換えたファイルは実在しないのか新規バッファ扱いとなっている
    - 期待動作ではないので、やはり`Enter`で開けないようブロックしたい
- NG)現在は`:e foo:bar.txt`で新規バッファになっているが、編集して保存してもどこに保存されたのかわからない。
  - 将来的にwslに本格対応する可能性はあるが、現状ではWindows側での利用がメインなので、"foo:bar.txt"といった名前のバッファは生成できないようにチェックして弾く仕様とする
- 「フィルタ解除後の再描画でもコロンが揺れない。」←フィルタとは？

#576
- NG)赤項目を選択して`Enter`で普通にバッファ生成できている
- NG)`:e foo:bar.txt`で新規バッファとして開いてしまっている
- 追加仕様)フィルタで、_fileFilterが空でないときの仕様を追加したい
  - popup内カーソルの移動でフィルタ外のファイルをスキップする
  - フィルタ外のファイルを文字色グレーにしたい。入力欄の編集でフィルタ対象が変わればリアルタイムに追従する。

#577
- OK)赤項目を選択して`Enter`でトースト&visualbellでブロックされるようになった
- OK)`:e foo:bar.txt`もブロックされている
- OK)フィルタ外のファイルをグレーで表示
- NG)フィルタ外のファイルをスキップできていない
- 追加仕様):e入力欄でファイル名(パス名)が1文字でも入力されているときの編集キーを追加
  - caretが2文字目以降にあるとき、`HOME`キーで1文字目に飛ぶ
    - 例)":e samplefile0"の'a'～'0'にcaretがあるとき`HOME`で's'までcaretが飛ぶ
  - caretが1文字目にあるとき、`HOME`キーで`:e aaa'の先頭の:まで飛ぶ(現状の動作)
    - 例)":e samplefile0"の's'にcaretがあるとき`HOME`で':'までcaretが飛ぶ

#578
- OK)":e fname"でのHOMEの例外挙動
- NG)フィルタ外のファイルをスキップ
  - ほぼOKだが、移動対象の1つ目にいるときの`↑`、最後にいるときの`↓`で対象外までカーソルが動いてしまう
    - 仕様追加)ループするようにしよう

#579
- OK)フィルタの動作
- NG)いつからか不明だが、popup候補先頭の"../"が"(カレントディレクトリ名)/"に置き換わっている。なので"."でフィルタ外になる

#580
- OK)"../"が"../"と表示される
- NG)"../"で`Enter`を押すとNTFS無効名と同じ扱いで移動がブロックされてしまう

#581
- OK)"../"で親ディレクトリへの移動
- 親ディレクトリに移動したとき「今いたディレクトリ」にカーソルを持って行く仕様だが、それだけでなく「今いたディレクトリ名」が丸ごとフィルタにセットされているようで、他候補に移動できない。親ディレクトリに移動したときはフィルタは一旦空にすべき。
- '.'フィルタで"../"にカーソルがあるときは`Enter`で親ディレクトリに移動したい

#582
- OK)親ディレクトリへの移動でフィルタクリア
- '.'フィルタで"../"にカーソルがあるとき`Enter`でNTFS無効名と同じ扱いでブロックされる
  - 仕様変更)例外的な動きになるが、'.'フィルタでは"../"はマッチしないようにしたい。".."ならマッチする。
- フィルタ外でグレー表示している項目でもclickでは選択できる →OK。それでよい

#583
- OK)'.'で"../"をフィルタから除外
- NG)'.'でフィルタリング処理は正常に動いているがpopup内カーソルが描画されない
- NG)"../"をクリックしたとき親ディレクトリに移動するがフィルタがクリアされない
- req)「'.' フィルタ時に親エントリを常に非アクティブカラーへ統一する追加 UI 調整」←YES。フィルタ外なのでグレーにすべき

#584
OK。:e popupの更新完了。次は別の不具合修正を。
- NG):b popup内のサイズが固定で一覧が上揃えになっている
  - #534で実装したあとのどこかでデグレしたっぽい
    - #534):b popupの縦幅はバッファ数に応じて可変にする。

#585
:b popupの修正はOK。続けて:b popupの仕様追加を。
- `F1`等の左に更新フラグ(*)を赤文字で表示する
  - 更新のない場合は*は表示しないが、`F1`等の表示位置は揃える
- :b popup内でコマンドフロートが':b'のまま、つまり' 'を入力していないときに限り、
  - `d`で`:q`相当の動作を。
    - modifiedの場合はpopupを閉じで:qの確認ダイアログへ、modifiedでなかった場合は:b popup継続
- popupの先頭行の上にヘッダ行を設け、"`d`:バッファ破棄"と表示する
  - `:b`の後ろに空白が入ったら"`d`:バッファ破棄"をグレー文字にする

#586
- NG)非modifiedなカレントバッファを`d`で破棄したあとコマンドフロートが消えてしまう
- req)"`d`:バッファ破棄"の左に"`数字`,`Fキー`:ダイレクト選択"を追加して。
  - "`Fキー`:ダイレクト選択"は常時表示
  - "`数字`,"は`:b `までは表示、もう1文字「空白以外」を入力するとグレー表示。
  - "`数字`,"の左にマージン3rem。

#587
- "数字"と"Fキー"は`F1`と同じ装飾にして。
- ヘッダ行の背景はwindow.THEMEを参照して。
```
  popupHeaderBg: "#000040"
```
- ヘッダ行の上マージンを0.2rem、下マージンを0.1rem増やして。
- 仕様変更)popup内の候補が無い状態では"ダイレクト選択"も含め全部グレー表示にして。
- `Fキー`と"ダイレクト選択"の間の':'は無くそう
- "ダイレクト選択"と"`d`バッファ破棄"の間をあと1rem増やして。
- NG)非modifiedなカレントバッファを`d`で破棄したあとコマンドフロートは残るようになったがフォーカスが消える

#588
- NG)`:b foo`などと入力して"F2 foo.txt"だけが候補になっているときでも`1`でバッファ1に飛んでしまう。
  - さらに`2`でもバッファ2に飛ぶ。`:b foo2`として候補0で続くべき。
- NG)`:b foo`などと入力して"F2 foo.txt"だけが候補になっているときでも"`Fキー` ダイレクト選択"がグレーになってしまう

#589
- OK)`:b foo`では数字キージャンプ無効
- 仕様変更)`F1`～`F8`は常に有効になっていて、それでよしとするので、popupヘッダの"`Fキー` ダイレクト選択"はグレー表示なしにする
- `Fキー`と"ダイレクト選択"の間が詰まり過ぎ。`d`と"バッファ削除"の間と同じくらい空けたい。

#590
:b popupの改修は完了。次は不具合修正を。
- EOF行が改行のみのとき、EOF改行が見えなくなることがある
  - 保存すると発生しなくなるので、保存後にEOF位置にINSERTした改行に関する現象
    - INSERTモードではcaretが来たときだけ見える (他行にcaretがあるときは見えず、本来は`↓`で移動できないはずなのに移動できる)
    - EOF改行にcaretがあるときにNORMALモードに移行するとEOF改行はcaretがどいてもそのまま残る
      - ただしINSERTモードにした瞬間に(caretが乗っていなければ)EOF改行が見えなくなる
  - 保存すると、EOF改行は普通の改行に変わる。
    - その後ろにINSERTした改行では同じことが起こる
    - 保存した後も普通の改行に戻らないこともあり、条件がよくわからない。

#591
EOF行のEOL記号が見えなくなったりする現象は収まってsix上でだけ見ていると不整合はなくなったが、EOF改行が保存されなくなった
- utf8-unixのファイルで「通常行(末尾EOL改行) - EOF行(EOL改行)」の状態で保存後に確認すると末尾に改行(0x0a)が1つしか付いていない。
```
$ tail -c 4 foo.txt | xxd
00000000: e380 820a
```

#592
変わっていない。NG
six上では4行目がEOF行(改行のみ)だが、保存したファイルを確認すると最後の改行が消えている。(添付スクショ,foo.txt)
```
[~/work/WebView2/six]$ wc --lines foo.txt;tail -c 5 foo.txt | xxd
3 foo.txt
00000000: 0a4c 2e33 0a                             .L.3.
```

#593
NG。保存する度にsix上で見ても末尾の改行が増えていく。パッチは破棄した。今#591後の状態

#594
- NG)EOF付近がバイナリで"33 34 0a 0a"のときはsixで保存しても変わらないが"33 34 0a"のときはsixで保存すると"33 34"になる
- NG)EOF付近がバイナリで"33 34 0a 0a 0a"のとき1つ目や2つ目の0aを`1x`で削除すると"33 34 0a"になる
- NG)EOF付近がバイナリで"33 34 0a 0a 0a"のとき2つ目や3つ目の0aを`1dd`で削除すると"33 34 0a"になる

#595
NG。
末尾行のEOFに対する処理が特殊になるのは、Vimや標準viと違って行末の改行位置までcaretを持って行ける仕様のためだったと思うが、caretの仕様をそのままでEOF付近の特殊処理をやめるとどのような支障がある？

#596
req)EOF付近の改行に関する特殊処理を全部やめてみて。

#597
prompt#590～#596は破棄した。
:b popupの改修は完了。次は不具合修正を。
- req)EOF付近で改行文字を自動で足したり削除したりする処理を廃止して。

#598
添付したように、foo.txtはsixで見ると3行から成り、3行目の末尾は改行文字でありEOFでもある。
これをTerminalで確認すると下記のように出力され、EOFの改行が削られているようだ。「削られた分を足す」という対処はせず「削っている箇所を修正」して。
```
[~/work/WebView2/six]$ wc --lines foo.txt;tail foo.txt | xxd
2 foo.txt
00000000: 6666 660a 6666 660a 4241 4241 42         fff.fff.BABAB
```

#599
色々観察すると下記の(A)～(D)のようになっていて、(C)に違和感がある。
- とりあえず、下記の{LF}(読み込み時にsixが付与した改行)をwindow.THEMEで指定した色でlist表示してくれる？
```
  dummyLFColor: "orange",
  dummyCRLFColor: "green",
```
(A)utf8-unixの下記のようなファイルを外部から:eで読んだ場合
```
ABC[LF]
DEF
```
1. sixは行末に改行無しは扱えないのでDEF行に{LF}を付与(付与フラグON)
```
ABC[LF]
DEF{LF}
```
2. ABC行を編集して`:w` →付与フラグONなので{LF}を削除
```
ABC[LF]
DEF
```
3. →保存→OK (確認した)
(B)utf8-unixの下記のようなファイルを外部から:eで読んだ場合
```
ABC[LF]
DEF[LF]
```
1. 最初からDEF行の行末に改行があるので{LF}は付与しない(付与フラグOFF)
2. ABC行を編集して`:w` →付与フラグOFFなのでそのまま保存 →OK (確認した)
(C)utf8-unixの下記のようなファイルを外部から:eで読んだ場合
```
ABC[LF]
DEF
```
1. sixは行末に改行無しは扱えないのでDEF行に{LF}を付与(付与フラグON)
```
ABC[LF]
DEF{LF}
```
2. DEF行の末尾で改行
2-a. 期待動作) {LF}行を編集したので[LF]に置き換えて付与フラグをOFFにする。
```
ABC[LF]
DEF[LF]
[LF]
```
2-b. 実際(想像) {LF}の前に[LF]を挿入
```
ABC[LF]
DEF[LF]
{LF}
```
3. `:w`で保存 → DEFの後ろに改行は1つだけだった(2-bからの保存時に{LF}が削除されたと思われる)
(D)utf8-unixの下記のようなファイルを外部から:eで読んだ場合
```
ABC[LF]
DEF
```
1. sixは行末に改行無しは扱えないのでDEF行に{LF}を付与(付与フラグON)
```
ABC[LF]
DEF{LF}
```
2. DEF行のEを削除
```
ABC[LF]
DF{LF}
```
3. `:w` →付与フラグがONなので{LF}を削除 →保存 →OK (確認した)

#600
- 仕様が曖昧だったが、Dummy行末記号の位置で改行した場合は通常改行に置き換えるものとする。
  - Dummy行末記号を{LF}で表すと、現状は読み込んだ直後に```DEF{LF}```だったところ、Fの後ろで改行すると```DEF[LF][LF]```と表示されている。正しくは```DEF[LF]```になるべき。
保存すると正しく```DEF[LF]```になっている。

#601
- "ABC`LF`"のように空行でない最終行の末尾にある改行は削除してDummy行末記号に置き換えられるが、最終行が`LF`だけだと削除できない。出来るようにしよう。
- Dummy行末記号を含まない最終行でINSERTモードで`j`や`↓`で次行に移動したときはDummy行末記号を挿入するようにしよう。
- 「なければオレンジ系にフォールバック。」→window.THEMEの定義がなかった場合のフォールバックはyellowに固定しろって強く言ったつもりだが。

#602
- 「次にこの2点を追加実装しますか？」→YES

#603
- 仕様変更)INSERTモードで`↓`など下方向の移動で最終行からさらに下の行へは移動できないものとする。
  - 削除操作の結果として「caret行が改行も含めて空」になったときは、前行にcaretを移動する
  - 削除操作の結果として「caret行が空行ではないが改行がない状態」になったときは、Dummy行末記号を表示する
  - Dummy行末記号は`:w`での保存対象外(=Dummy行末記号を削除した状態が保存対象)
  - Dummy行末記号の位置で改行を入力した場合はDummy行末記号を通常の改行で置き換える
  - Dummy行末記号がある状態で最終行の下に行を追加する操作(`o`)をした場合はDummy行末記号を通常の改行で置き換えた上で次行として通常の改行をappendする

#604
- NG)Dummy行末記号にcaretがあるときに`j`で次行が発生しないのは正しいが、通常の改行に置き換えられてしまう。
  - さらにmodifiedのフラグまで立ってしまう。`j`でmodifiedなどあり得ない。
    - undoバッファには入っておらず、undo操作で戻すことはできない。
- NG)EOF付近が"abc`LF``LF`"のときに"abc"直後の`LF`を削除すると"abc{LF}"になってしまう。({LF}はDummy行末記号を表す)
  - どういうロジックに従った結果？ 仕様の漏れ(矛盾 or 未定義)があるかもしれないが．．．

#605
- NG)EOF付近が"abc`LF``LF`"のときに1つ目の`LF`を`x`で削除すると"abc`LF`"になる(OK)が、undoすると"abc`LF``LF``LF`"になってしまう
- NG)EOF付近が"abc`LF``LF`"のときに2つ目の`LF`を`dd`で削除すると"abc{LF}"になる(NG) ({LF}はDummy行末記号を表す)

#606
- NG)EOF付近が"abc`LF``LF`"のときに1つ目の`LF`を`x`で削除すると"abc`LF`"になる(OK)が、undoすると"abc`LF``LF``LF`"になってしまう
  - #605で指摘したまま変化なし
- NG)EOF付近が"abc`LF``LF`"のときに2つ目の`LF`を`dd`で削除すると"abc`LF`"になる(OK)が、undoすると"abc`LF``LF``LF`"になってしまう
説明に違和感。「元テキストが」は一切関係ないはず。

#607
- NG)"abc'LF'"→`o`→"abc`LF``LF`"→`:w`で保存されたファイルを確認すると末尾に`LF`が1つしか付いていない。
- NG)EOF付近が"abc`LF``LF`"のときに1つ目の`LF`を`x`で削除すると"abc`LF`"になる(OK)が、undoすると"abc`LF``LF``LF`"になってしまう
  - #605で指摘したまま変化なし
- NG)EOF付近が"abc`LF``LF`"のときに2つ目の`LF`を`dd`で削除すると"abc{LF}"になってしまう ({LF}はDummy行末記号を表す)

#608
- "abc'LF'"→`o`→"abc`LF``LF`"→`:w` →OK。保存されたファイルを確認すると末尾に`LF`が2つ付いていた。
- NG)"abc`LF``LF`[EOF]"で2つ目の`LF`を`dd`で削除 →見た目に何も変化がないがmodifiedフラグが立つ。undoすると`LF`がひとつ増えて"abc`LF``LF``LF`[EOF]"となり、modifiedが消える。
- NG)EOF付近が"abc`LF``LF`"のときに1つ目の`LF`を`x`で削除すると"abc`LF`"になる(OK)が、undoすると"abc`LF``LF``LF`"になってしまう
  - #605で指摘したまま変化なし

#609
- ケースA: `dd`で見た目に変化ないがmodifiedになり、`u`で`LF`が増える →NG
- ケースB: 'x`で見た目はOKだが`u`で`LF`が増える →NG
- ケースC: OK

#610
CaseA: →NG
CaseB: →NG
- finalLFRun(末尾連続LF数)など記録する必要はないはず。常に静的な状態で判断できるはず。
- 「行削除_deleteWholeLines後、表示行集合（簡易比較）が削除前後で同一なら」←削除前後の比較など不要で、静的な状態で判断できるはず。
- 「dd/x 系削除は生分割基盤と LF 連続数保存により」←LFが何個連続しているかなど気にする必要はないはず。

#611
- CaseA: NG
  - 期待動作が異なる。「最終空行の改行文字が削除され、modifiedがカウントアップしてundoバッファに入りcaretはabc行末へ移動」が期待動作。
- CaseB: undoでLFが1つ増える(NG) ただし、増えた状態で他バッファに切り替えて戻ってくると正しい状態(LF2つ)になっているので表示だけの問題と思われる
- CaseC: OK

#612
- CaseA: NGだが、おそらく表示の問題だけで内部的には期待動作になっている。
  - `dd`で見た目が変わらない →他バッファに行って戻ってくると正しく削除されている(表示も正しくなっている)
  - undoでLFが1つ余分に挿入される →他バッファに行って戻ってくると正しくundoされている(表示も正しくなっている)
― CaseB: NG。症状変わらずで、他バッファに行って戻ってくると期待動作の見た目になっている
- 「表示だけ正しくない」という不具合は慎重かつ徹底的に潰してほしい。そうでないと今後も実装結果を検証するのが大変なので。

#613
- CaseAもCaseBも変化なし。内部動作的には正しそうだが一時的な表示が正しく反映できておらず、他バッファに行って戻ってくると正常になる。
  - `dd`で削除しても効かないように見えるケースでは、caretを他行に移してから他バッファに行って戻ることで正しくなるが、`dd`実行時のまま他バッファに行って戻っても正しくならない。
    - なので、表示だけではなく「caret位置の自動移動」も効いていない。(`dd`で削除された行に残れないので前行にcaretを移す処理)

#614
何も変わっていない。
- CaseA: `dd`→何も起きない。NG。他バッファへ行って戻ってきても変わらない。
  - `dd` →何も起きない→`k`でcaretを上に避けたのち他バッファへ行って戻ってくると正常な結果になっている
- CaseB: `x`→LF１個→undo→LF3個。NG。他バッファへ行って戻ってくるとLF2個になっている

#615
CaseAもCaseBも何も改善されていない。
新たな専用フラグを設けるとか場当たり的な対応は入れたくない。シンプルな仕様をキープして単純バグを潰すべき。
- 解決のヒントになるかわからないが'x'の別例を。
  - "abc`LF``LF`xyz`LF`"というバッファで、abc直後の`LF`を`x`で削除→"abc`LF`xyz`LF`"→`u`→"abc`LF``LF`xyz`LF``LF`"となる
    - `x`で削った`LF`の直後ではなく全体の末尾に余分な`LF`が付与されてしまっている。
    - 他バッファへ行って戻ってくると正しくなっている
  - "abc`LF``LF`xyz{LF}"というバッファで、abc直後の`LF`を`x`で削除→"abc`LF`xyz{LF}"→`u`→"abc`LF``LF`xyz{LF}"となる(OK)

#616
OK。CaseAもCaseBも即座に正しい結果が表示上も保存先ファイル上でも確認できた。
次は似ているが別ケースを。
- "abc`LF``LF`A{LF}"というバッファで'A'の行(最終行)を`dd`で削除すると"abc`LF`"になってしまう。NG
  - 期待動作は「"abc`LF`LF`"になって2つ目の`LF`にcaretが移動する」

#617
- Case#616 →OK
- 改行文字だけから成る最終行の改行を`x`で削除したときにcaretが前行の行頭に移動するが、行末に移動してほしい。
- 脱線だが、改行文字、Dummy行末記号を`LF`、{LF}で表しているけど、1文字で表す記号ないかな？コピペするから簡単に入力できなくてもいい。

#618
- あくまでもpromptでの話だが「通常の改行文字:␊, Dummy:␤」としよう。
- "abc␊␊"で末尾␊を`x`で削除→行頭('a')にcaretが移動してしまっている。NG

#619
変わらず。
- "abc␊␊"で末尾␊を`x`で削除→行頭('a')にcaretが移動してしまっている。NG

#620
変わらず。
- "abc␊␊"で末尾␊を`x`で削除→行頭('a')にcaretが移動してしまっている。NG

#621
OK。`x`後のcaret位置が期待動作になった。
- "abc␊␊␊"の2つ目の␊で`2x`としたら"abc␊"の先頭にcaretが移動したけど．．．あまりやらない操作だから放置でもいい
INSERTモードで"abc␊"の'␊'を`DEL`などで削除すると␤になる(OK)が、
- ␊だけの最終行で␊を削除しても見た目は␤にはならない(NG)
  - コード的には␤になっているようで、1文字挿入すると"a␤"のようになる。

#622
- 確認1) NG。変化なし
  - "abc␊␊"の2つ目の␊についての報告で、その修正がNGだった
  - 前行もなく先頭行が"␊"だけのファイルだった場合は、削除すると␤になっている(OK)

#623
NG。
- "abc␊␊"の2つ目の␊をINSERTモードで削除した場合
  - 期待動作）"abc␊␤"
  - 実際動作）"abc␊␊" (何も変化ないように見える)
    - ただし1文字挿入すると"abc␊X␤"などと␤に変化するので、見た目と中身が一致していないだけかもしれない

#624
NG。#623から変化なし。

#625
NG。
- `:e`で読み込んだ直後のファイルで"abc␊␊"の2つ目の␊をINSERTモードで削除した場合
  - 期待動作）"abc␊␊"→"abc␊␤"
  - 実際動作）"abc␊␊"→"abc␊␤"→"abc␊␊" と一瞬で遷移する。チラつき程度にしか␤が見えない。

#626
- INSERTモードで`DEL`で削除した直後は期待動作になったが、
  - "abc␊␤"→"abc␊X␤"としたあと'X'を削除すると"abc␊␊"になってしまう NG
  - "abc␊␤"で␤から`↑`で前行に移ると"abc␊"になってしまう NG

#627
- "abc␊␤"→"abc␊X␤"としたあと'X'を削除すると"abc␤"になってしまう。NG。期待動作は"abc␊␤"
  - 「'X'を削除して改行もない空行になったら前行にcaret移動」というルールに沿っているように見えるが、ここで␤も改行として判定すべき。

#628
- "abc␊␤"→"abc␊X␤"としたあと'X'を削除すると"abc␊␊"になってしまう。NG。期待動作は"abc␊␤"
  - おそらく実態は"abc␊␤"で表示だけ"abc␊␊"になってるっぽい。

#629
NG。期待値"abc␊␤"のところが"abc␊␊"になっている(おそらく表示だけ)
- 「特定遷移時にダミー強制表示フラグを立てる処理を追加」←安易に特異ケースのフラグを追加して処理を複雑にしないで。␤が仕様に反して␊に置き換わってしまうのは「余計な特殊処理」が原因なのではないか？␤は␤のままというシンプルな処理でよいはず
  - ␤に対する特殊な扱いは下記の2点だけで、他の場面では␊と同じ扱いで良いのでは？
    - 保存処理のときにファイル末尾が␊␤だったら␤を削る
    - ␤にcaretがあるときに`Enter`が押されたら␊に置き替える(␊␤にはしない)

#630
NG。変わっていない。
- ␤に対するルールを少し書き換える(保存処理) ※End Of Bufferを[EOB]と表記
  - ①保存処理のときにバッファ終端が"␤[EOB]"だったら␤を削る (␤は存在しないコードをsix上で可視化してるだけなので)
  - ②␤にcaretがあるときに`Enter`が押されたら␊に置き替える(␊␤にはしない)
※␤はバッファ内に0個ないし1個しか存在しない。存在する場合は必ず"␤[EOB]"のはず。
- ␤に対する特殊な処理は上記の2つでよいが、加えて␤を挿入する特殊な処理を2つ。
  - ③`:e`などでファイルオープンしてバッファに取り込んだとき終端が"␊[EOB]"でなかったら␤を挿入して"␤[EOB]"にする
  - ④INSERTモードで"␊[EOB]"の␊を削除する操作は␤への置き換えとする("␤[EOB]"になる)。そのまま保存するとルール①により削除される

#631
NG。INSERTモードで"abc␊X␤"のXを削除したら"abc␊␊"になっている(おそらく表示のみ)。期待値は"abc␊␤"。
- 「仕様どおりダミー最終改行の記号を「␤」に切り替える小変更を入れました。」→␤の表示は␊と同じ'↲'でいい。"␊␤"はprompt内のみ。

#632
期待動作ではないけど、今のままでもいい気がしてきた。1点だけ、
- "ab␊[EOB]"のとき、INSERTモードで'a'や'b'にcaretがいるときは`↓`で次行に移動できる(行番号,␊␤なし)が、末尾の␊からは移動できない。これが移動できれば整合性がとれてOKなんだが。

#633
OK。␤周りの修正は完了。次は不具合修正
- いつの間にかposInfo表示が書き換えられている。「行Y, 列X/W」だったはず。

#634
- posInfo表示の内容はOK。
- window.THEMEの色が反映されていない NG
```
  posInfoText: "#c0ffca", // "行Y, 列X/W"
```
※未定義時はyellow

#635
OK。
- 「#632 の下矢印挙動を統一する改善」←一度実装したらデグレが酷かったのでパッチを破棄したんだが、慎重に、最低限の修正をしてみてくれる？
  - INSERTモードで"a␊[EOB]"の'␊'から`↓`で次行に移動可能にする('a'の位置から`↓`のと同じ挙動にする)

#636
NG。INSERTモードで"a␊[EOB]"の␊にcaretが乗っただけで次行に行番号付きで␊が現れる。caretが移動すると消える。

#637
整合性は取れているようだが．．．
- `↓`で移動したあと文字を挿入したら␤を␊に変換したい。「␊無し行」が新たに保存されるのは意図的に[EOB]前の␊を削除した場合のみに限定したいので。

#638
NG。変わっていない。
- 「条件: prev が末尾LFあり / cur が末尾LF欠落 / caret が EOF / cur が prev より長い」
  - 「prevが末尾LFあり」という条件は不要(空バッファ以外では必ずtrueになるし空バッファでも適用したいので)
  - 「cur が末尾LF欠落 / caretがEOF」→「caretが"[行頭]␤[EOB]"の␤」でいい。
  - 「cur が prev より長い」←なぜ？理由がわからない。不要では？

#639
OK。次は不具合対応を。
- 起動直後に`:e noexist.txt`で新規バッファを作成したとき、コマンドが履歴に入らない。
  - ファイルが存在して読み込んだ時は履歴に入るようだ。

#640
NG。変化なし。
- 起動直後に`:e noexist.txt`で存在しないために空の新規バッファを作成したとき、コマンドが履歴に入らない。
- `:e fname`でfnameが存在しなかった場合に、おそらく正常処理だろうがConsoleにExceptionなどの情報が出力されるのを抑止したい。
```
_six.js:2508   GET http://127.0.0.1:46083/read?fs=%5C%5Cwsl.localhost%5CUbuntu%5Chome%5Cymaru%5Cwork%5CWebView2%5Csix%5Cfuga 404 (Not Found)
_fetchTextWithTimeout @ _six.js:2508
_loadFromPath @ _six.js:2555
(匿名) @ _six.js:8533
(匿名) @ _six.js:8541
_six.html#api=http%3A%2F%2F127.0.0.1%3A46083%2F:1  Access to XMLHttpRequest at 'file://wsl.localhost/Ubuntu/home/ymaru/work/WebView2/six/fuga' from origin 'file://' has been blocked by CORS policy: Cross origin requests are only supported for protocol schemes: chrome-extension, chrome-untrusted, data, edge, http, https, isolated-app.
_six.js:2494   GET file://wsl.localhost/Ubuntu/home/ymaru/work/WebView2/six/fuga net::ERR_FAILED
(匿名) @ _six.js:2494
_fetchTextSmart @ _six.js:2481
_loadFromPath @ _six.js:2561
await in _loadFromPath
(匿名) @ _six.js:8533
(匿名) @ _six.js:8541
_six.html#api=http%3A%2F%2F127.0.0.1%3A46083%2F:1  Access to fetch at 'file://wsl.localhost/Ubuntu/home/ymaru/work/WebView2/six/fuga' from origin 'file://' has been blocked by CORS policy: Cross origin requests are only supported for protocol schemes: chrome-extension, chrome-untrusted, data, edge, http, https, isolated-app.
_six.js:2498   GET file://wsl.localhost/Ubuntu/home/ymaru/work/WebView2/six/fuga net::ERR_FAILED
(匿名) @ _six.js:2498
Promise.catch
_fetchTextSmart @ _six.js:2496
_loadFromPath @ _six.js:2561
await in _loadFromPath
(匿名) @ _six.js:8533
(匿名) @ _six.js:8541
_six.js:2508   GET http://127.0.0.1:46083/read?fs=%5C%5Cwsl.localhost%5CUbuntu%5Chome%5Cymaru%5Cwork%5CWebView2%5Csix%5Cfuga 404 (Not Found)
_fetchTextWithTimeout @ _six.js:2508
_loadFromPath @ _six.js:2569
await in _loadFromPath
(匿名) @ _six.js:8533
(匿名) @ _six.js:8541
_six.js:2526  Disabling local API temporarily
_apiNoteFailure @ _six.js:2526
_loadFromPath @ _six.js:2569
await in _loadFromPath
(匿名) @ _six.js:8533
(匿名) @ _six.js:8541
_six.js:2664  open failed TypeError: Failed to fetch
    at _six.js:2498:16
    at async _loadFromPath (_six.js:2561:21)
    at async _six.js:8533:32
_loadFromPath @ _six.js:2664
await in _loadFromPath
(匿名) @ _six.js:8533
(匿名) @ _six.js:8541
```

#641
NG。変化なし。
- 起動直後に`:e noexist.txt`で存在しないために空の新規バッファを作成したとき、コマンドが履歴に入らない。

#642
OK。コマンド履歴対応は完了。次。
- 何時間も放置したあとの:wや:eで失敗する。即時終了して再開すれば問題なく実行できる。シンプルに対処は可能？

#643
とりあえずデグレはなさそうなので、数時間放置を試したあと問題があれば報告する。一旦完了。
- six自身の複数起動を抑止したい。
  - 起動時、一番最初(セッション情報から再現するより前)に「既に起動しているか」をチェックする

#644
複数起動の抑止は動作OKっぽいが、six.ps1を起動していたpowershellが自動で終了しなくなった(Ctrl-Cで終了する)。関連はありそう？

#645
後から起動した方のアプリウインドウが一瞬も開くことなく、警告も表示されず何も起きない感じになった。
Ctrl-Cでの終了が必須になったっぽいのは「元から起動してた方」。複数起動に関係なく、単独起動・単独終了でもpowershellを終了させるのにCtrl-Cが必要になった
対処可能なら対処したい。

#646
まだCtrl-Cが必要だが、元々長時間起動終了を繰り返したときなどに陥っていた症状なので、今回の多重起動抑止とは無関係かもしれない。
しばらく様子を見て再度相談することにする。一旦完了。
次はキーバインドを追加したい。
  - NORMALモード/VISUALモードで`HOME`を`0`と同等に。
  - NORMALモード/VISUALモードで`END`を`$`と同等に。
  - ヘルプにも追記を。

#647
「必要なら、VISUALタブに h/j/k/l などの基本モーション一覧も追記できます。表示確認して問題なければ、ほかのヘルプタブの表記ゆれや補足も整えますか？」→お願い。

#648
OK。
INSERTモードのヘルプを充実させたいが、sixで特に実装したわけではないtextarea標準のキー操作なども書ける？

#649
OK。次は機能追加を。
- VISUAL選択中の機能を追加する。Vimをお手本にしているが、sixの仕様と合わないところがあったら指摘して。
  - o (caret位置を選択範囲の先頭⇔末尾でトグル)
  - p (paste)
  - `i{`, `i}`, `a{`, `a}`
  - gU (大文字化)
  - gu (小文字化)

#650
OK。次はNORMALモードのsを実装したい。Vimと同じで`cl`と同じだと認識している。

#651
- ヘルプ(NORMALタブのオペレーター欄)に記載がない(空項目が増えた？)
- `x`などと同様に、1文字以下の削除ではyankバッファをリプレースしない仕様にしたい。
- `S`は実装不要。使わないので。
- VISUAL選択中の`g~`は実装不要。使わないので。将来的にVISUALモード中のオーバーレイガイダンス表示を実装したら操作を増やすかもしれないが。
- 「テキストオブジェクト拡張」とは？(質問のみ。実装はしないで)

#652
- ヘルプの記載が見当たらない。「オペレーター`p`」の下に「・」だけの項目がある(添付スクショ)
- 仕様変更。`x`同様にVimと異なる挙動にはなるが、改行文字も削除対象にしたい。`cl`も同様。

#653
- `s`の挙動・ヘルプ内容はOK
- `s`と`cl`とで相違がないようにしたいので、`cl`でも同様に改行文字も削除対象に。

#654
- NG)`cw`,`CW`の挙動を変更しろなどとは言っていない。word単位に改行も含めるのは望んでいないので元に戻して。
- NG)`cl`の挙動を`s`に合わせろと言っているが、変更されていない。`cl`で改行も削除対象とみなすように。

