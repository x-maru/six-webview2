# VISUAL

選択範囲の操作。`:s`との連携(`:'<,'>`自動挿入・ハイライト維持)。

`Y`選択範囲をWindowsクリップボードへコピーします（行選択/文字選択とも対応）。空の場合はWindowsクリップボードを更新しません。成功時のみ「Copied to Windows clipboard.」トーストを表示します。

## 移動(選択拡張)
- `h`,`←`左へ1文字（選択調整）
- `j`,`↓`下へ1行（選択調整）
- `k`,`↑`上へ1行（選択調整）
- `Alt+j`,`Alt+k`スムーズスクロール（上/下）
- `l`,`→`右へ1文字（選択調整）
- `gg`先頭へ（選択範囲更新）
- `G`末尾へ（選択範囲更新）
- `0`行頭へ（選択調整）
- `Home`行頭へ（`0`と同等。選択調整）
- `^`空白後の行頭へ（選択調整）
- `$`行末へ（選択調整）
- `End`行末へ（`$`と同等。選択調整。前置カウント対応）
- `w`,`b`単語単位で進む/戻る（選択調整）
- `e`,`ge`単語の末尾の次へ進む/末尾の次へ戻る（選択調整）
- `W`,`B`WORD 単位で進む/戻る（選択調整）
- `E`,`gE`WORD の末尾の次へ進む/末尾の次へ戻る（選択調整）
- `Nw`,`Nb`,`NW`,`NB`N回分まとめて移動（例: `3w`。選択調整）
- `Ne`,`Nge`,`NE`,`NgE`N回分まとめて移動（例: `3e`。選択調整）
- `{`段落/空行区切りの前へ（選択調整）
- `}`段落/空行区切りの次へ（選択調整）

## 操作
- `y`選択範囲をヤンク(six内部コピーバッファに格納)
- `Y`Windowsクリップボードへコピー
- `d`選択削除（同時にヤンク）
- `c`選択削除 + INSERT へ
- `o`caret を選択の反対端へトグル（anchor/caret 入替）
- `p`選択範囲をヤンクバッファ内容で置換（終了して NORMAL）
- `>`,`<`インデントを 'shiftwidth'分 増減。前置カウント `N`で`N`倍量。空行は変更しません。行頭の連続`TAB`は保持し、直後に空白を挿入/削除します
- `TAB` `>`と同等（前置カウント対応、VISUAL継続）
- `Shift+TAB` `<`と同等（前置カウント対応、VISUAL継続）

## 検索起動
- `/`選択文字列を初期値として前方向インクリメンタル検索入力へ。選択中の改行は'\n'に、'TAB'は`\t`にエスケープ表示。正規表現メタ文字は自動的にリテラル化（エスケープ）
- `?`選択文字列を初期値として後方向インクリメンタル検索入力へ。エスケープ仕様は同上

## 大文字/小文字変換
- `gU`選択範囲を大文字化（VISUAL継続）
- `gu`選択範囲を小文字化（VISUAL継続）

## テキストオブジェクト（簡易）
- `i{`,`i}`最も近い {…} の内部を選択（ネストは最内でなく最外を簡易検出）
- `a{`,`a}`最も近い {…} 全体を選択（波括弧含む）
   ※ ネスト対応は簡易。複雑な入れ子では最初の対応する括弧を採用する
